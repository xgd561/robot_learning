# 第8周 · 教案（理论课）🤖
### 给七年级小朋友的「巡线跟随」第八课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让机器人自己盯着地上的黑线、自己掌舵跟着走"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：变形车（运动前缀 `transform_`）**
> 这周的"巡线小车"由**变形车**来跑——摄像头盯黑线，它沿黑线走，遇小障碍还能抬升车身越过。形态就藏在代码里：巡线模型、偏移量修正通用，只有"怎么走"换成 `transform_`，还能多调用车身升降的 `transform_*` 方法，体现它"变形越障"的本事。装变形车时注意升降机构的舵机行程，别装反卡死。

前几周我们让机器人看墙自己停（第4周避障）、认手势/颜色自己动（第6、7周）。这周让它升级成**会"走路"的小车**：用摄像头认出地上的黑线，自己沿着线走，线往哪歪它就往哪修，像有轨道的小火车——但轨道是它自己盯出来的。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"巡线模型"是啥**：知道 `line_recognition` 让摄像头认出黑线，并告诉我们"线偏左还是偏右、偏多少"。
2. **加载模型 + 读线信息**：能写出 `ug.load_models(['line_recognition'])` 再读 `ug.get_single_track_total_info()`（和第6、7周"先装模型再读结果"一个套路）。
3. **说清楚"偏移量 offset"**：知道它就是"黑线偏离画面中心多少"——正数=偏右、负数=偏左（具体正负以你打印出来的为准，可能反过来），就是机器人该往哪边修的方向盘。
4. **用 `if / elif` 做"转向修正"**：偏右往右修、偏左往左修，偏很小就直走。
5. **用简化版 PID 的"P"思路**：明白"偏越多、修越多"（修正速度 = 偏移量 × 系数，并封顶），再用 `while True` 一直跟着线 + `try/except` 安全网。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人靠我们遥控、或靠"看墙决定停"；这周之后，它**自己用摄像头一直盯线、自己掌舵**——你写好规则，它替你沿着线一直走。这就是"自动驾驶"的雏形！记住这种"它自己顺着黑线走，我只在旁边看"的惊喜感。

**📌 一句话目标：** 用摄像头认黑线 + 偏移量修正，做出第一辆"自己沿黑线走"的小车：线偏右就往右修、偏左就往左修，稳定跟一段不掉线。这一步通了，避障、手势、巡线全是在"看 + 判断 + 修正"这棵树上开花。

---

## 一、这周我们要干啥？🚀
第4周我们让机器人"看墙、太近就停"；第6、7周让它"认手势、认颜色"。这周给它安一双**盯着地面的眼睛**，让它自己沿着地上的**黑胶带**走：

- 摄像头朝地面拍，把黑线从地板里"抠"出来；
- 算出黑线偏左还是偏右、偏多少；
- 线往哪歪，它就往哪修一点，一路顺着线走。

要做到这个，你要认识三个新朋友：**巡线模型 line_recognition**、**偏移量 offset**、还有简化版 **PID 调速（只讲 P）**。

## 二、机器人的"新眼睛"：摄像头认黑线（line_recognition 模型）
第6周认手势、第7周认颜色，都是"先装模型、再读结果"。这周认黑线一模一样：

- **加载模型**：一行就够，告诉机器人"我要用认黑线的本事了"：
```python
ug.load_models(['line_recognition'])   # 装上"认黑线"的模型
```
- **读线信息**：模型装好后，就能问它"线在哪"：
```python
信息 = ug.get_single_track_total_info()   # 读"单条黑线"的信息
```
  （如果你贴了**两条**平行线当轨道，用 `ug.get_double_track_total_info()` 读双线。这周先用单线最简单。）
- 它返回一大串"黑线信息"，里面最关键的一个数叫**偏移量**——下面细讲。

> 💡 小比喻：`line_recognition` 就像给摄像头装了个"黑线探测器"——装上它，摄像头才认得地上是黑线还是地板。
> ⚠️ 小提醒：不同 SDK 版本里模型名可能略有差别。如果 `load_models(['line_recognition'])` 报错说找不到这个模型，去编辑器里看看能加载的模型列表（或翻官方文档 `docs.ubtrobot.com/ugot/`）确认正确的名字，把上面引号里的换掉就行。

## 三、偏移量 offset —— 线偏在哪、偏多少
"偏移量"就是**黑线在画面里偏离中心多少**：

- 我们约定：**正数 = 线在画面右边，负数 = 线在左边**（具体正负以你打印出来的为准，可能反过来；反了就把左右对调）。
- 偏移量越大，说明线偏得越厉害，机器人该修得越多。

> 💡 小比喻：你开车低头看地上的线，线在你右边你就往右靠、线在左边就往左靠。偏移量就是"该往哪边靠、靠多少"的那个数——它就是机器人的方向盘。

## 四、简化版 PID 调速：只讲"P"（偏越多，修越多）
完整 PID 像三个旋钮（P、I、D）一起调，工程师用它让车丝滑跟线。这周我们只装**最简单也最管用的一颗旋钮：P**。

- **P（比例 Proportion）**：线偏得越多 → 转得越多；偏得少 → 轻轻修。就像骑自行车，歪一点轻轻扶，歪很多用力扶。
- 怎么用？**修正速度 = 偏移量 × 系数**，但**封顶**（别转飞）：
  - 偏移 = 20、系数 = 3 → 修正 60；
  - 偏移 = 50、系数 = 3 → 修正 150，但封顶 120，所以实际 120。
- I（积分）、D（微分）以后再说，先让它能顺着线走就行。

> 💡 "系数"和"阈值（多小的偏移算直走）"都是你要调的旋钮——调着调着它就更稳，这就是调试思维。
> 一句话：**P = 看偏移、按比例打方向盘，偏越多打得越狠，但别打死。**

## 五、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

> 小提醒：本周循环里要"歇一下"别让小车太累，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 六、这周学会后，你应该能回答这几个问题
- `line_recognition` 模型加载后干嘛用？（提示：让摄像头认黑线）
- `get_single_track_total_info()` 读出来最关键的是啥？（提示：偏移量 offset）
- 偏移量正数代表线在左还是右？（提示：约定偏右为正，但要以你打印为准）
- 简化 PID 里 "P" 是啥意思？（提示：偏越多、修越多）
- 为什么修正速度要"封顶"？（提示：别转飞、别晃）

> 答得上来，理论就过关啦！翻《实践手册》去造你的第一辆巡线小车 💪
