# UGOT 产品详解（备查：孩子搞不清形态/传感器/能干什么时，据此回答）

## 一句话定位
UGOT 是优必选（UBTECH）出的**多形态、强 AI 机器人套件**：模块化拼搭 + 高算力主控 + 开放 Python 编程，让学生轻松体验前沿机器人技术。官方文档：https://docs.ubtrobot.com/ugot/ 。适配小学/初中/高中不同学段的人工智能学习。

## 七种形态（一套主控换不同结构件，先确认孩子手里是哪种）
- **轮足机器人 Wheeled&Legged**：双足轮腿直立平衡，腿能屈伸适应高低地面，机身在高度起伏中保持海拔稳定。
- **麦轮车 Mecanum**：四轮全向移动、无角度限制、原地旋转，适合复杂狭小空间（本教案运动示例默认以它为基准）。
- **平衡车 Self-balancing**：双轮自平衡，可过小障碍、能负载。
- **蜘蛛机器人 Spider**：四足仿生，全向移动。
- **变形车 Transforming**：机体高度可调，越障 / 翻台阶 / 越野。
- **四足狗 Quadruper**：四足仿生步态，模拟真实狗狗动作。
- **工程车 Engineer**：带 3 轴自由度机械臂，抓取 / 取放 / 定位。

> 形态对应代码：把运动方法里的 `mecanum_` 换成 `balance_`(平衡车) / `wheelleg_`(轮足) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车)。工程车额外有机械臂方法 `mechanical_*`。

## 核心硬件规格（孩子问参数时照此答）
- 主控芯片 **RK3566**，1 TOPS AI 算力。
- 2.4 寸触控屏（可显示文字/表情）。
- 9 轴陀螺仪（测姿态/平衡）。
- 3 麦克风阵列，自研 ASR/TTS/NLP（语音识别 / 语音合成 / 自然语言）。
- 开放 **100+ 个 Python 接口**；结构开放；带 **GPIO 接口**（可接外部硬件）。
- 摄像头 100W 像素 RGB：支持 AI 跟随、自动驾驶、480P 图传、人脸识别、手势识别、人体姿态识别、交通标志识别。
- **TOF 传感器**：高精度测距，最远 **150cm**，典型视角 25°，支持多物体测距（本教案 `read_distance_data(1)` 读到的就是它，单位厘米）。
- 自研圆形电机：最大 360 RPM，最大扭矩 2kgf·cm。
- 自研圆形舵机：最大 60 RPM，最大扭矩 13kgf·cm。
- 锂电池 2600mAh，支持 12V 输出，满放满充约 2.5 小时。

## 官方 APP（UGOT APP）能做什么
手机/邮箱登录、3D 搭建指引（拼搭更省时）、FPV 驾驶、互动模式（自定义互动技能）、竞技模式（竞速/夺旗）、程序广场（下载程序一键玩）。

## 使用禁忌（安全，务必提醒孩子）
- 涉水路面：所有形态都不建议/不可。
- 雪地：多数形态不建议；四足狗/蜘蛛等不可。
- 湿泥土地面：部分不建议，部分不可。
- 冰面：多数不建议，部分不可。
- 松软沙地：多数不建议，部分不可。
简单记：**干、平整、室内地面最安全；水/冰/雪/泥/沙都别去**。

## 给孩子的一句话
搞不清某个形态或传感器能干什么时，先翻"七种形态"和"核心硬件规格"这两节再定项目；拿不准就问我。

## 编程环境（UGOT Python 怎么跑起来，备查）
- Windows / macOS 上，优必选官方编程入口是 **uPython**（面向初学者的代码编程环境），网页版地址 https://py.ubtrobot.com/ 。
- 用 uPython 前**必须先装 uCode Link 桥接软件**（本机常驻服务，网页编辑器靠它连到机器人）：Windows 安装包 https://assets-new.ubtrobot.com/upgrade-pro/upgrade/2023/11/1699498073643/uCodeLink-win-hasSign-v4.4.1-1-g0da54e64-release.exe ；macOS 为对应的 .dmg。
- uPython 里的 Python SDK 就是 `ugot` 库（已随 uCode Link 自带）；如需升级可在 uPython 里开 ipynb 执行 `pip install --upgrade ugot`，国内网络不通可加清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple`。
- 标准连机器流程：装好并运行 uCode Link → 机器人连 WiFi 并在「设置→系统信息」查 IP → 浏览器开 py.ubtrobot.com → 点 UGOT 选"连接到 UGOT"填 IP → 写代码 → 点"烧录并运行"。
