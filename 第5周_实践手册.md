# 第5周 · 实践手册（动手课）🔧
### 给七年级小朋友的「灯光与声音」第五课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：四足狗（新拼一台）
> 这周换 **四足狗**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 四足狗结构件包；②固定主控到机身平板、装四条腿的舵机/电机、接好线；③盯三点（螺丝拧紧 / 腿对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **四足狗拼法要点**：四条腿各接一个舵机/电机，身体是一块平板 + 主控；腿对称装，装完四条腿自然下垂、互不干扰。这只有腿的狗最萌，拼好先让它站一站。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本次让机器人**正面对着一面墙**（离个 1~2 米），这样它才有"障碍"可报警。场地要够长，别让它一启动就顶到墙。

## 步骤 ① 先让灯亮起来：认识灯带 + 亮绿灯（本周第一个小胜利！）✅
先不急着做车，我们让四盏灯一起亮绿灯，确认"灯带"通道是好的：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 四盏灯一起亮绿灯
ug.stop_chassis()
```
**应当看到**：机器人身上四盏灯一起变成绿色。说明"列表 + 灯带"通道都通了 ✅
> 💡 卡住先想：`[0,1,2,3]` 是哪四盏灯？—— 顶/左/右/底。`(0,255,0)` 三个数又分别是啥？—— R/G/B，绿是 G=255。

## 步骤 ② 玩一玩 RGB：换颜色、只亮一盏（调试思维练习）✅
改一改，看看灯会怎么变（改完点 Run）：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")

ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
ug.turn_off_lights()                       # 灭一下
ug.show_light_rgb([0,1,2,3], 0, 0, 255)   # 蓝灯
ug.turn_off_lights()
ug.show_light_rgb([0,1,2,3], 255, 255, 0) # 黄灯
ug.turn_off_lights()
ug.show_light_rgb([0], 255, 0, 0)         # 只亮顶灯（试试 [1,2] 只亮左右）
ug.stop_chassis()
```
**应当看到**：灯依次变红→灭→蓝→灭→黄→灭→最后只顶灯红。✅
> 💡 卡住先想：把 `(255,0,0)` 改成 `(0,0,255)` 灯为什么变蓝？—— 因为三个数是 R/G/B，蓝是 B=255。把 `[0,1,2,3]` 改成 `[0]` 为什么只亮顶灯？—— 列表里只列了编号 0。

## 步骤 ③ 组合出"报警巡逻车"（本周核心！）✅
现在把第4周的"看路"和第5周的"灯+声"拼起来：造一个 `报警()` 按钮（亮红灯+喊话），主循环里平时绿灯前进，遇障碍就调用它。完整可跑版：
```python
from ugot import ugot
import time                  # 让小车"歇 0.1 秒"要用到
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)               # 音量 1~5，先调好

def 报警():                              # 造一个"报警按钮"
    for _ in range(3):                   # 闪 3 次
        ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
        ug.play_audio_tts("前方有障碍，请绕行", 0)  # 女声喊话
        ug.turn_off_lights()             # 灭一下，才有"闪"的感觉
        ug.show_light_rgb([0,1,2,3], 255, 0, 0)

安全距离 = 30   # 厘米，前方小于这个就报警

while True:
    距离 = ug.read_distance_data(1)
    if 距离 == -1:
        continue                        # 读不到距离，先跳过这次
    if 距离 < 安全距离:
        ug.stop_chassis()                          # 先急停！安全第1
        ug.screen_print_text("警报!", "FFFF0000")  # 屏幕红字"警报!"
        报警()                                     # 按一下报警按钮
        break                                      # 本次演示结束
    else:
        ug.dog_move_speed(0, 40)               # 平时前进
        ug.show_light_rgb([0,1,2,3], 0, 255, 0)    # 平时绿灯
    time.sleep(0.1)                                # 歇 0.1 秒
```
**应当看到/听到**：机器人平时绿灯前进巡逻；靠近墙到约 30cm 时停下、屏幕蹦出红字"警报!"、四盏红灯闪 3 下、同时女声喊"前方有障碍，请绕行"。🎉
> 💡 重点：`报警()` 把"亮灯+喊话"打包成一个按钮，主循环里只写 `报警()` 五个字就全做了——这就是"函数组合"的省事。想让红灯"闪"起来，关键就是亮→灭→再亮。

## 步骤 ④ 加固：处理"-1" + 急停安全网（稳健版）✅
前面有个小漏洞：如果传感器**读不到**（返回 `-1`），`距离 < 30` 是 `False`，机器人会以为"安全"继续往前冲——这不行！所以我们要**特别照顾 -1：读不到就保守停下**，再套一层 `try/except` 急停：
```python
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

def 报警():
    for _ in range(3):
        ug.show_light_rgb([0,1,2,3], 255, 0, 0)
        ug.play_audio_tts("前方有障碍，请绕行", 0)
        ug.turn_off_lights()
        ug.show_light_rgb([0,1,2,3], 255, 0, 0)

安全距离 = 30

try:
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1:
            ug.stop_chassis()              # 读不到距离，"瞎了"，最安全是停
            time.sleep(0.1)
            continue
        if 距离 < 安全距离:
            ug.stop_chassis()
            ug.screen_print_text("警报!", "FFFF0000")
            报警()
            break
        else:
            ug.dog_move_speed(0, 40)
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)
        time.sleep(0.1)
except:
    ug.stop_chassis()       # 不管发生什么，先停车最安全
    print("急停！")
```
**应当看到**：和步骤③一样会报警；但即使传感器短暂失灵（显示 -1），它也会停下而不是乱冲。这就是"稳"的巡逻车！✅
> 💡 卡住先想：`if 距离 == -1:` 为什么用 `if` 而不是放在最后？—— 因为"-1"最危险，要**第一个**判断、优先处理（和第4周一个道理）。

## 步骤 ⑤ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `for _ in range(3):` 的 `3` 改成 `5`，红灯会闪几次？
- 把 `报警()` 里的 `(255,0,0)`（红）全改成 `(0,0,255)`（蓝），报警灯变什么色？
- 把 `ug.play_audio_tts("前方有障碍，请绕行", 0)` 的 `0`（女声）改成 `1`（男声），声音变了吗？
- 把喊话内容改成 `"我是小巡逻员"` ，机器人会喊啥？
- 把 `安全距离 = 30` 改成 `50`，小车是不是更早报警停下？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 机器人平时绿灯前进巡逻
- [ ] 遇障碍（< 30cm）自动停下、屏幕"警报!"、红灯闪 3 下 + 女声"前方有障碍，请绕行"
- [ ] 你能自己改 **闪烁次数**（`range(3)`）、**换灯色**（红→蓝）、**换喊话内容**
- [ ] 你能给家人演示："看，它会闪灯、还会喊话报警！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 亮灯带(红/绿/蓝) | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` / `(0,0,255)` |
| 灭灯 | `ug.turn_off_lights()` |
| 呼吸灯效果 | `ug.show_light_rgb_effect(0,255,0,2)` |
| 女声喊话 | `ug.play_audio_tts("文字", 0)` |
| 男声喊话 | `ug.play_audio_tts("文字", 1)` |
| 调音量(1~5) | `ug.set_volume(3)` |
| 屏幕红字 | `ug.screen_print_text("警报!", "FFFF0000")` |
| 读前方距离(厘米) | `ug.read_distance_data(1)` |
| 读不到距离时的值 | 返回 `-1` |
| 前进 | `ug.dog_move_speed(0, 40)` |
| 造"报警"按钮 | `def 报警():` |
| 急停 | `ug.stop_chassis()` |

> 💡 灯带编号：`0`顶 / `1`左 / `2`右 / `3`底；想亮哪盏就写进列表 `[0,1,2,3]`。
> 💡 RGB 三个数范围是 **0~255**（不是 0~100）：红 `(255,0,0)`、绿 `(0,255,0)`、蓝 `(0,0,255)`、黄 `(255,255,0)`、白 `(255,255,255)`。
> 💡 `dog_move_speed(方向, 速度)`：方向 0前/1后/2左转/3右转，速度 5~80。其它形态把 `dog_` 换成 `balance_`(平衡车) / `wheelleg_`(轮足) / `spider_`(蜘蛛) / `transform_`(变形车) / `mechanical_`(工程车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **平时用"呼吸绿灯"**：把 `ug.show_light_rgb([0,1,2,3], 0, 255, 0)` 换成 `ug.show_light_rgb_effect(0, 255, 0, 2)`（effect：0常亮 / 1关闭 / 2呼吸 / 3闪烁），让巡逻灯一呼一吸更带感。
- **报警后绕开障碍**：把"停下+报警"换成右转一下再继续巡逻（接第4周 进阶：`ug.dog_turn_speed(3, 120)` 转完记得 `ug.stop_chassis()` 和 `time.sleep(0.5)`）。
- **记录报警次数**：加一个变量 `次数 = 0`，每次报警 `次数 = 次数 + 1`，用 `ug.screen_print_text("报警"+str(次数), "FFFF0000")` 显示（接第2周 变量与 `str()`）。
- **换形态玩**：把 `dog_` 换成其它形态的前缀，换种机器人来巡逻报警。

---

## 🔮 接第6周预告
下一周我们玩**真 AI 手势遥控**：比个"手掌""点赞"手势，机器人就动起来！会学怎么"加载 AI 模型"和"读手势结果"。这一周的灯带和喊话，正好可以拿来给手势遥控做"反馈灯/语音"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
