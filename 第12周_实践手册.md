# 第12周 · 实践手册（动手课）🔧
### 给七年级小朋友的「四足狗仿生步态 + 语音播报」第十二课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。四足狗靠四条腿走，**别在太滑、太软**的地方，腿容易打滑摔。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：四足狗（重新拼一台）
> 综合阶段第四周，我们回到 **四足狗** 当演员。一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 四足狗结构件包；②固定主控到机身平板、装四条腿的舵机/电机、接好线；③盯三点（螺丝拧紧 / 腿对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **四足狗拼法要点**：四条腿各接一个舵机/电机，身体是一块平板 + 主控；腿对称装，装完四条腿自然下垂、互不干扰。这只有腿的狗最萌，拼好先让它在原地站一站，确认四脚站稳再动。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本周要让狗"歇一会儿再停"，得在开头加一行 `import time`（Python 自带的计时工具），待会儿代码里会看到。

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：四足狗 仿生步态 + 语音播报（dog_）
```
> 第 12 周就做这一个场景（课程安排里写好的）。它把第3周 函数封装 + 第5周 语音播报，拼到四足狗上。做完它，下周换蜘蛛还能接着玩。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第七节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🐕 运动 | `ug.dog_move_speed(0, 速度)` 让狗迈步前进 |
| ③ | 🔧 函数 | `def walk(秒数):` / `def run(秒数):` / `def turn(秒数):` 三个按钮 |
| ④ | 🔊 声音 | 每个按钮里加 `ug.play_audio_tts("我在走", 0)` 播报 |
| ⑤ | 🎬 表演 | 连起来：`walk(2); run(2); turn(1)` 一段小节目 |
| ⑥ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 四足狗动起来（确认狗腿听指挥）✅
先让四足狗**迈一步前进**，确认 `dog_` 接口通、四条腿会动：
```python
# 关1：四足狗动起来（dog_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.set_volume(3)               # 音量 1~5，先调好
ug.dog_move_speed(0, 35)       # 0=前进，速度 5~80
time.sleep(2)                  # 走 2 秒
ug.stop_chassis()              # 停！不然会一直走
ug.screen_print_text("站稳", "FF00FF00")
```
**应当看到**：四足狗迈着狗步前进 2 秒，然后停下站稳。🎉
> 💡 卡住先想：`dog_move_speed(0, 35)` 是"一直走"的命令，所以后面一定要 `stop_chassis()` 让它停。忘了停，它会走到撞墙——这就是急停按钮的用处。

### 关 2 · 把"走/跑/转身"封装成三个按钮（叠第3周 函数）✅
在关 1 基础上，把三种步态做成三个函数 `walk` / `run` / `turn`，再连起来表演。先**不加语音**，确认按钮好用：
```python
# 关2：四足狗 三种步态按钮（dog_，先不喊话）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

def walk(秒数, 速度=35):        # "走"按钮：中等速度前进
    ug.dog_move_speed(0, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

def run(秒数, 速度=70):         # "跑"按钮：速度更快
    ug.dog_move_speed(0, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

def turn(秒数, 速度=120):       # "转身"按钮：左转（2=左转）
    ug.dog_turn_speed(2, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

# ===== 表演：走 → 跑 → 转身 =====
walk(2)        # 慢慢走 2 秒
run(2)         # 飞快跑 2 秒
turn(1)        # 原地左转 1 秒
ug.screen_print_text("表演完毕", "FF00FF00")
```
**应当看到**：四足狗先慢慢走 2 秒 → 再飞快跑 2 秒 → 原地左转 1 秒，全程不卡壳。🎉
> 💡 卡住先想：`walk` 和 `run` 都是前进，区别只在**速度**（35 vs 70）——这就是两种"步态"。`turn` 用 `dog_turn_speed`，最后 `stop_chassis()` 不能省，否则它会一直转圈。想右转？把 `2` 改成 `3`。

### 关 3 · 每个按钮加语音播报：切换就喊话（叠第5周 声音）→ 完成大作品 ✅
最后在每个函数里加一行 `play_audio_tts`，让狗一按按钮就开口报幕。这就是课程安排里的"验收流程"——**每切换一个动作都有对应语音**：
```python
# 关3（终极版）：四足狗 步态按钮 + 语音播报（dog_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)               # 音量 1~5

def walk(秒数, 速度=35):
    ug.dog_move_speed(0, 速度)
    ug.play_audio_tts("我在走", 0)    # 👈 一迈步就播报
    time.sleep(秒数)
    ug.stop_chassis()

def run(秒数, 速度=70):
    ug.dog_move_speed(0, 速度)
    ug.play_audio_tts("我在跑", 0)    # 👈 一撒欢就播报
    time.sleep(秒数)
    ug.stop_chassis()

def turn(秒数, 速度=120):
    ug.dog_turn_speed(2, 速度)
    ug.play_audio_tts("我在转身", 0)  # 👈 一转身就播报
    time.sleep(秒数)
    ug.stop_chassis()

try:
    walk(2)        # 播报"我在走" + 走 2 秒
    run(2)         # 播报"我在跑" + 跑 2 秒
    turn(1)        # 播报"我在转身" + 转 1 秒
    ug.screen_print_text("表演完毕", "FF00FF00")
except:
    ug.stop_chassis()       # 不管发生什么，先停车最安全
```
**应当看到（验收两步）**：四足狗依次"走→跑→转身"，并且**每切换一个动作，女声就播报对应的一句话**（我在走 / 我在跑 / 我在转身）。🎉🎉🎉
> 💡 卡住先想：语音写在哪个位置，就会在哪个动作发生时喊？—— 写在 `dog_move_speed` 后面，所以"狗一开始动，喇叭就喊"。想让播报内容变？改 `play_audio_tts("想说的话", 0)` 里的字就行；想变男声把 `0` 改成 `1`。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关2/关3：把 `run` 的速度 `70` 改成 `40`，"跑"是不是变慢、和"走"差不多了？
- 关3：把 `walk` 的播报 `"我在走"` 改成 `"狗狗散步中"`，听它会喊啥？
- 关3：把 `turn` 里的 `dog_turn_speed(2, 120)` 改成 `dog_turn_speed(3, 120)`，转身方向变了吗？
- 关3：在最后加一行 `walk(1, 90)`，让节目变成"走→跑→转身→快速走"。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把四足狗拼好（四腿对称）、连上电脑
- [ ] 关1 跑通：四足狗能迈步前进、会停
- [ ] 关2 跑通：walk / run / turn 三个按钮都能切换不同步态
- [ ] 关3 跑通：每个动作切换时都有对应语音播报（我在走/我在跑/我在转身）
- [ ] 我能给家人演示验收：**多步态切换 + 每次切换都语音播报**

---

## 🃏 随身速查卡（四足狗步态 + 语音播报常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 四足狗前进 / 后退 | `ug.dog_move_speed(0, 40)` / `(1, 40)` |
| 四足狗左转 / 右转 | `ug.dog_turn_speed(2, 120)` / `(3, 120)` |
| 调音量(1~5) | `ug.set_volume(3)` |
| 女声 / 男声播报 | `ug.play_audio_tts("我在走", 0)` / `(..., 1)` |
| 造"走"按钮 | `def walk(秒数, 速度=35):` |
| 前进 N 秒 + 停 | `ug.dog_move_speed(0, 速度); time.sleep(秒数); ug.stop_chassis()` |
| 歇 N 秒 | `time.sleep(2)` |
| 急停 | `ug.stop_chassis()` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 方向数字：前进=0，后退=1，左转=2，右转=3；`dog_move_speed(方向, 速度)` 速度 5~80，`dog_turn_speed(方向, 速度)` 速度 5~280。
> 💡 其它形态把 `dog_` 换成 `wheelleg_`(轮足) / `balance_`(平衡车) / `spider_`(蜘蛛) / `transform_`(变形车) / `mecanum_`(麦轮车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **一直表演不歇**：用 `while True:` 把 `walk(2); run(2); turn(1)` 包起来，让它循环演节目（加 `try/except` 急停，想停就按急停）。
- **加"后退"按钮**：照 `walk` 把 `0`(前) 改成 `1`(后)，造一个 `back(秒数)` 按钮，播报"我在后退"。
- **播报 + 灯一起**：每个步态除了喊话，再配一种灯色（走=绿、跑=黄、转身=蓝），用 `ug.show_light_rgb`（灯函数见第5周速查卡）。
- **换形态**：把 `dog_` 换成 `spider_`(蜘蛛) 做"全向移动 + 传感器判断"（第13周预告）。

---

## 🔮 接第13周预告
下一周我们换 **蜘蛛机器人（spider_）**：四足全向移动，绕场地巡逻；用 TOF 测距判断前方有没有"墙/障碍"，遇障就转向，不撞。这周的"函数封装步态 + 语音播报"正好能拿去给蜘蛛做"不同巡逻动作配不同播报"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把走/跑/转身封装成函数按钮，并让每个按钮切换时开口播报"，一层层来最稳！
