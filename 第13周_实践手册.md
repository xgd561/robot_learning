# 第13周 · 实践手册（动手课）🔧
### 给七年级小朋友的「蜘蛛全向移动 + 传感器判断」第十三课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。蜘蛛腿向四周伸，**别在太挤、有易碎品**的地方玩，腿容易扫到东西。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：蜘蛛（重新拼一台）
> 综合阶段第五周，我们换 **蜘蛛** 当巡逻兵。一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 蜘蛛结构件包；②固定主控到机身、装四条仿生腿的舵机、接好线；③盯三点（螺丝拧紧 / 腿对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **蜘蛛拼法要点**：四条仿生腿向**四周伸开**，每条腿一个舵机；机身低、重心低。腿要**对称**（左前/右前/左后/右后别装错），装完四条腿自然张开、不打架。拼好连上电脑，本周就让它在场地里绕圈巡逻、见墙绕开。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本次把机器人**正面对着一面墙（或纸箱）**，离个 1~2 米，这样它才有"墙"可看、可绕。场地要够大，别让它一启动就顶到墙。

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：蜘蛛 全向巡逻 + 遇障转向避开（spider_）
```
> 第 13 周就做这一个场景（课程安排里写好的）。它把第3周 函数封装 + 第4周 TOF 判断，拼到蜘蛛上。做完它，下周换变形车还能接着玩。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第七节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🕷️ 运动 | `ug.spider_move_speed(0, 速度)` 让蜘蛛前进 |
| ③ | 🔧 函数 | `def 走(秒数):` / `def 转(方向,秒数):` 封装移动按钮 |
| ④ | 👀 传感器 | `ug.read_distance_data(1)` 读前方距离 |
| ⑤ | 🚦 判断 | `if 距离 < 30:` 近了就反应 |
| ⑥ | 🔄 避开 | `def 避开():` 停+转向绕开，代替"只停" |
| ⑦ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 蜘蛛动起来 + 封装"走/退/转"按钮（叠第3周 函数）✅
先让蜘蛛**动起来**，并把"走/退/转"做成按钮，确认四条腿听指挥：
```python
# 关1：蜘蛛 全向移动 + 方向按钮（spider_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

前进 = 0
后退 = 1
左转 = 2
右转 = 3

def 走(秒数, 速度=40):
    ug.spider_move_speed(前进, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

def 倒(秒数, 速度=40):
    ug.spider_move_speed(后退, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

def 转(方向, 秒数, 速度=120):
    ug.spider_turn_speed(方向, 速度)
    time.sleep(秒数)
    ug.stop_chassis()          # 转弯必须停，否则转圈圈

走(1); 倒(1); 转(左转, 1); 转(右转, 1)   # 四个按钮各按一下试试
ug.stop_chassis()
```
**应当看到**：蜘蛛前进 1 秒 → 后退 1 秒 → 左转 1 秒 → 右转 1 秒，四条腿灵活不卡壳。🎉
> 💡 卡住先想：`转` 这个函数里最后有 `ug.stop_chassis()`，因为 `spider_turn_speed` 是"一直转"，不喊停它就转圈圈停不下来——这其实也是咱们的急停按钮。

### 关 2 · 加测距，遇障就停（叠第4周 传感器，先不绕）✅
在关 1 基础上，加 TOF 测距：前方 < 30cm（或读不到 -1）就**停下 + 红灯**，先确认"看路"本事。注意**先判最危险的"-1"**：
```python
# 关2：蜘蛛 巡逻 + 遇障停（spider_，先不转弯）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

安全距离 = 30
try:
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1 or 距离 < 安全距离:        # 最危险：太近或读不到 → 停
            ug.stop_chassis()
            ug.screen_print_text("停!", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
        else:                                      # 安全 → 前进巡逻
            ug.spider_move_speed(0, 40)
            ug.screen_print_text("巡逻", "FF00FF00")
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：蜘蛛平时绿灯巡逻前进；你推墙/纸箱到 30cm 内，它红灯停下不撞。🎉
> 💡 卡住先想：`距离 == -1` 放最前面，是因为"读不到=最危险"，要第一个判断（忘了看《第4周_实践手册》）。这关它只会"停"，下一步教它"绕"。

### 关 3 · 升级"遇障转向避开" + 一直巡逻（叠第4周 判断 → 完成大作品）✅
最后把"停"升级成 **`避开()` 按钮**（停 + 转向绕开），遇到墙就自动拐弯、再继续巡逻。这就是课程安排里的"验收流程"：
```python
# 关3（终极版）：蜘蛛 全向巡逻 + 遇障自动转向避开（spider_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

安全距离 = 30

def 避开():                              # 把"停+转向绕开"打包成一个按钮
    ug.stop_chassis()
    ug.screen_print_text("避障", "FFFF0000")
    ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯提示
    ug.spider_turn_speed(3, 120)       # 右转绕开（3=右转，速度 5~280）
    time.sleep(0.8)                    # 转一会儿
    ug.stop_chassis()                  # 转完停，准备继续走
    ug.turn_off_lights()

try:
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1 or 距离 < 安全距离:
            避开()                              # 遇障 → 转向绕开（不再傻站）
        else:
            ug.spider_move_speed(0, 40)        # 平时前进巡逻
            ug.screen_print_text("巡逻中", "FF00FF00")
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
        time.sleep(0.1)
except:
    ug.stop_chassis()           # 不管发生什么，先停车最安全
    ug.turn_off_lights()
```
**应当看到（验收两步）**：蜘蛛绿灯绕场巡逻；你推墙/纸箱到 30cm 内，它红灯一闪、**自己右转绕开**，然后变回绿灯继续巡逻，全程不撞墙。🎉🎉🎉
> 💡 卡住先想：第4周是 `if 距离<30: ug.stop_chassis()`（停）；本周把那行换成 `避开()`（停+拐弯）——只换一个按钮，行为就从"站住"升级成"绕路"。想让它往左绕？把 `避开()` 里的 `3`(右转) 改成 `2`(左转)。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关1：把 `走(1)` 的 `速度=40` 改成 `走(1, 70)`，蜘蛛冲得更快了吗？（场地要够大！）
- 关2/关3：把 `安全距离 = 30` 改成 `15` 或 `50`，它离墙更近还是更早反应？
- 关3：把 `避开()` 里的 `spider_turn_speed(3, 120)` 改成 `spider_turn_speed(2, 120)`，绕开方向变了吗？
- 关3：把 `避开()` 里 `time.sleep(0.8)` 的 `0.8` 改成 `1.5`，它转得更多还是更少？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把蜘蛛拼好（四腿对称、向四周伸开）、连上电脑
- [ ] 关1 跑通：蜘蛛 走/退/转 按钮都能动
- [ ] 关2 跑通：蜘蛛巡逻，前方 < 30cm 就停 + 红灯（确认看路本事）
- [ ] 关3 跑通：蜘蛛遇障**自动转向绕开**、继续巡逻、不撞墙
- [ ] 我能给家人演示验收：**全向巡逻 + 接近障碍自动转向避开**

---

## 🃏 随身速查卡（蜘蛛全向巡逻 + 避障常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 蜘蛛前进 / 后退 | `ug.spider_move_speed(0, 40)` / `(1, 40)` |
| 蜘蛛左转 / 右转 | `ug.spider_turn_speed(2, 120)` / `(3, 120)` |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 方向常量 | `前进=0 后退=1 左转=2 右转=3` |
| 造"走"按钮 | `def 走(秒数, 速度=40):` |
| 造"避开"按钮 | `def 避开():` （停+转向+停） |
| 急停 | `ug.stop_chassis()` |
| 红灯 / 绿灯 | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` |
| 一直循环巡逻 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `spider_move_speed(方向, 速度)`：方向 0前/1后/2左转/3右转，速度 5~80；`spider_turn_speed(方向, 速度)`：2左转/3右转，速度 5~280。
> 💡 其它形态把 `spider_` 换成 `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车) / `transform_`(变形车) / `mecanum_`(麦轮车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **绕开后再走远点**：在 `避开()` 里转向后，加一句 `ug.spider_move_speed(0, 40); time.sleep(0.5); ug.stop_chassis()`，让它转完倒退/前进一小段再继续巡逻（更"聪明"地脱困）。
- **换边绕**：把 `避开()` 做成能选方向——认出墙在左就右转、墙在右就左转（先用固定右转跑通，再挑战）。
- **加语音播报**：遇障时 `ug.play_audio_tts("前方有障碍，转向", 0)` 喊一声（声音函数见第5周速查卡）。
- **换形态**：把 `spider_` 换成 `transform_`(变形车) 做"越障 + 巡线"（第14周预告）。

---

## 🔮 接第14周预告
下一周我们换 **变形车（transform_）**：它能调高机身、翻台阶、越野。小场景是"先沿黑线巡线，遇到台阶/障碍就调高机身翻越，过去后恢复低姿态继续巡线"——这周的"全向移动 + 传感器判断"正好能拿去给变形车做"遇障翻越"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把全向移动封装成函数、并用 TOF 把'遇障停'升级成'遇障绕开'"，一层层来最稳！
