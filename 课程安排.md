# 课程安排（UGOT Python 小导师 · 七年级零基础）

> **两阶段结构一句话说明**：第 1~8 周按 Python 知识点一条线讲透（知识点主线），第 9 周起换思路——前面学的全当"工具箱"，按当周拼出的机器人形态做综合项目（7 形态综合运用），不再新讲语法，只把旧本领串起来用。

- **学生**：13 岁，七年级，编程零基础
- **设备**：ThinkPad X240 + Windows 7；uCode Link v4.4.1 + 在线编辑器 py.ubtrobot.com（方式 A，全程优先）
- **上课时间**：每周六 19:00–21:00（第一节 19:00–19:50 理论；休息 19:50–20:00；第二节 20:00–20:50 动手）
- **每节课节奏**：开场先问"上周项目还记得吗 / 想挑战什么" → 本周理论/脚手架 → 第二节孩子动手跑通 → 结束让孩子演示并预告下周

---

## 第一阶段：知识点主线（第 1~8 周）

> 这一阶段每节只讲 1~2 个 Python 知识点（屏幕/循环→函数→传感器判断→灯光声音→手势 AI→颜色人脸→巡线），配合最小可跑演示，不要求孩子当场写整段。**关键原则：哪怕在学知识点，每周也给孩子一个明确的"要拼的机器人形态"，让他有东西可拆可装、保持组装的新鲜感和期待感。** 7 种形态会在前 8 周全部露脸一遍（前两周用麦轮车打基础、熟悉 API，之后每周换一种），第 9 周起再每个形态深挖一周做综合项目。

### 第一阶段形态一览（每周拼什么）
| 周次 | 形态 | 运动前缀 | 当周顺带玩的点（吸引组装） |
|---|---|---|---|
| 第 1 周 | 麦轮车 | `mecanum_` | 第一次拼装 + 连上就动，最有成就感 |
| 第 2 周 | 麦轮车 | `mecanum_` | 屏幕倒计时，连续两周好上手 |
| 第 3 周 | 平衡车 | `balance_` | 用函数拼路线，看它直立稳稳走 |
| 第 4 周 | 蜘蛛 | `spider_` | 四足全向避障巡逻，样子很酷 |
| 第 5 周 | 四足狗 | `dog_` | 灯光表情 + 语音报警，最萌最吸睛 |
| 第 6 周 | 轮足 | `wheelleg_` | 手势遥控，高级感拉满 |
| 第 7 周 | 工程车 | `mechanical_` | 认颜色 + 机械臂指/抓，最有玩头 |
| 第 8 周 | 变形车 | `transform_` | 巡线 + 越障，收尾带出"变形"概念 |

> 前缀切换提醒：换形态时只要把运动方法里的 `mecanum_` 换成对应前缀（平衡车 `balance_`、蜘蛛 `spider_`、四足狗 `dog_`、轮足 `wheelleg_`、工程车 `mechanical_`、变形车 `transform_`），Python 写法本身不变。屏幕/灯光/声音/传感器等接口是通用的，不随形态变。

### 第 1 周 · 环境搭建与连接（地基，最重要）
- **🤖 本周形态：麦轮车**（`mecanum_`）— 第一次拼，最容易上手，文档示例也以它为基准。
- **第一节（理论）**：讲清"让电脑和机器人说上话、再发命令让它动"这件事；为什么用在线编辑器（Win7 零安装）；WiFi 同网是关键；浏览器要用 Chrome/Firefox（绝不用 IE/旧 Edge）。
- **第二节（动手）**：① 机器人开机连 WiFi（设置→网络）；② X240 用 Chrome/Firefox 打开 py.ubtrobot.com；③ 确认 ugot 库已装（Libraries 里看）；④ 点黄色 UGOT → Connect to UGOT → 填机器人 IP（设置→关于）→ 连上；⑤ 写第一段代码（连接 + 前进 2 秒 + 屏幕显示"我连上啦"）→ Run。
- **本周目标**：机器人连上并动一下，拿到"我能控制它"的成就感。这一步不通，后面都没法开始。
- **万能开头（每段代码前 3 行）**：
  ```python
  from ugot import ugot
  ug = ugot.UGOT()
  ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP，电脑在同一 WiFi
  ```

### 第 2 周 · 屏幕与循环
- **🤖 本周形态：麦轮车**（`mecanum_`）— 连续第二周，孩子刚上手别急着换，先玩熟再开新形态。
- **知识点**：print、变量、for 循环。
- **场景项目**：机器人屏幕倒计时 + 语音报数。
- **演示**：用 `for` 从 3 数到 1，`ug.screen_print_text(...)` 显示数字，`ug.play_audio_tts(...)` 报数。

### 第 3 周 · 函数与运动
- **🤖 本周形态：平衡车**（`balance_`）— 换第一次新形态！用函数拼路线，看它直立稳稳走，孩子会很有新鲜感。
- **知识点**：def 函数、参数。
- **场景项目**：用函数拼出"前进→左转→前进"路线。
- **演示**：`ug.balance_move_speed(0,40)` 前进、`ug.balance_turn_speed(2,120)` 左转，封装成 `go_forward()` / `turn_left()` 函数（前缀换成 `balance_` 即可，Python 写法不变）。

### 第 4 周 · 传感器 + 判断
- **🤖 本周形态：蜘蛛**（`spider_`）— 四足全向移动，样子很酷；用来做避障巡逻最合适。
- **知识点**：if/else、while、布尔。
- **场景项目**：避障巡逻（TOF 测距 < 30cm 就停）。
- **演示**：`d = ug.read_distance_data(1)`，`if d < 30:` 停下，否则 `ug.spider_move_speed(0,40)` 前进（运动前缀换成 `spider_`；传感器/判断代码通用不变）。

### 第 5 周 · 灯光与声音
- **🤖 本周形态：四足狗**（`dog_`）— 灯光表情 + 语音报警，最萌最吸睛，孩子拼它动力最足。
- **知识点**：列表、RGB、函数组合。
- **场景项目**：会报警的巡逻狗（有障碍闪红灯 + 语音提示）。
- **演示**：`ug.show_light_rgb([0,1,2,3],255,0,0)` 闪红，`ug.turn_off_lights()`，`ug.play_audio_tts("前面有障碍",0)`（灯光/声音接口通用，不随形态变；运动时用 `ug.dog_move_speed(...)`）。

### 第 6 周 · 真 AI 手势
- **🤖 本周形态：轮足**（`wheelleg_`）— 手势遥控，高级感拉满；看轮足跟着手势走，孩子会很有成就感。
- **知识点**：加载模型、读 JSON、字符串比较。
- **场景项目**：手势遥控（比手势让轮足动）。
- **演示**：`ug.load_models(['gesture'])`，`raw = ug.VISION.gesture_inference()`，`json.loads(raw)` 取 `gesture` 字段（stone/palm/ok/good/scissor）判断动作，再调 `ug.wheelleg_move_speed(...)` / `ug.wheelleg_turn_speed(...)` 让机器人动（视觉/手势代码通用，仅运动前缀换成 `wheelleg_`）。

### 第 7 周 · 颜色 / 人脸识别
- **🤖 本周形态：工程车**（`mechanical_`）— 认出颜色后让机械臂指过去/夹起来，最有玩头，孩子最期待。
- **知识点**：视觉模型组合、条件分支。
- **场景项目**：认出红/绿球分类（或录入家人脸喊名字），并用机械臂指向/夹取对应目标。
- **演示**：`ug.load_models(['color_recognition'])` + `ug.get_color_total_info()`；或 `face_recognition` + `ug.get_face_recognition_total_info()`；识别到后用机械臂 `ug.mechanical_*` 方法指向/夹取（视觉代码通用，仅机械臂部分用 `mechanical_` 前缀）。

### 第 8 周 · 巡线
- **🤖 本周形态：变形车**（`transform_`）— 巡线 + 越障，收尾带出"变形"概念，正好和第 9 周起的形态深挖接上。
- **知识点**：摄像头识别黑线，跟随行驶（引入 PID 调速概念，简化讲）。
- **场景项目**：摄像头识别黑线，跟随行驶（遇小障碍可抬升车身越过）。
- **演示**：读画面/颜色，按偏移量调左右轮速度（用 `ug.transform_move_speed(...)` / `ug.transform_turn_speed(...)` 配合，简化版用 if 分段调速；前缀换成 `transform_`，Python 写法不变）。

---

## 第二阶段：7 形态综合运用（第 9 周起）

> **怎么上这一阶段的课**：不再新讲 Python 语法。每节开头和孩子确认"本周拼的是哪种形态"，把对应运动前缀一换，就让他用前 8 周的工具箱把项目跑通。形态运动前缀切换规则（结合 Full Kit 补强 issue）：麦轮 `mecanum_`、平衡车 `balance_`、轮足 `wheelleg_`、四足狗 `dog_`、蜘蛛 `spider_`、变形车 `transform_`；工程车机械臂 `mechanical_*`。

### 周次映射总表（验收用）

| 周次 | 形态 | 运动前缀 | 综合小项目 | 融合的前序知识点（≥2） |
|---|---|---|---|---|
| 第 9 周 | 麦轮车 | `mecanum_` | 巡线 + 避障 + 手势遥控整合 | 第4周 传感器判断 + 第6周 手势 AI + 第8周 巡线 |
| 第 10 周 | 平衡车 | `balance_` | 直立过障 + 灯光报警 | 第4周 传感器判断 + 第5周 灯光声音 |
| 第 11 周 | 轮足 | `wheelleg_` | 高低地形适应 + AI 跟随 | 第4周 传感器判断 + 第6/7周 AI 视觉 |
| 第 12 周 | 四足狗 | `dog_` | 仿生步态 + 语音播报 | 第3周 函数运动 + 第5周 声音 |
| 第 13 周 | 蜘蛛 | `spider_` | 全向移动 + 传感器判断 | 第3周 函数运动 + 第4周 传感器判断 |
| 第 14 周 | 变形车 | `transform_` | 越障/翻台阶 + 巡线 | 第8周 巡线 + 第4周 传感器判断 |
| 第 15 周 | 工程车 | `mechanical_` | 机械臂抓取 / 取放 | 第3周 函数运动 + 第4/7周 视觉识别 |
| 第 16 周 | 自由综合周 | （自选） | 自选形态 + 自选主题收官项目 | 自由组合前 8 周任意知识点 |

### 各形态项目详解

#### 第 9 周 · 麦轮车 `mecanum_` —— 巡线 + 避障 + 手势遥控整合
- **融合知识点**：第4周 TOF 避障（if/while/布尔）+ 第6周 手势 AI（加载模型、读 JSON）+ 第8周 巡线（摄像头跟随）。
- **小场景**：机器人沿黑线走，遇到障碍（TOF < 30cm）先停；孩子比个"ok/手掌"手势，切换成"手势遥控模式"手动指挥它走。
- **验收标准**：孩子能演示"巡线中→遇障自动停→比手势切手动→遥控通过→恢复巡线"的完整流程。
- **关键 API**：`ug.mecanum_move_speed(...)`、`ug.mecanum_turn_speed(...)`、`ug.read_distance_data(1)`、`ug.load_models(['gesture'])` + `ug.VISION.gesture_inference()`。

#### 第 10 周 · 平衡车 `balance_` —— 直立过障 + 灯光报警
- **融合知识点**：第4周 传感器判断（TOF 测距）+ 第5周 灯光与声音（闪红灯 + 语音）。
- **小场景**：平衡车自动直立前行，靠近障碍时灯带闪红 + 语音"注意障碍"并减速/停；障碍移开继续走。
- **验收标准**：孩子能演示平衡车过障全流程，并在危险距离触发红灯+语音报警。
- **关键 API**：`ug.balance_move_speed(...)` / `ug.balance_turn_speed(...)`（按平衡车接口）、`ug.read_distance_data(1)`、`ug.show_light_rgb([0,1,2,3],255,0,0)`、`ug.play_audio_tts("注意障碍",0)`、`ug.turn_off_lights()`。

#### 第 11 周 · 轮足 `wheelleg_` —— 高低地形适应 + AI 跟随
- **融合知识点**：第4周 传感器/姿态判断（读 `ug.read_gyro_data()` / `ug.get_attitude()` 感知起伏）+ 第6/7周 AI 视觉（手势或颜色/人脸跟随目标）。
- **小场景**：轮足机器人在有高低起伏的场地行走，机身靠陀螺仪保持海拔稳定；用"比手势"或"识别一个颜色块"作为跟随目标，让它追着目标走。
- **验收标准**：孩子能演示轮足过起伏地面不摔，并让机器人跟随指定目标移动。
- **关键 API**：`ug.wheelleg_move_speed(...)` / `ug.wheelleg_turn_speed(...)`、`ug.read_gyro_data()`、`ug.get_attitude()`、AI 模型 `gesture` / `color_recognition`。

#### 第 12 周 · 四足狗 `dog_` —— 仿生步态 + 语音播报
- **融合知识点**：第3周 函数与运动（把各种步态封装成函数）+ 第5周 声音（TTS 播报状态）。
- **小场景**：四足狗用仿生步态走（走/跑/转身封装成 `walk()` / `run()` / `turn()` 函数）；每切换一个动作就用语音播报"我在走 / 我在跑"。
- **验收标准**：孩子能演示四足狗多步态切换，且每次切换都有对应语音播报。
- **关键 API**：`ug.dog_move_speed(...)` / `ug.dog_turn_speed(...)`（按四足狗接口）、`ug.play_audio_tts(...,0)`、`ug.set_volume(3)`。

#### 第 13 周 · 蜘蛛 `spider_` —— 全向移动 + 传感器判断
- **融合知识点**：第3周 函数与运动（全向移动函数）+ 第4周 传感器判断（TOF 测距避障）。
- **小场景**：蜘蛛机器人四足全向移动，绕场地巡逻；用 TOF 测距判断前方是否有"墙/障碍"，遇障转向，不撞。
- **验收标准**：孩子能演示蜘蛛全向巡逻并在接近障碍时自动转向避开。
- **关键 API**：`ug.spider_move_speed(...)` / `ug.spider_turn_speed(...)`（按蜘蛛接口）、`ug.read_distance_data(1)`、`ug.stop_chassis()`。

#### 第 14 周 · 变形车 `transform_` —— 越障/翻台阶 + 巡线
- **融合知识点**：第8周 巡线（摄像头跟随）+ 第4周 传感器判断（检测台阶/障碍高度，决定抬升车身还是绕行）。
- **小场景**：变形车先沿黑线巡线，遇到台阶/障碍物时调高机身（变形能力）翻越，过去后恢复低姿态继续巡线。
- **验收标准**：孩子能演示变形车巡线中识别障碍并抬升机身越过，之后继续巡线。
- **关键 API**：`ug.transform_move_speed(...)` / `ug.transform_turn_speed(...)`（按变形车接口）、车身高度相关 `transform_*` 方法、`ug.read_distance_data(1)`。

#### 第 15 周 · 工程车 `mechanical_` —— 机械臂抓取 / 取放（其他形态没有，单独成项）
- **融合知识点**：第3周 函数与运动（把"伸出/夹取/抬起/放下"封装成函数）+ 第4/7周 视觉识别（摄像头认出要抓的目标颜色/物体）。
- **小场景**：工程车开到目标前，用机械臂 `mechanical_*` 方法对准目标 → 夹取 → 搬运 → 放到指定位置；可用颜色识别定位目标。
- **验收标准**：孩子能演示工程车用机械臂完成一次"识别→夹取→搬运→放下"的完整取放。
- **关键 API**：`ug.mechanical_move(...)` / `ug.mechanical_turn(...)`（按工程车接口）、机械臂 `ug.mechanical_*` 方法（夹爪/升降/旋转）、`ug.load_models(['color_recognition'])` + `ug.get_color_total_info()`。

#### 第 16 周 · 自由综合周（收官）
- **形态**：孩子自选当周拼装的形态。
- **主题**：孩子自选（如手势搬运 / 语音问答车 / 巡逻报警车），独立做一个能演示的项目。
- **要求**：至少融合 2 个前 8 周知识点，自己定方案、自己搭脚手架、自己跑通。
- **小导师角色**：只给线索和排查帮助，不直接给整段答案；最后让孩子完整演示一遍作为阶段收官。
- **验收标准**：孩子独立交付一个能现场演示的综合项目，并讲清楚用了哪些学过的本领。

---

## 给小导师的备注
- **第一阶段也要每周说清形态**：每节课开场就告诉孩子"本周我们拼 XX（形态名）"，让他有拆装目标和期待感；前两周用麦轮车打基础，第 3 周起每周换一种，7 种形态在前 8 周全部露脸。换形态时顺势教他"前缀一换，Python 写法不变"，这正好是第二阶段深挖前的预演。
- 第二阶段每节开场务必先和孩子确认当周形态，并**第一时间把运动前缀换对**（最容易踩的坑就是复制了麦轮代码却没改前缀）。
- 7 形态逐一排进第 9~15 周（各 1 周），第 16 周留作自由综合收官，整体约 8 周。若某形态孩子特别感兴趣，可按 Full Kit 补强 issue 的规则合并/顺延，但保持"每形态≥1 个综合项目"的底线。
- 安全底线不变：同 WiFi、空旷场地、先 `stop_chassis()`、水/冰/雪/泥/沙地面都不去。
