# 第1周 · 实践手册（动手课）🔧
### 给七年级小朋友的「让机器人听我话」第一课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 步骤 ⓪ 安装 uCode Link（最重要的一步，先做这个！）✅
> 🔑 还记得《教案》里说的"传声筒"吗？**uCode Link 就是这个传声筒**。不装它，网页编辑器就找不到机器人，后面全白搭。所以这是第一步里的一步，必须先做。

**下载（在 X240 上用浏览器点这个链接）：**
- Windows 安装包（官方 v4.4.1）：
  https://assets-new.ubtrobot.com/upgrade-pro/upgrade/2023/11/1699498073643/uCodeLink-win-hasSign-v4.4.1-1-g0da54e64-release.exe
- （如果你换 Mac 电脑，用对应的 .dmg 版；本期用 X240 的 Win 版就行）

**安装 + 确认（照着做）：**
1. 双击下载好的 `.exe` 文件，一路点"下一步"装好（Win7 上能装就装，装不上见下方"卡住了"）。
2. 装完后，看电脑**右下角托盘**（时间旁边那一小排图标），应该出现一个 uCode Link 的小图标 → 说明它在运行了。
3. ✅ 确认它一直开着：**整个上课过程中都别关它**，它就是那个常驻的"传声筒"。
4. 想知道它是不是在正常工作？打开 py.ubtrobot.com 后，如果步骤④能连上机器人，就说明它干活了。

> 💡 小提醒：uCode Link 装好后，网页编辑器里自带的 `ugot` 库也就有了（所以步骤③才能看到 `ugot`）。它是"一套带走"的。

**卡住了怎么办（Win7 老机器常见）：**
- 如果双击 `.exe` 报错、打不开 → 先别硬刚，告诉小导师/家长，我们退一步用 py.ubtrobot.com 网页编辑器再排查；
- 还是连不上 → 多半是 WiFi / 防火墙挡住了，检查电脑和机器人是不是真同一个 WiFi。
- 这一步不通，后面的课都没法开始，所以**一定先把它跑顺**。

---

## 步骤 ① 机器人开机，连 WiFi ✅
- 机器人开机 → 进「设置 → 网络」→ 连到你家的 WiFi。
- 记下它的 IP：进「设置 → 系统信息（关于）」，那里有一串像 `192.168.x.x` 的数字，**拍个照**记下来（这就是机器人在 WiFi 里的"门牌号"，为什么重要看《教案》第五节）。
- 💡 小提示：如果找不到 IP 在哪，或数字看不清，别急，喊小导师。

## 步骤 ② 电脑打开编辑器 ✅
- 先确认右下角 **uCode Link 还在运行**（步骤⓪装的那个图标亮着）。
- 在 **X240** 上用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com/ 。

## 步骤 ③ 确认 ugot 库在 ✅
- 在编辑器里找 **Libraries（库）** 列表，能看到 `ugot` 就说明 OK（uCode Link 已经自带了）。

## 步骤 ④ 连上机器人 ✅
- 点页面上黄色的 **UGOT** 按钮 → 选 **Connect to UGOT** → 填上步骤①记的机器人 IP → 连上！
- 连上了会有提示，这就代表"对话通道"打通了 ✅

## 步骤 ⑤ 跑第一段代码（见证奇迹）✅
把下面这段代码整段粘进编辑器，**先把 IP 换成你机器人真实的那个**，然后点 Run：

```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人设置→关于里的真实 IP

ug.mecanum_move_speed_times(0, 40, 2, 0)   # 前进 2 秒（速度 40）
ug.screen_print_text("我连上啦", "FFFF0000")  # 屏幕显示红字
ug.stop_chassis()   # 停！安全第一
```

**你应当看到**：机器人往前走了 2 秒 → 屏幕亮出红红的"我连上啦" → 停下。
**这一刻，你就能控制它了！** 🎉

---

## 📲 "烧录并运行"到底是什么？（点 Run 之前先懂这个）

在编辑器里，你写完代码后点的是那个黄色按钮里的 **「烧录并运行」**。这两个词分开讲：

### 1）"运行"好懂——就是"出发！"
"运行"=`run`=让电脑现在就照你写的代码去做。就像你写好一张"前进 2 秒"的便条，喊一声"出发！"，机器人就开始照做。不点运行，代码只是躺在那儿，机器人一动不动。

### 2）"烧录"是什么？打个比方 🔥
"烧录"听着吓人，其实你可以这么想：
- 机器人脑子里有个**小仓库（编程模式）**，平时空着；
- 你点「烧录并运行」，电脑不但让机器人"现在动一下"，还顺手把这份程序**存进机器人脑子里的仓库**，就像把一张便条**贴进它的备忘板**，以后在机器人屏幕上「编程模式」里能看到这个新程序。
- 为什么叫"烧录"？老说法里是把程序"刻"进机器芯片里（像用火烧个印）。现在不用真烧，但名字留下来了——你只要记住：**烧录 = 把程序存进机器人，让它记住这份代码**。

> 💡 记不住"烧录"俩字也没事，你就当它是"**存进机器人 + 立刻跑**"这一个按钮。

### 3）红灯闪烁是在说啥？
点下去以后，**主控（机器人身体）会闪一下红灯**——这不是坏了！那是机器人在说："收到收到，我正在把程序写进肚子、准备跑啦！" 红灯闪完，程序就开始动。
- 如果红灯一直狂闪、机器人没动 → 多半是 IP 没连对或不在同 WiFi，回头看步骤①④。
- 如果根本不闪灯、也没反应 → 检查 uCode Link 是不是还开着（右下角托盘）。

### 4）"编程模式里发现新程序"又是什么？
跑完之后，你在机器人屏幕上进「编程模式」，会看到多了一个你刚写的程序。意思是：**这份代码已经被它记住了**，以后不用开电脑也能从机器人里找到它再跑（进阶玩法，本周先知道有这回事就行）。

> 一句话：**「烧录并运行」= 把便条贴进机器人备忘板 + 喊它出发 + 红灯闪一下表示"收到"**。

---

## 🧪 自己试着改（调试思维练习）
改一改、看看会发生什么（改完点 Run）：
- 把速度 `40` 改成 `70`，机器人走得更快还是更慢？
- 把 `0`（前进）改成 `1`（后退），机器人往哪走？
- 把 `"FFFF0000"`（红）改成 `"FF00FF00"`（绿），屏幕字变什么颜色？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 🚀 进阶挑战（选修，用 HP Win10 电脑）
想玩"自己装 Python 写代码"？换到 **HP（Win10）** 电脑，照下面三步（来自官方手册）：
1. **装 Python 3.7+**：去 https://www.python.org/downloads/ 下 Win10 版，**安装时勾"Add Python to PATH"**。
2. **装 SDK**：打开 cmd，敲
   ```
   pip install ugot
   ```
   国内网络差用清华镜像：`pip install ugot -i https://pypi.tuna.tsinghua.edu.cn/simple`
3. **跑官方示例**（灯带红色呼吸 2 秒）：
   ```python
   from ugot import ugot
   import time
   got = ugot.UGOT()
   got.initialize("主控IP")   # ← 换成机器人真实 IP
   got.show_light_rgb_effect(255, 0, 0, 0)
   # effect: 0常亮 1关闭 2呼吸 3闪烁
   time.sleep(2)
   ```

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 机器人连上、动了一下
- [ ] 屏幕显示了文字
- [ ] 你能给家人演示："看，它听我的！"

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 前进 N 秒 | `ug.mecanum_move_speed_times(0, 40, 2, 0)` |
| 屏幕写字(红) | `ug.screen_print_text("文字", "FFFF0000")` |
| 灯带红色呼吸 | `ug.show_light_rgb_effect(255, 0, 0, 2)` |
| 急停 | `ug.stop_chassis()` |

**第三方工具（选修）**：装 SDK `pip install ugot` ｜ 升级 `pip install --upgrade ugot` ｜ 需 Python 3.7+

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
