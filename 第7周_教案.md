# 第7周 · 教案（理论课）🤖
### 给七年级小朋友的「颜色与人物识别」第七课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让机器人认出红/绿球、或认出家人的脸喊名字"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：工程车（机械臂前缀 `mechanical_*`）**
> 这周认出红/绿球后，让**工程车**用机械臂指向、甚至夹取目标——这是其它形态都没有的"手"。形态就藏在代码里：视觉识别（`load_models` / `get_color_total_info`）通用，多出来的是 `mechanical_*` 机械臂动作（伸出/夹取/抬起），你写的夹取函数就是工程车的"手"。工程车机械臂舵机多、走线要理好，夹爪先调松紧适中。

前几周机器人会看路（TOF 测距）、还会听我们比手势对不对？超棒！这周给它装上一双真正的"AI 眼睛"——能**认出颜色**，或者**认出是谁的脸**。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"AI 模型"是啥**：知道 `load_models` 就像给机器人换"眼睛技能包"，想用颜色眼睛就装 `color_recognition`，想用人脸眼睛就装 `face_recognition`。
2. **装好眼睛并取回结果**：写出 `ug.load_models(['color_recognition'])`，再用 `ug.get_color_total_info()` / `ug.get_face_recognition_total_info()` 把机器人"看到"的东西拿回来。
3. **用 `json.loads` 把"文字"变"字典"**：明白识别结果是一段像字典的文字（JSON），得先 `data = json.loads(raw)` 才能取出里面的颜色名 / 人名。
4. **用 `if / elif` 做"分类反应"**：能写出"看到红→红灯红字，看到绿→绿灯绿字"，讲得清 `elif` 是"否则如果"。
5. **二选一、自己改着玩**：能换成蓝球闪蓝灯，或录入家人脸后让机器人喊名字，并说清改的是哪一行——这就是调试思维。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人只会"看距离"和"听手势"；这周之后，它能真正**认出东西**了——红还是绿、这是谁。这就是 AI 视觉（计算机视觉）最神奇的地方：你写几行规则，机器人替你把"看到的一团像素"变成"红色！""是爸爸！"。记住这种"它真的认出来了"的惊喜感！

**📌 一句话目标：** 用 `load_models` 装"颜色眼睛"或"人脸眼睛"，配合 `if` 判断，让机器人正确认出颜色/人脸并做出不同反应，拿到"我的机器人能认东西"的成就感。这步通了，第8周巡线、后面综合项目都在这双"眼睛"上长出来。

---

## 一、这周我们要干啥？🚀
前几周我们让机器人动、数数、拼路线、还会自己看路刹车对不对？这周给它升级成**有视觉的小车**：

- **选项A 颜色分拣车**：摄像头看到红球就闪红灯、喊"红色"；看到绿球就闪绿灯、喊"绿色"。
- **选项B 人脸识别喊名**：先录入家人的脸、起个名字，机器人认出来就喊"你好 爸爸"。

两个都超好玩，**二选一**，挑你最想挑战的。要做到这个，你要认识三个新朋友：**AI 模型 model**、**读取识别结果**、**JSON 文字变字典**，再用上第4周学过的**判断 if/elif** 让不同结果做不同反应。

## 二、AI 模型 model —— 给机器人换"眼睛技能包"
你手机里装了不同 APP 对吧？想拍照用相机 APP，想聊天用微信。机器人也一样：它肚子里装了好几种"眼睛技能"，想用哪个，就先把它**加载（load）**进来。

```python
ug.load_models(['color_recognition'])    # 装"颜色眼睛"
# ug.load_models(['face_recognition'])   # 或者装"人脸眼睛"
```
- `load_models` 意思是"加载模型"——模型（model）就是一套训练好的"认东西本领"。
- 方括号 `[...]` 里写模型名字。今天咱们**一次专心用一个**，别两个一起装（装多了机器人会犯迷糊）。

> 💡 小比喻：模型就像机器人的"眼睛技能包"——`color_recognition` 是"认颜色眼镜"，`face_recognition` 是"认脸眼镜"。想用哪副，先戴上。

## 三、把"看到"的东西取回来（读取识别结果）
装好眼睛后，用下面这两个方法把结果拿回来：
```python
颜色结果 = ug.get_color_total_info()             # 颜色眼睛看到的
人脸结果 = ug.get_face_recognition_total_info()  # 人脸眼睛看到的
```
- 拿到的是一段**文字**（后面讲它长啥样）。
- ⚠️ 先装对应模型，再读结果——没装 `color_recognition` 就去 `get_color_total_info`，机器人会迷糊报错。

> 💡 小提醒：这两个方法**返回的是一段文字**，不是直接能用的数字。下一节就讲怎么把它变成能取用的东西。

## 四、JSON 文字怎么变成能用的东西？`json.loads`
机器人返回的那段文字，长得像字典，但电脑还当它是"一串字"。我们要用 `json.loads` 把它**变成真正能用的字典**：
```python
import json
raw = ug.get_color_total_info()   # raw 是一段像字典的文字
data = json.loads(raw)            # data 变成真字典，能取里面的内容了
```
变成字典后，就能取里面的字段：
- `data['total']`：一共看到几个颜色块（数字）。
- `data['info'][0]['color']`：第 1 个看到的颜色叫啥（比如 `"red"` 红、`"green"` 绿）。

> 💡 小比喻：`json.loads` 就像一个"翻译机"——把机器人说的"外国话（文字）"翻译成你能听懂的"家乡话（字典）"，翻译完你才能问它"第几个是什么颜色"。

> ⚠️ 小提醒：如果你这版 SDK 直接返回字典（打印 raw 出来已经是 `{total:...}` 而不是带引号的纯文字），那就**不用翻译**：把 `data = json.loads(raw)` 直接写成 `data = raw` 就行。动手时步骤①会教你确认。

## 五、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

## 六、用 if / elif 做"分类反应"（不同结果 → 不同动作）
第4周我们用 `if 距离 < 30` 判断"近不近"，这次一模一样，只是判断的东西从"数字"变成了"看到的颜色名字"。而且因为颜色可能不止两种，我们用 `elif`（否则如果）：
```python
if 颜色 == 'red':
    ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
    ug.play_audio_tts("这是红色", 0)
elif 颜色 == 'green':
    ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
    ug.play_audio_tts("这是绿色", 0)
else:
    ug.turn_off_lights()                       # 其它颜色：关灯
```
- `==` 是两个等号，意思是"等于"（一个等号是"把值装进变量"，别混）。
- `elif 颜色 == 'green':` 读成"否则如果颜色是绿"——像排队领奖：红先领，绿排第二，其它进 `else`。
- 灯带编号 `[0,1,2,3]` 是四个灯位置（顶/左/右/底），都填上就是整圈同色。

> 💡 小比喻：`if / elif / else` 就像一个分诊台——红去红灯窗口、绿去绿灯窗口、其它去"关灯"窗口，各走各的门。

## 七、这周学会后，你应该能回答这几个问题
- `load_models(['color_recognition'])` 是在干啥？（提示：给机器人装"颜色眼睛"）
- 识别结果为什么要用 `json.loads(raw)` 翻译一下？（提示：它返回的是文字，变字典才能取内容）
- `data['info'][0]['color']` 在取什么？（提示：第 1 个看到的颜色名字）
- `elif` 和 `if` 有啥不一样？（提示：elif 是"否则如果"，用来排第二第三的条件）
- 为什么判断颜色要用 `==` 而不是 `=`？（提示：== 是比较"等不等于"，= 是把值装进变量）

> 答得上来，理论就过关啦！翻《实践手册》去让它真的认出颜色/人脸 💪
