# 第13周 · 教案（理论课）🤖
### 给七年级小朋友的「蜘蛛全向移动 + 传感器判断」第十三课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"把蜘蛛的全向移动做成按钮、再用'眼睛'判断前方有墙就自动绕开"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：蜘蛛（运动前缀 `spider_`）**
> 这周我们换一台**蜘蛛机器人**——四条仿生腿向四周伸开，能**全向移动**（原地转、往哪走都行），特别适合在窄场地巡逻。代码里它的专属前缀是 `spider_`：所有 `spider_move_speed` / `spider_turn_speed` 就是"蜘蛛怎么动"。本周你把第3周"把动作封装成函数按钮"的本事、和第4周"用 TOF 眼睛看路、自己决定停"的本事，都装在它身上，让它绕场巡逻、遇墙自动转向绕开。想换别的形态？把前缀一换就行，Python 写法不变。

前几周你做过：第3周"把前进/后退/转弯做成按钮"、第4周"看前方距离、近了就自己停"。这周我们要把这两块老积木，给**一台四足全向的蜘蛛**做出两个新本事：①把"走/退/转"做成全向移动按钮；②让它一边巡逻、一边用"眼睛"看路，前方有墙就**自动转向绕开**，不撞。

**✅ 这周结束，你要能：**
1. **说清楚蜘蛛是啥**：知道它是"四条腿向四周伸开"的仿生机器人，**全向移动**（原地转、四面走），机身低、重心低，适合巡逻。
2. **认下第 13 周的大作品**：**「蜘蛛全向巡逻 + 传感器判断」**——绕场地巡逻，用 TOF 测距判断前方有墙/障碍，遇障就自动转向避开。
3. **复习并升级"函数按钮"**：能写出 `走` / `倒` / `转` 等按钮，把蜘蛛的全向移动封装起来（第3周）。
4. **升级"避障"**：明白第4周是"近了就停"，本周升级成"近了就**转向绕开**"——把避开动作也做成一个 `避开()` 按钮。
5. **会"三关叠加"**：先让蜘蛛动起来+封装按钮（关1）→ 加测距"遇障停"（关2）→ 升级成"遇障转向避开+巡逻"（关3）。

**🚀 这周学完，你会多一个超能力：**
> 以前第4周避障是"看一眼、停住"；这周起，你**让蜘蛛学会"看一眼、拐个弯绕过去"**——它不再傻站着，而是自己想办法继续走。这是"传感器判断"从"停"进化到"绕"的跨越！记住这种"它自己绕着墙走、不撞"的聪明感 😎

**📌 一句话目标：** 把"全向移动"封装成函数按钮，并让蜘蛛巡逻时遇障自动转向避开，跑通"全向巡逻 + 遇障绕开"的完整流程。

---

## 一、这周我们要干啥？🚀
前几周你攒下的本事，咱们数一数：
- 把动作做成"按钮"（函数 `def`），拼成路线（第3周）
- 用 TOF "眼睛"看前方距离、近了就自己停（第4周）

这两块就像两块**乐高积木**。这周我们给它们换个最会"钻缝隙"的新底盘——**蜘蛛**，让它在场地里巡逻，前方一有墙就自己拐弯绕开。

第 13 周咱们不贪多，就定**一个场景项目**👇

## 二、第 13 周的大作品长啥样？🎬
> **项目名：蜘蛛全向巡逻 + 传感器判断**

场景是这样的（找一块周围有墙/纸箱的空地）：
1. 蜘蛛**绕场地巡逻**——平时一直往前走，绿灯亮着 = "我在巡逻"；
2. 它前面有 TOF "眼睛"一直量距离，前方 **< 30cm 有墙/障碍**（或读不到 `-1`）→ 它**立刻停下、闪红灯**，然后**自动转向**绕开，再继续前进巡逻；
3. 全程不用你动手，它自己看路、自己绕墙，就是不撞。

**🎯 本周验收标准（给爸爸妈妈演示这个流程就算通关）：**
> 蜘蛛全向巡逻，并在**接近障碍时自动转向避开**（不撞墙）

**🧩 融合的前序知识点（两块合一）：**
- 第3周 函数与运动（把"走/退/转"封装成全向移动按钮）
- 第4周 传感器判断（TOF 测距 `read_distance_data(1)`，if 判断，遇障反应）

> 💡 小比喻：这就像你给蜘蛛请了个"巡逻保安+导航员"——保安（TOF）盯着前方，导航员（代码）一看要撞墙就喊"拐弯！"，蜘蛛自己绕过去接着巡逻。

## 三、回顾：你攒下的"积木"速览
动手前，先把要用到的本事收进一张表，这就是本周的"材料库"：

| 积木（本事） | 一句话能干啥 | 来自 |
|---|---|---|
| 🔌 连接 | 三行代码让电脑抓住机器人（填 IP） | 第1周 |
| 🕷️ 运动 | `spider_move_speed` / `spider_turn_speed` 前进/转弯 | 第4周(用蜘蛛) |
| 🔧 函数按钮 | `def 走(秒数):` 把一串动作打包成按钮 | 第3周 |
| 👀 传感器 | `read_distance_data(1)` 读前方距离（厘米，-1=读不到） | 第4周 |
| 🚦 判断 | `if 距离 < 30:` 近了就反应 | 第4周 |
| 🛑 安全 | `try/except` + `stop_chassis()` 急停 | 第4周 |

> 💡 你看，本周用到的每一块，都是你已经学过的！这周的新意在于：① 换上**蜘蛛**底盘（全向移动）；② 把第4周的"遇障停"**升级成"遇障绕开"**。

## 四、新朋友：蜘蛛是什么？
前面几周你拼过麦轮车、四足狗、轮足、平衡车……这周换 **蜘蛛（Spider）**：
- 它是**四条仿生腿向四周伸开**的机器人，机身低、重心低，腿能往不同方向蹬，所以能**全向移动**——前进、后退、左转、右转，甚至原地打转都轻松，特别适合在窄场地、有障碍的地方钻来钻去。
- 我们的代码不用管它"先迈哪条腿"，只要下命令：
  - `ug.spider_move_speed(0, 40)` —— 前进（方向 0前/1后/2左转/3右转，速度 5~80）
  - `ug.spider_turn_speed(2, 120)` —— 转弯（方向 2左转/3右转，速度 5~280）

> 💡 小提醒：蜘蛛腿向四周伸，**别在太挤、有易碎品**的地方玩，腿容易扫到东西。拼装时四条腿要对称、舵机角度归中，装完让它在原地站一站再动。

形态对应代码复习：把运动方法里的 `mecanum_` 换成 `spider_`(蜘蛛) / `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车) / `transform_`(变形车)。本周咱们就认 `spider_` 这一个。

## 五、复习"函数按钮"：把全向移动封装起来（第3周）
第3周我们学过 `def`：把一串动作起个名字做成按钮。这周把蜘蛛的"走/退/转"都做成按钮，以后巡逻、绕墙只要喊按钮名字：

```python
前进 = 0
后退 = 1
左转 = 2
右转 = 3

def 走(秒数, 速度=40):
    ug.spider_move_speed(前进, 速度)
    time.sleep(秒数)
    ug.stop_chassis()          # 走完停，不然会一直走

def 转(方向, 秒数, 速度=120):
    ug.spider_turn_speed(方向, 速度)
    time.sleep(秒数)
    ug.stop_chassis()          # 转弯必须停，否则转圈圈
```
- 冒号 `:` 不能丢，下面**缩进**的行都属于这个按钮。
- `秒数` 是参数（可调旋钮），`速度=40` 是默认值。
- 这样 `走(2)`、`转(右转, 1)` 几个字就指挥蜘蛛动，清爽又不容易写错。

> 💡 小比喻：函数就像一个"一键宏"——你平时要写一整行的动作，录成一个按钮，以后点一下全做完。

## 六、从"遇障停"升级到"遇障绕开"（本周最核心！）
第4周我们写过"近了就停"。这周要**升级**：近了不傻站，而是**转向绕开**。秘密就在把"避开"也做成一个按钮 `避开()`：

```python
安全距离 = 30

def 避开():
    ug.stop_chassis()                  # 先停，安全第1
    ug.spider_turn_speed(3, 120)       # 右转绕开（3=右转）
    time.sleep(0.8)                    # 转一会儿
    ug.stop_chassis()                  # 转完停，准备继续走
```
然后在巡逻循环里：**前方有墙 → 按一下 `避开()` 按钮**，它就自己拐弯；拐完继续前进巡逻。

> 💡 重点：第4周是 `if 距离 < 30: ug.stop_chassis()`（停）；本周是 `if 距离 < 30: 避开()`（停+拐弯）。只把"停"换成"避开按钮"，蜘蛛就从"站住"变成"绕路"——这就是函数组合的妙处：换一个按钮，行为就升级了。

## 七、"最小可跑版（MVP）"——先动起来，再装修 🏗️
正确姿势还是 **MVP（最小可跑版）**，本周用**"三关叠加法"**：
- **关 1**：先让蜘蛛**动起来 + 封装"走/退/转"按钮**（确认腿听指挥）；
- **关 2**：在关 1 上加**测距"遇障停"**（第4周本事，先不绕）；
- **关 3**：把"停"升级成 **`避开()`（遇障转向绕开）+ 一直巡逻**，完成大作品。

> 💡 小比喻：就像先让蜘蛛会走、再教它"见墙停"、最后教它"见墙拐弯绕过去"。一层层加，每层都能跑，就不会卡死。

## 八、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。每个项目都直接复用，不重复讲啦。

> 小提醒：本周要让蜘蛛"歇一会儿再停"，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 九、这周学会后，你应该能回答这几个问题
- 蜘蛛跟四足狗比，最不一样的地方是啥？（提示：四腿向四周伸开、能全向移动/原地转）
- 第 13 周的大作品是哪两块老积木拼起来的？（提示：第3周 函数封装 + 第4周 TOF 判断）
- 第4周避障是"近了就__"，本周升级成"近了就__"？（提示：停 → 转向绕开）
- `避开()` 这个按钮里，为什么最后要有 `stop_chassis()`？（提示：转弯会一直转，得喊停）
- 验收标准是哪两件事？（提示：全向巡逻 + 接近障碍自动转向避开）

> 答得上来，理论就过关啦！翻《实践手册》去造蜘蛛的移动按钮、教它绕墙巡逻 💪
