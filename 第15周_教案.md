# 第15周 · 教案（理论课）🤖
### 给七年级小朋友的「工程车机械臂抓取 / 取放」第十五课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让工程车认出目标、开过去、用机械臂夹起来搬到指定位置"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：工程车（运动前缀 `mechanical_`）**
> 这周我们换一台**工程车**——它带一条 **3 轴机械臂**（底座旋转 + 大臂 + 小臂 + 夹爪），是七种形态里**唯一有"手"的**！代码里它的移动命令前缀是 `mechanical_`：`mechanical_move` / `mechanical_turn` 让车身走。本周你把第3周"把动作封装成函数"的本事、和第4/7周"摄像头认东西"的本事，都装在它身上，让它认出目标、开过去、用机械臂夹起来、搬到指定位置放下。想换别的形态？把前缀一换就行，Python 写法不变。

前几周你做过：第3周"把前进/后退/转弯做成按钮"、第7周"摄像头认颜色/认人脸"、第4周"用 TOF 看距离"。这周我们要把这几块老积木，给**一台有机械臂的工程车**做出一个新本事：①摄像头认出目标（比如红球）；②开到目标前；③用机械臂**夹取 → 搬运 → 放下**，完成一次"取放"。

**✅ 这周结束，你要能：**
1. **说清楚工程车是啥**：知道它是"带 3 轴机械臂"的机器人，机械臂能伸、能夹、能抬，是其它形态没有的"手"。
2. **认下第 15 周的大作品**：**「工程车机械臂抓取 / 取放」**——认出目标 → 开到目标前 → 夹取 → 搬运 → 放到指定位置。
3. **复习并升级"函数按钮"**：能写出 `伸出()` / `夹取()` / `抬起()` / `放下()` 等按钮，把机械臂动作封装起来（第3周）。
4. **复习"视觉识别"**：能加载 `color_recognition`，认出目标颜色、读出它的位置（第7周）。
5. **会"三关叠加"**：先让工程车认出目标+开过去（关1）→ 把机械臂动作封装成函数（关2）→ 串成"识别→夹取→搬运→放下"完整取放（关3）。

**🚀 这周学完，你会多一个超能力：**
> 以前你是"让机器人动"或"让机器人认东西"；这周起，你**让机器人像人一样"看见目标 → 伸手夹起来 → 搬走放下"**——它第一次有了"手"，能真正"干活"了！记住这种"它用夹爪把球夹起来搬走"的成就感 😎

**📌 一句话目标：** 把"视觉识别 + 函数封装"拼到工程车上，跑通一次"识别→夹取→搬运→放下"的完整取放。

---

## 一、这周我们要干啥？🚀
前几周你攒下的本事，咱们数一数：
- 把动作做成"按钮"（函数 `def`），拼成路线（第3周）
- 摄像头认颜色、认人脸（第7周）
- 用 TOF "眼睛"看前方距离（第4周）

这三块就像三块**乐高积木**。这周我们给它们换个最"能干活的"新底盘——**工程车**（带机械臂），让它认出目标、开过去、用"手"夹起来搬走。

第 15 周咱们不贪多，就定**一个场景项目**👇

## 二、第 15 周的大作品长啥样？🎬
> **项目名：工程车机械臂抓取 / 取放**

场景是这样的（地上放一个红球当目标，旁边留一块"放置区"）：
1. 工程车**用摄像头认出红球**（加载颜色模型，认出 `red`）；
2. 它**开到红球前**（用 TOF 量距离，离得够近就停）；
3. 机械臂**伸出 → 夹取**（夹爪夹住红球）→ **抬起**（举起来）；
4. 它**搬运**到放置区，**放下**红球——完成一次"取放"。

**🎯 本周验收标准（给爸爸妈妈演示这个流程就算通关）：**
> 工程车用机械臂完成一次"识别 → 夹取 → 搬运 → 放下"的完整取放

**🧩 融合的前序知识点（两块合一）：**
- 第3周 函数与运动（把"伸出/夹取/抬起/放下"封装成函数按钮）
- 第4/7周 视觉识别（摄像头认出目标颜色 / 物体，定位目标）

> 💡 小比喻：这就像你给工程车请了个"搬运工"——眼睛（摄像头）找到球，脚（轮子）开过去，手（机械臂）夹起来搬走放下。眼、脚、手配合，一次活就干完了。

## 三、回顾：你攒下的"积木"速览
动手前，先把要用到的本事收进一张表，这就是本周的"材料库"：

| 积木（本事） | 一句话能干啥 | 来自 |
|---|---|---|
| 🔌 连接 | 三行代码让电脑抓住机器人（填 IP） | 第1周 |
| 🚗 移动 | `mechanical_move` / `mechanical_turn` 让车身走/转 | 本周(工程车) |
| 🎥 视觉 | `load_models(['color_recognition'])` + `get_color_total_info()` 认目标颜色 | 第7周 |
| 👀 传感器 | `read_distance_data(1)` 读前方距离（厘米，-1=读不到） | 第4周 |
| 🦾 机械臂 | `mechanical_*` 方法：夹爪/升降/旋转（工程车独有） | 本周新用 |
| 🔧 函数 | `def 夹取():` 把机械臂动作打包成按钮 | 第3周 |
| 🛑 安全 | `try/except` + `stop_chassis()` 急停 | 第4周 |

> 💡 你看，本周用到的每一块，都是你已经学过的！这周的新意在于：① 换上**工程车**底盘（带机械臂）；② 把"视觉找目标 + 函数控制手"**串成一次搬运干活**。

## 四、新朋友：工程车是什么？
前面几周你拼过麦轮车、蜘蛛、四足狗、变形车……这周换 **工程车（Engineer）**：
- 它在车体上加了一条 **3 轴机械臂**（底座旋转 + 大臂 + 小臂 + 夹爪）——这是七种形态里**唯一有"手"**的，能夹东西、搬东西。
- 车身移动命令和其它形态一样（前缀 `mechanical_`）：
  - `ug.mechanical_move(0, 35)` —— 前进（方向 0前/1后/2左转/3右转，速度 5~80）
  - `ug.mechanical_turn(2, 120)` —— 转弯（方向 2左转/3右转，速度 5~280）
- 机械臂的动作（伸、夹、抬、转）用 **`mechanical_*` 系列方法**控制——这是它独门绝技。

> 💡 小提醒：工程车机械臂舵机多、**走线要理好**别缠轮子，夹爪先调松紧适中（能夹住球又不会捏太死）。这是七种里零件最多的，慢慢拼不急。

形态对应代码复习：把运动方法里的 `mecanum_` 换成 `mechanical_`(工程车) / `transform_`(变形车) / `spider_`(蜘蛛) / `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车)。本周咱们就认 `mechanical_` 这一个。

## 五、复习"函数按钮"：把机械臂动作封装起来（第3周）
第3周我们学过 `def`：把一串动作起个名字做成按钮。这周把机械臂的四个动作做成按钮，以后取放只要喊按钮名字：

```python
def 伸出():
    # 机械臂往前伸（具体方法名看你这版 SDK，见下方说明）
    ...

def 夹取():
    # 夹爪闭合，夹住目标
    ...

def 抬起():
    # 机械臂举起来
    ...

def 放下():
    # 机械臂放下 / 夹爪张开
    ...
```
- 冒号 `:` 不能丢，下面**缩进**的行都属于这个按钮。
- 这样 `伸出(); 夹取(); 抬起()` 几个字就指挥机械臂动，清爽又不容易写错。

> 💡 小比喻：函数就像一个"一键宏"——你平时要写一整行的动作，录成一个按钮，以后点一下全做完。

## 六、复习"视觉识别"：认出目标颜色（第7周）+ 升级成"开过去"（本周）
第7周学过：加载颜色模型，就能认出红/绿球。我们用它**定位目标**：

```python
ug.load_models(['color_recognition'])
raw = ug.get_color_total_info()
data = json.loads(raw)            # 翻译：文字→字典
if data['total'] > 0:
    颜色 = data['info'][0]['color']        # 第1个颜色的名字，比如 "red"
    x = data['info'][0]['x']               # 它在画面里的左右位置（小=左，大=右）
```
然后配合第4周的 TOF：开到目标前、离得够近（比如 < 15cm）就停——这就是"开到目标前"。

> ⚠️ **关于机械臂 `mechanical_*` 方法名（重要，别瞎猜）**：控制夹爪/升降/旋转的方法是 `mechanical_*` 系列，但**不同 SDK 版本名字可能不同**（比如夹爪可能叫 claw / grip，升降可能叫 up / lift）。我们的规矩是"不编方法名"。所以 `伸出()` / `夹取()` / `抬起()` / `放下()` 这几个按钮里，你要**照着你这版 SDK 里真实的 `mechanical_` 机械臂方法**来写（去编辑器里看能调用的方法列表，或翻官方文档 `docs.ubtrobot.com/ugot/` 确认）。下一节和《实践手册》都会教你一步步确认。

## 七、"最小可跑版（MVP）"——先动起来，再装修 🏗️
正确姿势还是 **MVP（最小可跑版）**，本周用**"三关叠加法"**：
- **关 1**：先让工程车**认出目标 + 开到目标前**（第7周 颜色 + 第4周 TOF）；
- **关 2**：在关 1 基础上，把**机械臂动作封装成函数**（伸出/夹取/抬起/放下）；
- **关 3**：把关 1 + 关 2 串起来，完成 **"识别 → 夹取 → 搬运 → 放下"** 完整取放，完成大作品。

> 💡 小比喻：就像先教它"看见球并走过去"、再教它"手怎么动"、最后让它"看见→走过去→伸手夹起→搬走放下"一气呵成。一层层加，每层都能跑，就不会卡死。

## 八、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。每个项目都直接复用，不重复讲啦。

> 小提醒：本周要让小车"歇一会儿再动"，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 九、这周学会后，你应该能回答这几个问题
- 工程车跟别的形态比，最独门绝技是啥？（提示：带 3 轴机械臂，有"手"能夹能搬）
- 第 15 周的大作品是哪几块老积木拼起来的？（提示：第3周 函数 + 第4/7周 视觉识别）
- 怎么让工程车"认出红球并走到它前面"？（提示：颜色识别定位 + TOF 量距离靠近）
- 取放流程是哪四步？（提示：识别 → 夹取 → 搬运 → 放下）
- 为什么机械臂的方法名不能乱写？（提示：不同 SDK 版本名字不同，要照真实方法写，不编名字）
- 验收标准是哪一件事？（提示：用机械臂完成一次"识别→夹取→搬运→放下"）

> 答得上来，理论就过关啦！翻《实践手册》去造工程车的机械臂、教它夹球搬走 💪
