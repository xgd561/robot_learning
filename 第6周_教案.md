# 第6周 · 教案（理论课）🤖
### 给七年级小朋友的「真AI手势遥控」第六课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让机器人认出你的手势、再听手势指挥"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：轮足（运动前缀 `wheelleg_`）**
> 这周的"手势遥控"由**轮足**来听你指挥——你比"手掌"它就前进、比"点赞"就停，轮足靠腿屈伸稳稳走。形态就藏在代码里：加载模型、读手势、比字符串这些"AI 眼睛"代码是通用的，只有"怎么动"要换成 `wheelleg_`，也就是"轮足怎么走"。装轮足时舵机角度先归中再装，避免一开机乱蹬。

前几周机器人已经会"自己看路、自己刹车"（第4周避障），还会"闪灯喊话"（第5周报警车）对不对？超棒！这周我们要给它装上**真·AI 的眼睛**——让它认出你比的是"手掌"还是"点赞"，然后**听你的手势指挥**前进、停下、转弯。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"AI 模型"是啥**：知道要先 `ug.load_models(['gesture'])` 把"认手势的小专家"唤醒，模型就像机器人脑子里一个会看图、认手势的专家。
2. **用 `json.loads` 解析 JSON**：明白 AI 返回的是一张"带名字的数据条"（JSON），`json.loads` 把它拆开成能看懂的内容。
3. **用字典取值 `data.get('gesture')`**：知道字典就像一个"贴了标签的抽屉柜"，用名字 `'gesture'` 就能取出对应的手势值。
4. **用字符串比较 `==` 判断手势**：能写出 `if 手势 == 'palm':` 让机器人按不同手势做不同动作，讲得清两边都要加英文单引号。
5. **把 5 种手势映射到 5 个动作**：手掌→前进、点赞→停、剪刀→左转、OK→右转、石头→后退。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人是"你写命令它才动"；第4周它学会"自己看路"，这周它学会"认出你的手势"。这是你第一次让它用上**真 AI**——摄像头看到你的手，大脑认出是哪种手势，再照你的意思动。记住这种"我比个手势，它就听话"的神奇感！

**📌 一句话目标：** 加载手势模型 + 读 JSON + 用 `==` 比手势，做出一个"手势遥控器"：比手掌前进、比点赞停下，能加更多手势映射。这一步通了，第7周认颜色/人脸、第8周巡线都是在"AI 看 + 判断"这棵树上开花。

---

## 一、这周我们要干啥？🚀
前几周我们让机器人动、数数、拼路线、自己避障、还会报警对不对？超棒！这周给它升级成**听得懂"手势"的机器人**：

- 它用摄像头当"眼睛"，盯着你的手；
- 脑子里的 AI 小专家认出你比的是"手掌"还是"点赞"；
- 你比"手掌"它就前进，比"点赞"它就停下——**像拿遥控器指挥它**！

要做到这个，你要认识四个新朋友：**AI 模型**、**JSON**、**字典**、**字符串比较 `==`**。别怕，一个一个来。

## 二、AI 模型 model —— 机器人脑子里的"看图小专家"
你前面玩的都是"写死"的命令：前进就前进。但"认出手势"这种事，靠人写规则可写不出来的——它得靠一个**训练好的 AI 模型**。

- 模型就像机器人脑子里一个**会看图的专家**。但你一开机，这个专家是"在睡觉"的，得先把它**叫醒**：
```python
ug.load_models(['gesture'])   # gesture = 手势，把"认手势"的专家叫醒
```
- `load_models` 里的 `['gesture']` 是说"我要加载'手势'这个模型"（模型名用英文单引号包起来）。
- 叫醒之后，机器人才"看得见"你的手势、才认得出来。

> 💡 小比喻：`load_models` 就像按开关打开一盏灯——灯亮了（模型加载好），房间才看得清。专家醒了，它才开始认手势。

## 三、JSON —— AI 给你的"带名字的小纸条"
专家认出手势后，不会大声喊"手掌！"，而是给你一张**带名字的数据条**。它长这样：
```json
{"gesture": "palm", "score": 0.95}
```
这就是 **JSON**（读作"杰森"，一种电脑之间传数据的格式）。你可以把它想成一张**快递单**：
- 左边 `"gesture"` 是"项目名"（标签）；
- 右边 `"palm"` 是"内容"（手势是手掌）。

> 💡 那为什么不直接给文字？因为 AI 一次可能认出好多信息（手势名、准不准的分数 score…），用 JSON 这张"快递单"打包好，我们想取哪个就取哪个。

## 四、字典 dictionary —— 用名字取东西的"抽屉柜"
JSON 拆开以后，在 Python 里变成一种叫**字典（dictionary）**的东西。字典就像一个**贴了标签的抽屉柜**：每个抽屉有名字（标签），拉开就拿出里面的东西。

```python
import json
raw = ug.VISION.gesture_inference()   # raw 是那张"纸条"（还是一串文字）
data = json.loads(raw)                # json.loads 把纸条拆成字典（抽屉柜）
手势 = data.get('gesture')            # 拉开 'gesture' 这个抽屉，取出手势名字，如 'palm'
```
- `json.loads(raw)`：`loads` 就是"把文字变成字典"，这样电脑才看得懂里面每一项。
- `data.get('gesture')`：`get` 是按名字取东西——"从抽屉柜里，把标签是 `'gesture'` 的抽屉打开，里面是什么就给我"。

> 💡 小比喻：字典就像学校的储物柜，柜门上写着名字，你喊"gesture 那个柜子"，它就递给你里面的东西。

## 五、字符串比较 `==` —— "这两个字一样吗？"
我们从抽屉里拿到手势名字（比如 `'palm'`，一串**文字**），接下来要让机器人"看它是什么、就做什么"。这要用**字符串比较 `==`**：

```python
if 手势 == 'palm':          # 如果手势是 'palm'（手掌）
    ug.mecanum_move_speed(0, 40)   # 就前进
```
- `==` 是"等于、一模一样吗"的意思，专门用来比**文字（字符串）**。
- 注意：两边都要加**英文单引号** `' '`！`'palm'` 是文字；左边 `手势` 已经是文字了不用加引号（它就是从抽屉里取出的文字）。

> 💡 小比喻：`==` 就像一个"对暗号"——你比的是 `'palm'`，机器人存的暗号也是 `'palm'`，对上了它就执行对应动作。

### 今天的手势有这 5 种（记住它们的英文！）
| 英文 | 中文 |
|------|------|
| `stone` | 石头 ✊ |
| `palm` | 手掌 🖐 |
| `ok` | OK 👌 |
| `good` | 点赞 👍 |
| `scissor` | 剪刀 ✌ |

> 这 5 个英文就是 AI 认出来的"暗号"。后面我们让每个暗号对应一个动作，就做成"手势遥控器"啦。

## 六、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

> 小提醒：本周要"一直盯着你的手看"，得在开头加一行 `import json`（拆 JSON 纸条用），待会儿实践手册里会看到。

## 七、这周学会后，你应该能回答这几个问题
- `load_models(['gesture'])` 是在干啥？（提示：把"认手势"的 AI 专家叫醒）
- AI 返回的那张"带名字的纸条"叫什么？（提示：JSON）
- `data.get('gesture')` 是从哪取、取什么？（提示：从字典里取手势名字）
- `if 手势 == 'palm':` 里的 `==` 在比什么？为什么右边要加单引号？（提示：比文字一不一样；`'palm'` 是文字）
- 把"手掌"换成"点赞"，代码里改哪几个字？（提示：把 `'palm'` 改成 `'good'`）

> 答得上来，理论就过关啦！翻《实践手册》去造你的手势遥控器 💪
