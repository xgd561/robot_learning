# 第12周 · 教案（理论课）🤖
### 给七年级小朋友的「四足狗仿生步态 + 语音播报」第十二课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"把几种走法打包成按钮、每切换一次就开口播报"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：四足狗（运动前缀 `dog_`）**
> 这周我们换一台**四足狗**——四条腿仿生走路，像真狗狗一样！代码里它的专属前缀是 `dog_`：所有 `dog_move_speed` / `dog_turn_speed` 就是"四足狗怎么动"。本周你把第3周"把动作封装成函数按钮"的本事、和第5周"开口说话（TTS）"的本事，都装在它身上，让它用不同步态走路，每换一种就用女声报出来。想换别的形态？把前缀一换就行，Python 写法不变。

前几周你做过：第3周"把前进/后退/转弯做成按钮，拼成路线"、第5周"让机器人闪灯又会喊话"。这周我们要把这两块老积木，给**一台会迈四条腿的狗**做出两个新本事：①把"走/跑/转身"做成不同的"步态按钮"；②每切换一个动作，它就开口播报"我在走 / 我在跑"。

**✅ 这周结束，你要能：**
1. **说清楚四足狗是啥**：知道它是"四条腿仿生"的机器人，能走出像狗一样的步态（走、跑、转身），特别萌。
2. **认下第 12 周的大作品**：**「四足狗仿生步态 + 语音播报」**——把"走/跑/转身"封装成 `walk()` / `run()` / `turn()` 函数，每切换一个动作就语音播报。
3. **复习并升级"函数按钮"**：能写出 `def walk(秒数):` / `def run(秒数):` / `def turn(秒数):`，讲得清参数和默认速度。
4. **把"语音播报"接进每个按钮**：明白在函数里加一行 `ug.play_audio_tts("我在走", 0)`，切换动作时它就开口，这就是"函数组合"。
5. **会"三关叠加"**：先让狗能动起来（关1）→ 把三种步态做成按钮（关2）→ 每个按钮里加语音播报（关3）。

**🚀 这周学完，你会多一个超能力：**
> 以前第3周你造了"走/转"按钮、第5周你让机器人开口；这周起，你**把"开口"装进每个按钮里**——狗一迈步就自己报"我在走"，一跑就报"我在跑"。这是"函数 + 语音"的组合升级：动作和声音绑在一起，一条命令全做了！记住这种"我一声令下，狗又动又报"的帅感 😎

**📌 一句话目标：** 把"走/跑/转身"封装成函数按钮，并让每个按钮切换时语音播报，跑通四足狗多步态切换 + 语音演示。

---

## 一、这周我们要干啥？🚀
前几周你攒下的本事，咱们数一数：
- 把动作做成"按钮"（函数 `def`），拼成路线（第3周）
- 让机器人开口说话、闪灯（第5周）

这两块就像两块**乐高积木**。这周我们给它们换个最萌的新底盘——**四足狗**，让它不仅"迈着狗步走路"，还**每走一步都开口报幕**，像个会解说的狗狗演员。

第 12 周咱们不贪多，就定**一个场景项目**👇

## 二、第 12 周的大作品长啥样？🎬
> **项目名：四足狗仿生步态 + 语音播报**

场景是这样的：
1. 四足狗有几种"走法"（步态）：**慢慢走 `walk()`**、**飞快跑 `run()`**、**原地转身 `turn()`**——每一种都做成一个按钮；
2. 你按一下 `walk(2)`，它就迈着狗步走 2 秒，同时女声播报 **"我在走"**；
3. 你再按 `run(2)`，它撒欢跑 2 秒，播报 **"我在跑"**；按 `turn(1)` 它转身，播报 **"我在转身"**；
4. 把几个按钮连起来，就像让狗狗表演一段"走→跑→转身"的小节目，每一下都有配音。

**🎯 本周验收标准（给爸爸妈妈演示这个流程就算通关）：**
> 四足狗能切换多种步态（走/跑/转身），且**每切换一个动作都有对应语音播报**

**🧩 融合的前序知识点（两块合一）：**
- 第3周 函数与运动（把"走/跑/转身"封装成 `walk()` / `run()` / `turn()` 函数）
- 第5周 声音（TTS 播报状态，`play_audio_tts` + `set_volume`）

> 💡 小比喻：这就像你给狗狗请了个"解说员"——它每做一个动作，解说员（语音）就喊一句"它在走！""它在跑！"。而你只要按一下按钮，动作和喊话就一起发生了。

## 三、回顾：你攒下的"积木"速览
动手前，先把要用到的本事收进一张表，这就是本周的"材料库"：

| 积木（本事） | 一句话能干啥 | 来自 |
|---|---|---|
| 🔌 连接 | 三行代码让电脑抓住机器人（填 IP） | 第1周 |
| 🐕 运动 | `dog_move_speed` / `dog_turn_speed` 前进/转弯 | 第5周(用狗) |
| 🔧 函数按钮 | `def walk(秒数):` 把一串动作打包成按钮 | 第3周 |
| 🔊 声音 | `play_audio_tts("文字", 0)` 女声播报 + `set_volume(3)` 调音量 | 第5周 |
| 🧩 组合 | 在函数里加一行语音，动作+声音绑一起 | 第5周(函数组合) |
| 🛑 安全 | `try/except` + `stop_chassis()` 急停 | 第4周 |

> 💡 你看，本周用到的每一块，都是你已经学过的！这周的新意在于：① 换上**四足狗**底盘（迈狗步）；② 把"语音播报"**装进每个函数按钮里**，切换动作就自动配音。

## 四、新朋友：四足狗是什么？
前面几周你拼过麦轮车、蜘蛛、四足狗（第5周其实是四足狗当巡逻保安）……这周正式认识它：
- 它是**四条腿仿生**机器人，腿像真狗狗一样交替迈步，能走出"走 / 跑 / 转身"等步态。
- 我们的代码不用管它"先迈哪条腿"，只要下命令：
  - `ug.dog_move_speed(0, 40)` —— 前进（方向 0前/1后，速度 5~80）
  - `ug.dog_turn_speed(2, 120)` —— 转弯（方向 2左/3右，速度 5~280）

> 💡 小提醒：四足狗靠四条腿走路，**别在太滑、太软**的地面玩，腿容易打滑。拼装时四条腿要对称、舵机角度先归中，装完让它在原地站一站再动。

形态对应代码复习：把运动方法里的 `mecanum_` 换成 `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车) / `spider_`(蜘蛛) / `transform_`(变形车)。本周咱们就认 `dog_` 这一个。

## 五、复习"函数按钮"+ 加一招：把语音装进去
第3周我们学过 `def`：把一串动作起个名字做成按钮。这周复习它，并**升级**——在函数里加一行语音，让按钮"又动又报"：

```python
def walk(秒数, 速度=40):                  # 造一个"走"按钮
    ug.dog_move_speed(0, 速度)            # ① 迈狗步前进
    ug.play_audio_tts("我在走", 0)        # ② 开口播报（新加的！）
    time.sleep(秒数)
    ug.stop_chassis()                     # 走完停，不然会一直走
```
- 冒号 `:` 不能丢，下面**缩进**的行都属于这个按钮。
- `秒数` 是参数（可调旋钮），`速度=40` 是默认值（不写就用 40）。
- **关键升级**：第②行 `play_audio_tts` 写在按钮里，所以你一按 `walk(2)`，它**既走又报**，不用在外面再写一遍语音。

> 💡 小比喻：就像你给遥控器按钮接了个喇叭——按"走"→狗走 + 喇叭喊"我在走"；按"跑"→狗跑 + 喇叭喊"我在跑"。一个按钮，两件事一起干。

## 六、把"走/跑/转身"做成三个按钮
不同的步态，其实就是**不同的速度 / 不同的动作**。我们用三个按钮区分：

```python
def walk(秒数, 速度=35):        # 慢慢走
    ug.dog_move_speed(0, 速度)
    ug.play_audio_tts("我在走", 0)
    time.sleep(秒数)
    ug.stop_chassis()

def run(秒数, 速度=70):         # 飞快跑（速度更大）
    ug.dog_move_speed(0, 速度)
    ug.play_audio_tts("我在跑", 0)
    time.sleep(秒数)
    ug.stop_chassis()

def turn(秒数, 速度=120):       # 原地转身（用转弯接口）
    ug.dog_turn_speed(2, 速度)  # 2=左转；想右转用 3
    ug.play_audio_tts("我在转身", 0)
    time.sleep(秒数)
    ug.stop_chassis()
```
- `walk` 和 `run` 都是前进，区别在**速度**（走慢、跑快）；
- `turn` 用转弯接口 `dog_turn_speed`，所以最后必须 `stop_chassis()`，否则它会一直转圈。

> 💡 卡住先想：为什么 `walk` 和 `run` 速度不一样、却都是"前进"？—— 因为"步态"就是"怎么动"，快慢就是两种步态。想加"后退"按钮？照 `walk` 把 `0`(前) 改成 `1`(后) 就行。

## 七、"最小可跑版（MVP）"——先动起来，再装修 🏗️
正确姿势还是 **MVP（最小可跑版）**，本周用**"三关叠加法"**：
- **关 1**：先让四足狗**动起来**——按一个按钮它会走（确认狗腿听指挥）；
- **关 2**：在关 1 上加**三个按钮**：`walk` / `run` / `turn`，连起来表演"走→跑→转身"；
- **关 3**：在关 2 的每个按钮里加**语音播报**——切换动作就开口"我在走/我在跑/我在转身"，完成大作品。

> 💡 小比喻：就像先让狗会走，再教它走/跑/转身三种花样，最后给它配个"解说员"边演边喊。一层层加，每层都能跑，就不会卡死。

## 八、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。每个项目都直接复用，不重复讲啦。

> 小提醒：本周要让狗"歇一会儿"再停，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 九、这周学会后，你应该能回答这几个问题
- 四足狗跟麦轮车比，最不一样的地方是啥？（提示：四条腿仿生，能走出"走/跑"等步态）
- `def walk(秒数, 速度=40):` 里的 `def` 和 `秒数` 各是干啥的？（提示：def=定义按钮；秒数=可调参数）
- `walk` 和 `run` 都是前进，区别在哪？（提示：速度不同，是两种步态）
- 怎么让"按按钮就开口播报"？（提示：在函数里加一行 `play_audio_tts`）
- 验收标准是哪两件事？（提示：多步态切换 + 每切换都有语音播报）

> 答得上来，理论就过关啦！翻《实践手册》去造三个步态按钮、给狗狗配个"解说员" 💪
