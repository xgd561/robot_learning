# 第14周 · 实践手册（动手课）🔧
### 给七年级小朋友的「变形车越障翻台阶 + 巡线」第十四周课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。**翻台阶别找太高的坎**，小台阶/小障碍块最合适，太高容易翻车。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：变形车（重新拼一台）
> 综合阶段第六周，我们换 **变形车** 当越野兵。一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 变形车结构件包；②固定主控到机身、装**升降/变形机构**、接好线；③盯三点（螺丝拧紧 / 机构方向装对 / 线别卡住机构）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **变形车拼法要点**：机体高度可调，装上升降 / 变形机构；注意机构舵机的**行程和限位**，别装反卡死。拼好先不通电，手动试试升降是否顺滑，再开机。本周它要"抬身子翻台阶"，升降机构一定要装对。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
4. 地上贴好一条黑线（宽 3~5 厘米、别太细），线中间放一个小台阶/小障碍块（书叠几本、或小纸盒，别太高）。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：巡线要**黑线贴在浅色地面**上（白纸/浅色瑜伽垫），摄像头朝地面；翻台阶的坎别超过小车能跨的高度。

---

## 📣 先确认"抬升车身"的真实方法名（本周关键一步，别跳过！）
> 变形车"抬升/下降机身"靠的是 `transform_*` 系列方法，但**不同 SDK 版本名字可能不同**（比如有的叫抬升、有的叫变形/爬阶）。我们的规矩是**不编方法名**，所以你得先确认你这版 SDK 里真实的写法，后面 `翻越()` 按钮里才好填。两个办法：
> - **办法 A（最稳）**：在 uPython 编辑器里，连上机器人后，输入 `ug.` 看自动弹出的方法列表，找带 `transform_` 且和"高度/升降/爬阶"相关的那个；
> - **办法 B**：翻官方文档 https://docs.ubtrobot.com/ugot/ 查变形车（transform）的车身高度/越障方法名。
> 找到后，把下面 `抬升车身()` / `下降车身()` 里注释的示例行，换成你确认过的真实方法名即可。**没确认前别乱填，填错小车不会动、还可能报错。**

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：变形车 越障翻台阶 + 巡线（transform_）
```
> 第 14 周就做这一个场景（课程安排里写好的）。它把第8周 巡线 + 第4周 TOF 判断，拼到变形车上，再用上它独门绝技"抬升车身翻台阶"。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第七节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🎥 巡线 | `load_models(['line_recognition'])` + `get_single_track_total_info()` 读偏移量 |
| ③ | 🔧 跟线 | 偏移>0 往右修 / <0 往左修 / ≈0 直走 |
| ④ | 👀 传感器 | `read_distance_data(1)` 读前方距离 |
| ⑤ | 🚦 判断 | `if 距离 < 30:` 近了就翻越 |
| ⑥ | 🔄 变形 | `翻越()`：停+抬升车身+前进翻过+下降恢复 |
| ⑦ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 变形车沿黑线巡线（叠第8周 巡线，先不翻越）✅
先让变形车**沿黑线自己走起来**，确认巡线眼睛和 `transform_` 接口都通：
```python
# 关1：变形车 沿黑线巡线（transform_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])   # 装"认黑线"模型
ug.set_track_recognition_line(0)        # 0=单轨(一条黑线)；1=双轨
ug.screen_print_text("开始巡线", "FFFFFF00")

阈值 = 10        # 偏移小于这个数，就算"基本直走"
系数 = 3         # 偏越多，修正越多
前进速度 = 35
修正速度 = 90

try:
    while True:
        信息 = ug.get_single_track_total_info()   # 返回 [偏移量, 线型, x, y]
        偏移量 = 信息[0]
        if 偏移量 > 阈值:
            ug.transform_turn_speed(3, min(120, 偏移量 * 系数))   # 偏右→往右修（3=右转）
            time.sleep(0.1); ug.stop_chassis()
        elif 偏移量 < -阈值:
            ug.transform_turn_speed(2, min(120, -偏移量 * 系数))  # 偏左→往左修（2=左转）
            time.sleep(0.1); ug.stop_chassis()
        else:
            ug.transform_move_speed(0, 前进速度)                  # 基本居中，直走
        time.sleep(0.05)
except:
    ug.stop_chassis()          # 不管出啥意外，先停车最安全
```
**应当看到**：变形车沿黑线自己走，线往哪歪它就往哪修，稳定跟一段不掉线。🎉
> 💡 卡住先想：如果它走反方向（线在右它往左），把上面的 `3` 和 `2` 对调就行——第一次跑让娃试一下方向。如果读 `信息[0]` 报错，先 `print(信息)` 看返回的是不是列表（有的版本返回字典），按真实结构改。

### 关 2 · 加测距，遇障停（叠第4周 传感器，先不翻）✅
在关 1 的跟线循环里，**最前面**加 TOF 测距：前方 < 30cm（或读不到 -1）就停下等，确认"看路"本事。注意**先判最危险的"-1"**：
```python
# 关2：变形车 巡线 + 遇障停（transform_，先不翻越）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])
ug.set_track_recognition_line(0)
安全距离 = 30
阈值 = 10; 系数 = 3; 前进速度 = 35; 修正速度 = 90

try:
    while True:
        距离 = ug.read_distance_data(1)          # 先读障碍
        if 距离 == -1 or 距离 < 安全距离:        # 有障碍/读不到 → 停 + 红灯
            ug.stop_chassis()
            ug.screen_print_text("有障碍", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)
            time.sleep(0.2)
            continue                             # 这圈先不跟线，等障碍移开
        # —— 没障碍，正常巡线 ——
        ug.show_light_rgb([0,1,2,3], 0, 255, 0)
        ug.screen_print_text("巡线中", "FF00FF00")
        信息 = ug.get_single_track_total_info()
        偏移量 = 信息[0]
        if 偏移量 > 阈值:
            ug.transform_turn_speed(3, min(120, 偏移量 * 系数)); time.sleep(0.1); ug.stop_chassis()
        elif 偏移量 < -阈值:
            ug.transform_turn_speed(2, min(120, -偏移量 * 系数)); time.sleep(0.1); ug.stop_chassis()
        else:
            ug.transform_move_speed(0, 前进速度)
        time.sleep(0.05)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：平时绿灯沿黑线走；你推台阶/纸箱到 30cm 内，它红灯停下不撞、等障碍移开又继续巡线。🎉
> 💡 卡住先想：`距离 == -1` 放最前面，是因为"读不到=最危险"，要第一个判断（忘了看《第4周_实践手册》）。这关它只会"停"，下一步教它"抬身子跨过去"。

### 关 3 · 升级"遇障抬升车身翻越 + 过后恢复"（叠变形绝技 → 完成大作品）✅
最后把"停"升级成 **`翻越()` 按钮**（停 + 抬升车身 + 前进翻过 + 下降恢复），遇到台阶就自动跨过去、再继续巡线。这就是课程安排里的"验收流程"：

```python
# 关3（终极版）：变形车 巡线 + 遇障抬升车身翻越 + 恢复继续巡线（transform_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

安全距离 = 30
阈值 = 10; 系数 = 3; 前进速度 = 35

# ===== 变形车独门绝技：抬升 / 下降机身 =====
# ⚠️ 下面的 transform_* 高度方法名，请替换成你这版 SDK 里确认过的真实方法（见步骤⓪说明）！
def 抬升车身():
    ug.screen_print_text("抬升机身", "FFFF0000")
    # ug.transform_raise_body()      # ← 换成你 SDK 里真实的"抬升车身"方法名
    pass   # 占位：确认真实方法后，把上面注释行取消注释即可

def 下降车身():
    ug.screen_print_text("恢复低姿态", "FF00FF00")
    # ug.transform_lower_body()      # ← 换成你 SDK 里真实的"下降车身"方法名
    pass

def 翻越():
    ug.stop_chassis()
    ug.screen_print_text("翻越中", "FFFF0000")
    ug.show_light_rgb([0,1,2,3], 255, 255, 0)   # 黄灯=正在翻越
    抬升车身()                                  # ① 抬升机身
    ug.transform_move_speed(0, 前进速度)         # ② 抬着身子往前开，翻过台阶
    time.sleep(1.5)
    ug.stop_chassis()
    下降车身()                                  # ③ 翻过去，恢复低姿态
    ug.turn_off_lights()

try:
    ug.load_models(['line_recognition'])
    ug.set_track_recognition_line(0)
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1 or 距离 < 安全距离:
            翻越()                              # 遇障 → 抬升车身翻越（不再傻站）
            continue
        # —— 没障碍，正常巡线 ——
        ug.show_light_rgb([0,1,2,3], 0, 255, 0)
        ug.screen_print_text("巡线中", "FF00FF00")
        信息 = ug.get_single_track_total_info()
        偏移量 = 信息[0]
        if 偏移量 > 阈值:
            ug.transform_turn_speed(3, min(120, 偏移量 * 系数)); time.sleep(0.1); ug.stop_chassis()
        elif 偏移量 < -阈值:
            ug.transform_turn_speed(2, min(120, -偏移量 * 系数)); time.sleep(0.1); ug.stop_chassis()
        else:
            ug.transform_move_speed(0, 前进速度)
        time.sleep(0.05)
except:
    ug.stop_chassis()           # 不管发生什么，先停车最安全
    ug.turn_off_lights()
```
**应当看到（验收三步）**：变形车绿灯沿黑线走 → 前方遇到小台阶，黄灯一亮、**抬升车身翻过去** → 过去后恢复低姿态、变回绿灯继续沿黑线巡线，全程不撞、不卡。🎉🎉🎉
> 💡 卡住先想：第4周是 `if 距离<30: ug.stop_chassis()`（停）；本周把那行换成 `翻越()`（停+抬升+翻过+恢复）——只换一个按钮，行为就从"站住"升级成"跨过去"。`抬升车身()` / `下降车身()` 里那两行 `transform_*` 一定要换成你**确认过的真实方法名**，不然小车不会抬身子（只会停+前进，那就成了关2的"停"版）。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关1：把 `阈值 = 10` 改成 `5`，对更小偏移也修正，走得更"贴线"还是更容易抖？
- 关1：把 `系数 = 3` 改成 `6`，修正更猛还是更温柔？太大是不是会左右晃？
- 关2/关3：把 `安全距离 = 30` 改成 `15` 或 `50`，它离坎更近还是更早反应？
- 关3：把 `翻越()` 里 `time.sleep(1.5)` 的 `1.5` 改成 `2.5`，它翻越用更久还是更短？（坎高就调大）
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把变形车拼好（升降机构装对、手动试过顺滑）、连上电脑
- [ ] 关1 跑通：变形车沿黑线自己走、会修偏
- [ ] 关2 跑通：巡线中遇障（< 30cm）自动停 + 红灯，障碍移开继续
- [ ] 关3 跑通：遇小台阶**自动抬升车身翻越**、过后恢复低姿态继续巡线
- [ ] 我能给家人演示验收：**巡线前进 + 遇障抬升翻越 + 过后恢复继续巡线**

---

## 🃏 随身速查卡（变形车巡线 + 翻越常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 加载巡线模型 | `ug.load_models(['line_recognition'])` |
| 设单/双轨 | `ug.set_track_recognition_line(0)`（`0`单轨/`1`双轨） |
| 读单线信息 | `ug.get_single_track_total_info()` → `[偏移量, 线型, x, y]`（`偏移量`：0没线/正偏右/负偏左） |
| 偏右往右修 | `ug.transform_turn_speed(3, 速度)` |
| 偏左往左修 | `ug.transform_turn_speed(2, 速度)` |
| 直走 | `ug.transform_move_speed(0, 35)` |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 抬升/下降机身 | `transform_*` 高度方法（**名字以你这版 SDK 为准**，见步骤⓪） |
| 红灯 / 绿灯 / 黄灯 | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` / `(255,255,0)` |
| 急停 | `ug.stop_chassis()` |
| 一直循环 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `transform_move_speed(方向, 速度)`：方向 0前/1后/2左转/3右转，速度 5~80；`transform_turn_speed(方向, 速度)` 速度 5~280。
> 💡 其它形态把 `transform_` 换成 `spider_`(蜘蛛) / `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车) / `mecanum_`(麦轮车)。
> ⚠️ 偏移量返回结构（列表 `[偏移,线型,x,y]` 或字典 `{...}`）**以你打印出来的真实结构为准**——这是调试巡线的第一步（见《第8周_实践手册》步骤①）。
> ⚠️ 车身高度方法（`transform_*`）**以你这版 SDK 真实方法名为准**，别照抄示例名。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **双线跟随（火车轨道）**：贴两条平行线，用 `ug.get_double_track_total_info()` 读双线，夹在中间走，比单线更稳。
- **屏幕一直显示偏移量**：在循环里加 `ug.screen_print_text("偏移:" + str(偏移量), "FFFF0000")`，让它"心里有数"。
- **会报警的翻越车**：翻越时闪红灯 + 喊"前方有台阶，翻越中"（灯光/语音函数见第5周）。
- **换形态**：把 `transform_` 换成其它形态前缀，做对应的"巡线 + 避障"（复习第9~13周）。

---

## 🔮 接第15周预告
下一周我们换 **工程车（mechanical_）**：它带 3 轴机械臂，能用"伸出/夹取/抬起/放下"做成函数，再配合颜色识别定位目标，完成一次"识别→夹取→搬运→放下"的完整取放。这周的"函数封装动作 + 传感器判断"正好能拿去给机械臂做"认出目标再夹"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把巡线 + TOF 判断拼到变形车上，并让遇障时抬升车身翻越"，一层层来最稳！车身高度方法名记得先确认再填～
