# 第15周 · 实践手册（动手课）🔧
### 给七年级小朋友的「工程车机械臂抓取 / 取放」第十五课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。机械臂有活动范围，**别让手臂打到人或易碎品**。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：工程车（带 3 轴机械臂，新拼一台）
> 这周换 **工程车**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 工程车结构件包（含机械臂）；②固定主控到底盘、装 3 轴机械臂、接好线；③盯三点（螺丝拧紧 / 机械臂舵机方向装对 / 走线理好别缠轮子）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **工程车拼法要点**：在车体上加 **3 轴机械臂**（底座旋转 + 大臂 + 小臂 + 夹爪）。机械臂舵机多、**走线要理好**别缠轮子，夹爪先调松紧适中（能夹住球又不会捏太死）。这是七种里零件最多的，慢慢拼不急。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
4. 地上放一个**红球/绿积木**当目标，旁边留一块"放置区"。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：这周要用**摄像头认颜色**，所以光线亮一点，目标球放在浅色背景前最容易被认出（见《第7周_实践手册》）。

---

## 📣 先确认"机械臂"的真实方法名（本周关键一步，别跳过！）
> 工程车的**车身移动**用 `ug.mechanical_move(...)` / `ug.mechanical_turn(...)`，这两个是确定的；但**机械臂**的"伸出/夹取/抬起/放下"靠的是 `mechanical_*` 系列方法（夹爪 / 升降 / 旋转），**不同 SDK 版本名字可能不同**（比如夹爪可能叫 claw / grip，升降可能叫 up / lift）。我们的规矩是**不编方法名**，所以你得先确认你这版 SDK 里真实的写法，后面机械臂按钮里才好填。两个办法：
> - **办法 A（最稳）**：在 uPython 编辑器里，连上机器人后，输入 `ug.` 看自动弹出的方法列表，找带 `mechanical_` 且和"夹爪/升降/旋转/臂"相关的那些；
> - **办法 B**：翻官方文档 https://docs.ubtrobot.com/ugot/ 查工程车（engineer）的机械臂方法名。
> 找到后，把下面 `伸出()` / `夹取()` / `抬起()` / `放下()` 里注释的示例行，换成你确认过的真实方法名即可。**没确认前别乱填，填错小车不会动、还可能报错。**

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：工程车 机械臂抓取 / 取放（mechanical_）
```
> 第 15 周就做这一个场景（课程安排里写好的）。它把第3周 函数封装 + 第4/7周 视觉识别，拼到工程车上，做出"认出目标→夹起来→搬走放下"的搬运工。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第七节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🎥 视觉 | `load_models(['color_recognition'])` + `get_color_total_info()` 认目标 |
| ③ | 👀 传感器 | `read_distance_data(1)` 开到目标前停 |
| ④ | 🦾 机械臂 | `伸出()` / `夹取()` / `抬起()` / `放下()` 按钮（mechanical_*） |
| ⑤ | 🔧 组合 | 识别→开过去→夹取→搬运→放下 |
| ⑥ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 认出目标 + 开到目标前（叠第7周 颜色 + 第4周 TOF）✅
先让工程车**认出红球、并开到它前面停下**，确认"眼睛"和"脚"都通：
```python
# 关1：工程车 颜色识别定位目标 + 开到目标前（mechanical_）
from ugot import ugot
import json, time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

目标颜色 = "red"      # 你想抓的目标颜色，比如红球写 "red"
安全距离 = 15        # 离目标多近算"到啦"（厘米）

ug.load_models(['color_recognition'])   # 装"颜色眼睛"
try:
    while True:
        raw = ug.get_color_total_info()
        data = json.loads(raw)
        if data['total'] > 0 and data['info'][0]['color'] == 目标颜色:
            ug.screen_print_text("看到目标", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯=找到啦
            ug.play_audio_tts("我看到目标了", 0)
            距离 = ug.read_distance_data(1)           # 用 TOF 量离目标多远
            if 距离 == -1 or 距离 < 安全距离:
                ug.stop_chassis()                     # 够近了，停！
                ug.screen_print_text("到啦", "FF00FF00")
                break
            else:
                ug.mechanical_move(0, 35)             # 还没到，继续开过去
        else:
            ug.stop_chassis()
            ug.screen_print_text("找目标", "FFFFFFFF")
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：工程车摄像头认出红球 → 红灯亮、喊"我看到目标了" → 开过去，离红球 15cm 内停下报"到啦"。🎉
> 💡 卡住先想：认不出球？光线亮一点、球离镜头 30~60 厘米（见《第7周_实践手册》）。`data['info'][0]['color']` 的名字以你打印出来的真实值为准（常见 `"red"`/`"green"`）。

### 关 2 · 把机械臂动作封装成函数（叠第3周 函数）✅
在关 1 基础上，把机械臂的四个动作做成按钮。**方法名先确认**（见步骤⓪），下面用注释占位：
```python
# 关2：工程车 机械臂动作按钮（mechanical_，方法名需确认）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

# ⚠️ 下面的 mechanical_* 方法名，请替换成你这版 SDK 里确认过的真实方法（见步骤⓪说明）！
def 伸出():
    ug.screen_print_text("伸出", "FFFFFF00")
    # ug.mechanical_arm_forward()     # ← 换成你 SDK 里真实的"伸出"方法
    pass   # 占位：确认真实方法后，把上面注释行取消注释即可

def 夹取():
    ug.screen_print_text("夹取", "FFFF0000")
    # ug.mechanical_claw_close()      # ← 换成真实的"夹爪闭合"方法
    pass

def 抬起():
    ug.screen_print_text("抬起", "FF00FF00")
    # ug.mechanical_arm_up()          # ← 换成真实的"抬起"方法
    pass

def 放下():
    ug.screen_print_text("放下", "FFFFFFFF")
    # ug.mechanical_claw_open()       # ← 换成真实的"夹爪张开"方法
    pass

# 试一下每个按钮（确认手臂会动）
伸出(); time.sleep(1)
夹取(); time.sleep(1)
抬起(); time.sleep(1)
放下(); time.sleep(1)
ug.stop_chassis()
ug.screen_clear()
```
**应当看到**：每按一个按钮，屏幕显示对应文字；等你把 `mechanical_*` 真实方法填进去后，机械臂就会真的伸出 / 夹 / 抬 / 放。🎉
> 💡 卡住先想：现在 `pass` 只是占位，手臂不会真动——这是正常的！先把真实方法名填进注释行（取消注释），手臂才会动。填错会报错，把红字念给小导师听。

### 关 3 · 串成完整取放：识别→夹取→搬运→放下（叠机械臂 → 完成大作品）✅
最后把关 1（识别+开过去）+ 关 2（机械臂按钮）串起来，完成课程安排里的"验收流程"：
```python
# 关3（终极版）：工程车 识别→夹取→搬运→放下 完整取放（mechanical_）
from ugot import ugot
import json, time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)

目标颜色 = "red"
安全距离 = 15

# ===== 机械臂按钮（方法名以你 SDK 为准，见步骤⓪）=====
def 伸出():
    # ug.mechanical_arm_forward()   # ← 换真实方法
    pass
def 夹取():
    # ug.mechanical_claw_close()    # ← 换真实方法
    pass
def 抬起():
    # ug.mechanical_arm_up()        # ← 换真实方法
    pass
def 放下():
    # ug.mechanical_claw_open()     # ← 换真实方法
    pass

try:
    ug.load_models(['color_recognition'])
    # ① 识别目标 + 开到目标前
    while True:
        raw = ug.get_color_total_info()
        data = json.loads(raw)
        if data['total'] > 0 and data['info'][0]['color'] == 目标颜色:
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)
            距离 = ug.read_distance_data(1)
            if 距离 == -1 or 距离 < 安全距离:
                ug.stop_chassis(); break
            ug.mechanical_move(0, 35)
        else:
            ug.stop_chassis()
        time.sleep(0.1)

    # ② 机械臂取放：伸出 → 夹取 → 抬起 → 搬运 → 放下
    ug.screen_print_text("夹取中", "FFFF0000")
    ug.play_audio_tts("开始夹取", 0)
    伸出(); time.sleep(1)
    夹取(); time.sleep(1)
    抬起(); time.sleep(1)
    ug.mechanical_move(0, 35); time.sleep(1.5); ug.stop_chassis()   # 搬运到放置区
    放下(); time.sleep(1)
    ug.screen_print_text("完成!", "FF00FF00")
    ug.show_light_rgb([0,1,2,3], 0, 255, 0)
except:
    ug.stop_chassis()           # 不管发生什么，先停车最安全
    ug.turn_off_lights()
    ug.screen_clear()
```
**应当看到（验收全流程）**：工程车认出红球 → 开到球前停下 → 机械臂**伸出夹住、抬起** → 它把球**搬运**到放置区 → **放下** → 屏幕"完成!"绿灯。一次完整的"识别→夹取→搬运→放下"就做完了！🎉🎉🎉
> 💡 卡住先想：这关是把关1（眼+脚）和关2（手）拼起来——`while` 循环负责"找到并靠近"，循环 `break` 出来后，一连串机械臂按钮负责"夹起搬走放下"。手臂不动？先回关2确认 `mechanical_*` 真实方法名填对了。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关1：把 `目标颜色 = "red"` 改成 `"green"`，换成绿球它才认，红球就不理了——试试！
- 关1：把 `安全距离 = 15` 改成 `8` 或 `25`，它离球更近还是更远才停？
- 关3：把搬运那段 `time.sleep(1.5)` 的 `1.5` 改成 `2.5`，它搬得更远还是更近？（放置区远就调大）
- 关3：把 `目标颜色` 改成你手边有的颜色，现场换个球玩。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把工程车拼好（机械臂装对、走线理好）、连上电脑
- [ ] 关1 跑通：工程车认出目标颜色 + 开到目标前停下
- [ ] 关2 跑通：机械臂"伸出/夹取/抬起/放下"四个按钮都能动（方法名已确认）
- [ ] 关3 跑通：完成一次"识别→夹取→搬运→放下"完整取放
- [ ] 我能给家人演示验收：**用机械臂完成一次完整取放**

---

## 🃏 随身速查卡（工程车取放常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 车身前进 / 转弯 | `ug.mechanical_move(0, 35)` / `ug.mechanical_turn(2, 120)` |
| 加载颜色眼睛 | `ug.load_models(['color_recognition'])` |
| 读颜色结果 | `raw = ug.get_color_total_info()` → `data = json.loads(raw)` |
| 取颜色名 | `data['info'][0]['color']`（如 `"red"`） |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 机械臂动作 | `mechanical_*` 方法（**名字以你这版 SDK 为准**，见步骤⓪） |
| 红灯 / 绿灯 | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` |
| 女声说话 | `ug.play_audio_tts("文字", 0)` |
| 急停 | `ug.stop_chassis()` |
| 一直循环 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 方向数字：前进=0，后退=1，左转=2，右转=3；`mechanical_move(方向, 速度)` 速度 5~80，`mechanical_turn(方向, 速度)` 速度 5~280。
> 💡 其它形态把 `mechanical_` 换成 `transform_`(变形车) / `spider_`(蜘蛛) / `dog_`(四足狗) / `wheelleg_`(轮足) / `balance_`(平衡车)。
> ⚠️ 机械臂 `mechanical_*` 方法（夹爪/升降/旋转）**以你这版 SDK 真实方法名为准**，别照抄示例名——这是调试取放的第一步。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **用 x 让车对准目标**：认出球后，读 `data['info'][0]['x']`，球偏左就左转、偏右就右转，先对准再开过去（更准的"瞄准"）。
- **语音播报每一步**：夹取时喊"夹住了"、放下时喊"放好啦"（声音函数见第5周）。
- **换形态复习**：把 `mechanical_` 换成其它形态前缀，做对应的"识别 + 动作"项目（复习第9~14周）。
- **准备收官**：下一周是**自由综合周**——你自己选形态、自己定主题（手势搬运 / 语音问答车 / 巡逻报警车…），独立做一个能演示的项目！

---

## 🔮 接第16周预告（收官周）
下一周是 **自由综合周**：形态你自选、主题你自选（比如"手势搬运车""巡逻报警车"），要求至少融合 2 个前 8 周知识点，自己定方案、自己搭脚手架、自己跑通。这 15 周攒下的"工具箱"（连接 / 运动 / 函数 / 传感器 / 灯光声音 / 手势 / 颜色 / 巡线 / 各种形态）全都归你调配——去做出一个属于你自己的大作品吧！

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把视觉识别 + 函数封装拼到工程车上，让机械臂完成一次取放"，一层层来最稳！机械臂方法名记得先确认再填～
