# 第8周 · 实践手册（动手课）🔧
### 给七年级小朋友的「巡线跟随」第八课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

> 💡 小提醒：本次要**准备一条黑胶带**（或黑色电工胶带），贴在浅色地板 / 一大张白纸 / 浅色瑜伽垫上，做成一条直的、或带一两个弯的线；线不要太细（3~5 厘米宽好认），场地要够长，别让它一启动就冲出线外。

---

## 🤖 本周形态拼装：变形车（新拼一台）
> 这周换 **变形车**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 变形车结构件包；②固定主控到机身、装升降/变形机构、接好线；③盯三点（螺丝拧紧 / 机构方向装对 / 线别卡住机构）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **变形车拼法要点**：机体高度可调，装上升降 / 变形机构；注意机构舵机的**行程和限位**，别装反卡死。拼好先不通电，手动试试升降是否顺滑，再开机。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
4. 地上贴好一条黑线，机器人放在线起点、摄像头朝地面。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

## 步骤 ① 认识"新眼睛"：加载模型 + 读一次线信息并打印 ✅
先不急着开车，我们让机器人**把黑线信息打出来看一眼**，确认眼睛是好的、也看清偏移量长啥样：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])        # 装上"认黑线"模型（先装，后面才能读）
ug.screen_print_text("巡线就绪", "FFFFFF00")

信息 = ug.get_single_track_total_info()      # 读一次黑线信息
print(信息)                                  # 先打印出来，看看里面长啥样
ug.stop_chassis()
```
**应当看到**：编辑器下方打印出一大串线信息（比如带 `offset` / `position` / `angle` 之类字段的一串内容）。✅
> 💡 卡住先想：这里面**哪个数代表"线偏左/右多少"**？把它记下来（常见叫 `offset` 或 `position` 或 `angle`，不同版本不一样）。下面每一步我们都要用这个数。
> ⚠️ 如果 `load_models(['line_recognition'])` 报错说找不到模型：去编辑器里看能加载的模型列表，或翻官方文档 `docs.ubtrobot.com/ugot/` 确认模型名，把引号里的换掉再试。

## 步骤 ② 用 if 做"判断"：偏右往右修、偏左往左修（本周核心概念！）✅
现在让机器人**自己决定往哪修**：偏移 > 阈值就往右修，< -阈值就往左修，很小就直走。先用"判断一次"的版本（把 `信息['偏移']` 换成你步骤①看到的真实字段名）：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])

阈值 = 10        # 偏移小于这个数，就算"基本直走"
系数 = 3         # P：偏越多，修正越多
前进速度 = 35

信息 = ug.get_single_track_total_info()
偏移量 = 信息['偏移']   # ← 换成步骤①打印出来看到的真实字段名！

if 偏移量 > 阈值:
    ug.transform_turn_speed(3, min(120, 偏移量 * 系数))   # 线偏右 → 往右修（3=右转）
elif 偏移量 < -阈值:
    ug.transform_turn_speed(2, min(120, -偏移量 * 系数))  # 线偏左 → 往左修（2=左转）
else:
    ug.transform_move_speed(0, 前进速度)                  # 基本直走
```
**应当看到**：把机器人放到黑线起点，它会根据线偏哪边做一次修正——偏右就往右扭一下，偏左就往左扭。✅
> 💡 这里只判断了"一次"。要让它一直跟着线，得加上循环，下一步就来。

## 步骤 ③ 加 while 循环，让它一直跟着线走（本周最核心！）✅
把"看一眼"变成"一直看"，用 `while True:` 套住判断。完整可跑版（记得把 `信息['偏移']` 换成步骤①看到的真实字段名）：
```python
from ugot import ugot
import time                  # 让小车"歇一下"要用到
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])
ug.screen_print_text("开始巡线", "FFFFFF00")

阈值 = 10
系数 = 3
前进速度 = 35

while True:
    信息 = ug.get_single_track_total_info()
    偏移量 = 信息['偏移']   # ← 换成你步骤①看到的真实字段名！

    if 偏移量 > 阈值:
        ug.transform_turn_speed(3, min(120, 偏移量 * 系数))   # 偏右→往右修
        time.sleep(0.1)
        ug.stop_chassis()                                  # 修一下就停转，避免一直转圈
    elif 偏移量 < -阈值:
        ug.transform_turn_speed(2, min(120, -偏移量 * 系数))  # 偏左→往左修
        time.sleep(0.1)
        ug.stop_chassis()
    else:
        ug.transform_move_speed(0, 前进速度)                 # 直走
    time.sleep(0.05)
```
**应当看到**：机器人沿着黑线自己走，线往哪歪它就往哪修，稳定跟一段不掉线！🎉
> 💡 重点：`while True` 会一直跑。想让它停下，关掉编辑器运行、或按急停。下一步我们给它加"读不到线"的处理 + 安全网。
> 💡 卡住先想：如果它一直转圈不停，是不是漏了 `stop_chassis()`？如果它走反方向（线在右它往左），说明你的偏移量正负和我们约定反了——把 `3` 和 `2` 对调就行。

## 步骤 ④ 加固：处理"读不到线" + 加急停安全网（稳健版）✅
和避障车一样，线信息也可能读不到（比如走到线外、摄像头被挡）。读不到时最安全是"停下或慢慢直走等一下"，别乱冲。再加 `try/except` 急停：
```python
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])

阈值 = 10
系数 = 3
前进速度 = 35
读不到就停 = True   # 读不到线时：True=停下等待，False=继续直走（二选一）

try:
    while True:
        信息 = ug.get_single_track_total_info()
        if 信息 is None or '偏移' not in 信息:        # ← 字段名按步骤①改；读不到就保守处理
            if 读不到就停:
                ug.stop_chassis()
            else:
                ug.transform_move_speed(0, 前进速度)
            time.sleep(0.1)
            continue

        偏移量 = 信息['偏移']   # ← 换成真实字段名

        if 偏移量 > 阈值:
            ug.transform_turn_speed(3, min(120, 偏移量 * 系数))
            time.sleep(0.1); ug.stop_chassis()
        elif 偏移量 < -阈值:
            ug.transform_turn_speed(2, min(120, -偏移量 * 系数))
            time.sleep(0.1); ug.stop_chassis()
        else:
            ug.transform_move_speed(0, 前进速度)
        time.sleep(0.05)
except:
    ug.stop_chassis()       # 不管发生什么，先停车最安全
    print("急停！")
```
**应当看到**：和步骤③一样能跟线；即使走到线外短暂读不到，它也会停下或直走等一下，不乱冲。这就是"稳"的巡线车！✅
> 💡 卡住先想：`if 信息 is None or '偏移' not in 信息:` 为什么要单独判断？——因为读不到线是最危险的，要**优先处理**，不能当成"偏移=0 直走"冲出去。

## 步骤 ⑤ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `阈值 = 10` 改成 `5`，机器人对更小的偏移也修正，走得更"贴线"还是更容易左右抖？
- 把 `系数 = 3` 改成 `6`，修正更猛还是更温柔？太大是不是会左右晃？
- 把 `前进速度 = 35` 改成 `50`，走更快但还跟得住吗？（场地要更大！）
- 把 `transform_turn_speed` 的 `3` / `2` 对调，看它是不是走反了——这就验证你偏移量的正负方向。
- 想更顺滑？试试把"修一下就停"换成 `ug.transform_move_speed(3, 修正速度)` / `ug.transform_move_speed(2, 修正速度)` 让它"边走边拐"（弧线跟随），对比哪种更稳。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 加载巡线模型成功，能打印出黑线信息、找到偏移量字段
- [ ] 机器人沿黑线自己走，偏右往右修、偏左往左修，稳定跟一段不掉线
- [ ] 走到线外读不到时，保守停下或直走，不乱冲
- [ ] 你能自己调 `阈值` / `系数` / `前进速度`，观察小车反应变化
- [ ] 你能给家人演示："看，它自己盯着黑线走，不用我遥控！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 加载巡线模型 | `ug.load_models(['line_recognition'])` |
| 读单线信息 | `ug.get_single_track_total_info()` |
| 读双线信息 | `ug.get_double_track_total_info()` |
| 偏右往右修 | `ug.transform_turn_speed(3, 速度)` |
| 偏左往左修 | `ug.transform_turn_speed(2, 速度)` |
| 直走 | `ug.transform_move_speed(0, 35)` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| P 修正封顶 | `min(120, abs(偏移量) * 系数)` |
| 一直循环跟线 | `while True:` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `transform_move_speed(方向, 速度)`：方向 0前/1后/2左转/3右转，速度 5~80；`transform_turn_speed(方向, 速度)` 速度 5~280。
> 其它形态把 `transform_` 换成 `balance_`(平衡车) / `wheelleg_`(轮足) / `dog_`(四足狗) / `spider_`(蜘蛛) / `mechanical_`(工程车)。
> ⚠️ 偏移量字段名（offset / position / angle…）**以你步骤①打印出来的真实结构为准**——这是调试的第一步。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **双线跟随（火车轨道）**：贴两条平行线，用 `ug.get_double_track_total_info()` 读双线，夹在中间走，比单线更稳。
- **屏幕一直显示偏移量**：在循环里加 `ug.screen_print_text("偏移:" + str(偏移量), "FFFF0000")`，让它"心里有数"。
- **会报警的巡线车**：读到线就绿字"跟住啦"，读不到就闪红灯 + 喊"看不到线"（灯光/语音函数见第5周）。
- **弯道挑战**：把黑胶带弯成 S 形，调小 `阈值`、调大 `系数`，看它能不能顺弯不脱线。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
