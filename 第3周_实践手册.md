# 第3周 · 实践手册（动手课）🔧
### 给七年级小朋友的「函数与运动」第三课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：平衡车（新拼一台）
> 这周换 **平衡车**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 平衡车结构件包；②固定主控到直立机身、装两个大轮、接好线；③盯三点（螺丝拧紧 / 大轮一左一右方向装对 / 线别卡住轮子）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **平衡车拼法要点**：两个大轮一左一右装到直立机身，主控**竖装、重心居中**；陀螺仪方向朝上，否则站不稳。装完先别通电，用手扶一下试平衡，确认不往一边倒再开机。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要上周的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

## 步骤 ① 先造第一个"按钮"：`走` ✅
把下面这段粘进编辑器（**IP 换成你的**），点 Run，确认它能前进：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

前进 = 0

def 走(秒数, 速度=40):                       # 造一个"前进按钮"
    ug.balance_move_speed_times(前进, 速度, 秒数, 0)

走(2)                  # 按下按钮：前进 2 秒（速度默认 40）
ug.stop_chassis()      # 停！安全第一
```
**应当看到**：机器人往前走 2 秒然后停下。✅
> 💡 卡住先想：`def 走(秒数, 速度=40):` 下面**缩进的那行**才是按钮要做的事；`走(2)` 才是"按下去"。冒号和缩进都不能少。

## 步骤 ② 把四个方向都做成"按钮"（本周核心！）✅
照着 `走` 的样子，把"后退、左转、右转"也补成按钮。完整可跑版：
```python
from ugot import ugot
import time                  # 转弯"等一会儿"要用到
ug = ugot.UGOT()
ug.initialize("192.168.x.x") # ← 换成机器人真实 IP

# 方向常量（给数字起好记的名字）
前进 = 0
后退 = 1
左转 = 2
右转 = 3

def 走(秒数, 速度=40):
    ug.balance_move_speed_times(前进, 速度, 秒数, 0)

def 倒(秒数, 速度=40):
    ug.balance_move_speed_times(后退, 速度, 秒数, 0)

def 转(方向, 秒数, 速度=120):
    ug.balance_turn_speed(方向, 速度)   # 开始转
    time.sleep(秒数)                    # 转"秒数"秒
    ug.stop_chassis()                  # 停！（转弯必须喊停，否则会一直转圈）

走(1); 倒(1); 转(左转, 1); 转(右转, 1)   # 四个按钮各按一下试试
ug.stop_chassis()
```
**应当看到/听到**：机器人前进 1 秒 → 后退 1 秒 → 左转 1 秒 → 右转 1 秒，全程不卡壳。🎉
> 💡 重点：`转` 这个函数里最后有 `ug.stop_chassis()`。因为 `balance_turn_speed` 是"一直转"，不喊停它就转圈圈停不下来——这其实也是咱们的急停按钮。

## 步骤 ③ 拼一条路线：前进→左转→前进 ✅
现在四个按钮都好了，把路线像搭积木一样写出来：
```python
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x") # ← 换成机器人真实 IP

前进 = 0
后退 = 1
左转 = 2
右转 = 3

def 走(秒数, 速度=40):
    ug.balance_move_speed_times(前进, 速度, 秒数, 0)

def 倒(秒数, 速度=40):
    ug.balance_move_speed_times(后退, 速度, 秒数, 0)

def 转(方向, 秒数, 速度=120):
    ug.balance_turn_speed(方向, 速度)
    time.sleep(秒数)
    ug.stop_chassis()

# ===== 拼路线：前进 → 左转 → 前进 =====
走(2)         # ① 前进 2 秒
转(左转, 1)   # ② 左转 1 秒
走(2)         # ③ 再前进 2 秒
ug.stop_chassis()
```
**应当看到**：机器人先直走 2 秒 → 原地左转 1 秒 → 再直走 2 秒，走完稳稳停下。这一刻，你就像在指挥它走一条小路！✅

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `走(2)` 改成 `走(3)`，机器人多走多久？
- 把 `转(左转, 1)` 改成 `转(右转, 1.5)`，路线变成往哪边拐、转多久？
- 把 `走(2)` 的 `速度=40` 改成 `走(2, 70)`，冲得更快还是更慢？（场地要够大！）
- 在最后加一行 `倒(1)`，让路线变成"前进→左转→前进→后退"。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 四个"按钮"（走/倒/转）都跑通，机器人能前进、后退、左右转
- [ ] 拼出的"前进→左转→前进"路线走完不卡壳、不一直转圈
- [ ] 你能自己改路线：比如"前进→右转→前进→后退"
- [ ] 你能给家人演示："看，我造了几个按钮，拼一下它就走完全程！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 方向常量 | `前进=0 后退=1 左转=2 右转=3` |
| 造"前进"按钮 | `def 走(秒数, 速度=40):` |
| 前进 N 秒 | `ug.balance_move_speed_times(0, 40, 2, 0)` |
| 左转/右转（持续） | `ug.balance_turn_speed(2, 120)` |
| 等一会儿 | `time.sleep(1)` |
| 拼路线 | `走(2); 转(左转, 1); 走(2)` |
| 急停 | `ug.stop_chassis()` |

> 💡 `balance_move_speed_times(方向, 速度, 数值, 单位)`：单位 **0=秒、1=厘米、2=度**。
> 其它形态把 `balance_` 换成 `wheelleg_`(轮足) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车) / `mechanical_`(工程车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- 给路线加"语音播报"：每走一步用 `ug.play_audio_tts("前进", 0)` 喊出来（声音函数见《第2周_实践手册》速查卡）。
- 做一个"方形巡逻"：前进→右转→前进→右转→前进→右转→前进→右转，看机器人能不能走回原点。
- 把 `balance_` 换成其它形态的前缀，换种机器人来走这条路线。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
