# 第6周 · 实践手册（动手课）🔧
### 给七年级小朋友的「真AI手势遥控」第六课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

> 💡 小提醒：本周靠**摄像头认手势**，记得把机器人正面对着你、光线亮一点识别更准；手离镜头约 30~60 厘米最舒服。玩之前清好一块空地，机器人一前进就往后退，别撞桌角。

---

## 🤖 本周形态拼装：轮足（新拼一台）
> 这周换 **轮足**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 轮足结构件包；②固定主控到机身、装两条腿+轮子、接好线；③盯三点（螺丝拧紧 / 腿对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **轮足拼法要点**：双足 + 轮子混合，两条"腿"装到机身两侧、能屈伸；舵机角度**先归中**再装，避免一开机乱蹬。装完先不通电，用手轻轻掰一下腿，确认能屈伸顺畅。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

## 步骤 ① 先唤醒"认手势小专家"，读一次手势 ✅
先不急着让它动，我们让机器人**报出它认出的手势**，确认 AI 眼睛是好的：
```python
import json
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['gesture'])   # 唤醒"认手势"专家（很重要，不唤醒它啥也认不出）

raw = ug.VISION.gesture_inference()   # 让它看一眼你的手，返回一张"纸条"
data = json.loads(raw)                # 把纸条拆成字典
手势 = data.get('gesture')            # 取出手势名字

ug.screen_print_text(str(手势), "FFFF0000")  # 把认出的手势显示在屏幕（红字）
ug.play_audio_tts(str(手势), 0)               # 顺便念出来（女声）
ug.stop_chassis()
```
**应当看到/听到**：你在镜头前比个"手掌"，屏幕显示 `palm`、女声念"palm"。换"点赞"试试，应该变成 `good`。✅
> 💡 卡住先想：如果屏幕显示 `None` 或念不出字，说明它没认清——手离镜头近一点、光线亮一点、手势比标准一点再试。如果一直不认，检查 `load_models(['gesture'])` 那行有没有写、IP 连对没。

## 步骤 ② 用 `==` 判断一个手势：手掌→前进（本周核心概念！）✅
现在让机器人**按手势行动**：认出"手掌"就前进，别的先不管。先试用"判断一次"的版本：
```python
import json
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['gesture'])

raw = ug.VISION.gesture_inference()
data = json.loads(raw)
手势 = data.get('gesture')

if 手势 == 'palm':                 # 认出"手掌"就前进
    ug.wheelleg_move_speed(0, 40)   # 0=前进，速度40
    ug.screen_print_text("前进", "FF00FF00")   # 绿字提示
else:
    ug.stop_chassis()              # 不是手掌就先停着
```
**应当看到**：你比"手掌"→机器人往前走、屏幕绿字"前进"；你放下手或比别的→它就停。✅
> 💡 这里只判断了"一次"。要让它一直盯着你的手看，得加上循环，下一步就来。

## 步骤 ③ 加 while 循环，做成"手势遥控器"（本周最核心！）✅
把"看一眼"变成"一直看"，用 `while True:` 套住判断；再把 5 种手势都接上动作。完整可跑版：
```python
import json
import time
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['gesture'])    # 唤醒"认手势"专家

while True:
    raw = ug.VISION.gesture_inference()   # 一直盯着你的手看
    data = json.loads(raw)
    手势 = data.get('gesture')

    if 手势 == 'palm':                 # 手掌 → 前进
        ug.wheelleg_move_speed(0, 40)
        ug.screen_print_text("前进", "FF00FF00")
    elif 手势 == 'good':               # 点赞 → 停
        ug.stop_chassis()
        ug.screen_print_text("停", "FFFF0000")
    elif 手势 == 'scissor':            # 剪刀 → 左转
        ug.wheelleg_turn_speed(2, 120)
        ug.screen_print_text("左转", "FFFFFF00")
    elif 手势 == 'ok':                 # OK → 右转
        ug.wheelleg_turn_speed(3, 120)
        ug.screen_print_text("右转", "FF0000FF")
    elif 手势 == 'stone':              # 石头 → 后退
        ug.wheelleg_move_speed(1, 40)
        ug.screen_print_text("后退", "FFFF00FF")
    else:
        ug.stop_chassis()              # 认不出或没手 → 先停，最安全
    time.sleep(0.1)                   # 歇 0.1 秒，别让小专家太累
```
**应当看到**：你比"手掌"它前进，比"点赞"它停，比"剪刀"左转、"OK"右转、"石头"后退——像个真遥控器！🎉
> 💡 重点：转弯用的是 `wheelleg_turn_speed`（一直转），所以**比完手势记得比"点赞"让它停**，否则它会一直转圈。想让它立刻停，关掉编辑器运行、或直接比"点赞"（good）。

## 步骤 ④ 加固：处理"认不出" + 加急停安全网（稳健版）✅
前面有个小漏洞：万一某一刻 `手势` 是 `None`（没认出），`else` 会把它当"停"，其实也安全；但更稳的写法是**先确认真的认出了再判断**，并套上 `try/except` 急停：
```python
import json
import time
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['gesture'])

try:
    while True:
        raw = ug.VISION.gesture_inference()
        data = json.loads(raw)
        手势 = data.get('gesture')

        if 手势 == 'palm':
            ug.wheelleg_move_speed(0, 40)
            ug.screen_print_text("前进", "FF00FF00")
        elif 手势 == 'good':
            ug.stop_chassis()
            ug.screen_print_text("停", "FFFF0000")
        elif 手势 == 'scissor':
            ug.wheelleg_turn_speed(2, 120)
            ug.screen_print_text("左转", "FFFFFF00")
        elif 手势 == 'ok':
            ug.wheelleg_turn_speed(3, 120)
            ug.screen_print_text("右转", "FF0000FF")
        elif 手势 == 'stone':
            ug.wheelleg_move_speed(1, 40)
            ug.screen_print_text("后退", "FFFF00FF")
        else:
            ug.stop_chassis()          # 没认出就停（含 手势 为 None 的情况）
        time.sleep(0.1)
except:
    ug.stop_chassis()       # 不管发生什么，先停车最安全
    print("急停！")
```
**应当看到**：和步骤③一样能听手势指挥；即使偶尔没认出，它也乖乖停下不乱冲。这就是"稳"的手势遥控器！✅
> 💡 卡住先想：`else:` 放在最后，是不是把"认不出"也兜底成"停"了？—— 对，认不出就停，比乱动安全。

## 步骤 ⑤ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把"手掌→前进"和"点赞→停"对调：让比"点赞"反而前进，行不行？（提示：把 `if` 里的 `'palm'` 和 `'good'` 互换）
- 把前进速度 `40` 改成 `70`，机器人冲得更快了吗？（场地要够大！）
- 把 `scissor`(剪刀) 的动作从"左转"改成"后退"，让两个手势都后退，试试看。
- 把 `time.sleep(0.1)` 改成 `time.sleep(0.3)`，观察它对手势反应变快还是变慢？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 比"手掌"机器人前进，比"点赞"它停下
- [ ] 5 种手势（石头/手掌/OK/赞/剪刀）各触发对应动作
- [ ] "认不出"时它乖乖停下，不乱冲
- [ ] 你能自己改映射：比如把某个手势换成别的动作
- [ ] 你能给家人演示："看，我比个手势，它就听话！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 唤醒手势专家 | `ug.load_models(['gesture'])` |
| 读一次手势(返回纸条) | `ug.VISION.gesture_inference()` |
| 拆纸条成字典 | `data = json.loads(raw)` |
| 取出手势名字 | `手势 = data.get('gesture')` |
| 判断手掌→前进 | `if 手势 == 'palm': ug.wheelleg_move_speed(0, 40)` |
| 判断点赞→停 | `elif 手势 == 'good': ug.stop_chassis()` |
| 一直看手势 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 急停 | `ug.stop_chassis()` |

> 💡 手势暗号（AI 认出来的字）：`stone`石头 / `palm`手掌 / `ok` / `good`点赞 / `scissor`剪刀。
> 方向数字：前进=0，后退=1，左转=2，右转=3；`wheelleg_move_speed(方向, 速度)` 速度 5~80，`wheelleg_turn_speed(方向, 速度)` 速度 5~280。
> 其它形态把 `wheelleg_` 换成 `balance_`(平衡车) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车) / `mechanical_`(工程车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **加语音播报**：每认出一个手势用 `ug.play_audio_tts("前进", 0)` 喊出来（声音函数见《第2周_实践手册》速查卡）。
- **换种机器人形态**：把 `wheelleg_` 换成其它形态的前缀，让四足狗/平衡车也听手势指挥。
- **接第7周预告**：把模型从 `['gesture']` 改成 `['color_recognition']`（颜色识别），认出红球就让机器人前进、绿球就停——做成"看颜色行动"的小车。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
