# 第11周 · 教案（理论课）🤖
### 给七年级小朋友的「轮足高低地形适应 + AI 跟随」第十一周课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先想清楚"轮足怎么在高低地上不摔倒、又怎么追着一个目标走"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：轮足（运动前缀 `wheelleg_`）**
> 这周我们换一台**轮足机器人**——它像"带轮子的两条腿"，腿能屈伸、轮子能滚，专门擅长走**不平的地面**。代码里它的专属前缀是 `wheelleg_`：所有 `wheelleg_move_speed` / `wheelleg_turn_speed` 就是"轮足怎么动"。本周你把第4周"读传感器判断"的眼睛、第6/7周"AI 视觉认东西"的脑子，都装在它身上，让它一边过起伏地不摔、一边追着你指定的目标走。想换别的形态？把前缀一换就行，Python 写法不变。

前几周你做过：第4周"看路自己停"、第6周"比手势它就动"、第7周"认颜色/认人脸"。这周我们要用这些老积木，给**一台会爬坡的轮足**做出两个新本事：①在高低地上走不摔；②认一个目标、追着它走。

**✅ 这周结束，你要能：**
1. **说清楚轮足是啥**：知道它是"双足 + 轮子"混合，腿能屈伸，专门过**高低起伏**的地面，机身靠陀螺仪保持不摔。
2. **认下第 11 周的大作品**：**「轮足高低地形适应 + AI 跟随」**——在起伏场地走不摔，并追着"一个手势"或"一个颜色块"移动。
3. **读懂"姿态"**：知道机器人用 `read_gyro_data()`（一串数字）和 `get_attitude()`（7 个"是/否"）感知自己歪没歪、晃没晃。
4. **会"三关叠加"**：先让轮足走起来 + 读出姿态（关1）→ 加"摇晃保护"过起伏不摔（关2）→ 加"AI 跟随"追目标（关3）。
5. **懂"AI 跟随"怎么实现**：认出目标颜色块后，读它的**左右位置 x**，偏左就左转、偏右就右转、中间就前进——像用眼睛追着球跑。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人要么"看路停"（第4周）、要么"听手势动"（第6周）、要么"认颜色"（第7周），各玩各的；这周起，你**把它们串成一台"会爬坡、还会追目标"的轮足**。这是从"单招"到"让机器人在复杂地形里自己看着目标走"的跨越！记住这种"它摇摇晃晃爬过坡、还盯着球追"的惊喜感 😎

**📌 一句话目标：** 把"姿态感知 + AI 视觉"拼到轮足上，跑通"过起伏不摔 + 追着指定目标走"的完整流程。

---

## 一、这周我们要干啥？🚀
前几周你攒下的本事，咱们数一数：
- 用传感器判断、近了就停（第4周）
- 比手势让机器人动（第6周）
- 认颜色 / 认人脸（第7周）

这三块就像三块**乐高积木**。这周我们给它们换个新底盘——**轮足**，让它在"不平的地上"也能干活，还能追着目标跑。

第 11 周咱们不贪多，就定**一个场景项目**👇

## 二、第 11 周的大作品长啥样？🎬
> **项目名：轮足高低地形适应 + AI 跟随**

场景是这样的（找一块有点小坡/小坎的地面，再拿一个红球当目标）：
1. 轮足**自己靠腿和陀螺仪保持平衡**，在高低起伏的地上慢慢走，不摔倒；
2. 它身上有"姿态传感器"一直在感知：自己有没有**晃**（快倒了）？一晃我们就帮它减速/停 + 闪红灯报警，护住它；
3. 你让它**追一个目标**：认出红球（或比个"手掌"手势）→ 它用摄像头盯着那个球，球在左边它就左转、在右边就右转、在中间就前进，**一路追着球跑**。

**🎯 本周验收标准（给爸爸妈妈演示这个流程就算通关）：**
> 轮足过起伏地面不摔（摇晃会自动保护）+ 能追着指定目标（颜色块/手势）移动

**🧩 融合的前序知识点（两块合一）：**
- 第4周 传感器/姿态判断（读 `read_gyro_data()` / `get_attitude()` 感知起伏）
- 第6/7周 AI 视觉（手势 `gesture` 或颜色 `color_recognition` 当跟随目标）

> 💡 小比喻：这就像你给轮足请了个"体操教练 + 小跟班"——教练（陀螺仪）盯着它别摔倒，小跟班（摄像头）盯着球、带着它一路追。全程不用你推。

## 三、回顾：你攒下的"积木"速览
动手前，先把要用到的本事收进一张表，这就是本周的"材料库"：

| 积木（本事） | 一句话能干啥 | 来自 |
|---|---|---|
| 🔌 连接 | 三行代码让电脑抓住机器人（填 IP） | 第1周 |
| 🤸 姿态感知 | `get_attitude()` 读出"前倾/后倾/左倾/右倾/屏朝上/屏朝下/摇晃" | 第4周(延伸) |
| 🧭 陀螺仪 | `read_gyro_data()` 读出一串倾斜数字 | 第4周(延伸) |
| 🚗 运动 | `wheelleg_move_speed` / `wheelleg_turn_speed` 前进/转弯 | 第6周 |
| ✋ 手势 | 比"手掌/点赞…"让它动（第6周） | 第6周 |
| 🔴 颜色 | 认出红/绿球，还能读出它在画面里的位置 x | 第7周 |
| 🛑 安全 | `try/except` + `stop_chassis()` 急停 | 第4周 |

> 💡 你看，本周用到的每一块，都是你已经学过的！这周的新意在于：① 换上**轮足**底盘（会爬坡）；② 把"姿态感知"第一次真正用来**护它不摔**；③ 把"认颜色"升级成**追着目标走**。

## 四、新朋友：轮足是什么？
前面几周你拼过麦轮车、蜘蛛、四足狗、平衡车……这周换 **轮足（Wheeled&Legged）**：

- 它是**双足 + 轮子**混合：两条"腿"能屈伸，底下还有轮子能滚。
- 腿能屈伸，所以地面**有高有低**时，它能把腿伸长缩短来适应，机身保持平稳——叫"高低地形适应"。
- 它身上有**陀螺仪**（第1周产品介绍里提过），能**自己感知歪没歪、自动调整**保持不摔。
- 我们的代码不用管它"怎么站稳"，只要像其它形态一样下命令：
  - `ug.wheelleg_move_speed(0, 40)` —— 前进（方向 0前/1后，速度 5~80）
  - `ug.wheelleg_turn_speed(2, 120)` —— 转弯（方向 2左/3右，速度 5~280）

> 💡 小提醒：轮足最擅长"不平的地面"，所以本周特意找有小坡、小坎的场地玩（别太陡！）。拼装时两腿要对称、舵机角度先归中，避免一开机乱蹬。

形态对应代码复习：把运动方法里的 `mecanum_` 换成 `wheelleg_`(轮足) / `balance_`(平衡车) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车)。本周咱们就认 `wheelleg_` 这一个。

## 五、认识"姿态"：陀螺仪 + get_attitude（本周新概念！）
第4周我们读过 TOF "眼睛"（距离）。这周认识另一个传感器：**陀螺仪**——它告诉机器人"我现在歪成啥样、晃不晃"。有两个读法：

**① 读一串数字（精细）：`read_gyro_data()`**
```python
陀螺 = ug.read_gyro_data()
# 返回一串数字：[俯仰, 横滚, 偏航, 三轴角速度, 三轴加速度]
# 简单说：数字越大，表示歪/转得越厉害
```
**② 读 7 个"是/否"（好懂）：`get_attitude()`**
```python
姿态 = ug.get_attitude()
# 返回 7 个布尔值(对/错)：前倾, 后倾, 左倾, 右倾, 屏朝上, 屏朝下, 摇晃
# 姿态[6] 就是第 7 个"摇晃"：True=正在晃，快保不住平衡了
```
> 💡 小比喻：`read_gyro_data()` 像医生量体温给的具体数字；`get_attitude()` 像医生直接说"摇不摇晃——摇晃！"。我们这周主要用 `get_attitude()` 的"摇晃"那一项来**保护轮足**：它一晃，我们就帮它停一下，别真摔了。

## 六、"AI 跟随"怎么实现？（本周最酷的一块）
"追着目标走"听起来很高深，其实拆开很简单——靠**摄像头认出目标 + 看它在画面里偏左还是偏右**：

1. 加载颜色模型 `ug.load_models(['color_recognition'])`，认出你的红球；
2. 读结果里目标的 **x**（它在画面里的左右位置）：
   - x 小 → 目标在**左边** → 让它**左转**对准；
   - x 大 → 目标在**右边** → 让它**右转**对准；
   - x 在中间 → 目标正前方 → **前进**追上去！
3. 一直循环，它就"眼睛盯着球、一路追着跑"。

```python
raw = ug.get_color_total_info()
data = json.loads(raw)
if data['total'] > 0:
    x = data['info'][0]['x']        # 目标的左右位置
    if x < 120:                      # 偏左 → 左转
        ug.wheelleg_turn_speed(2, 100)
    elif x > 200:                    # 偏右 → 右转
        ug.wheelleg_turn_speed(3, 100)
    else:                            # 中间 → 前进追
        ug.wheelleg_move_speed(0, 35)
```
> 💡 小比喻：就像你盯着前面跑的球——球往左你往左拐，球往右你往右拐，球正前方你就直冲。机器人也是这个理，只是它用 x 这个数字判断"球在左还是右"。

## 七、"最小可跑版（MVP）"——先动起来，再装修 🏗️
正确姿势还是 **MVP（最小可跑版）**，本周用**"三关叠加法"**：
- **关 1**：先让轮足**走起来 + 读出姿态**，认识"歪/晃"长啥样；
- **关 2**：在关 1 上加**摇晃保护**——一晃就减速/停 + 红灯报警（过起伏不摔）；
- **关 3**：在关 2 上加**AI 跟随**——认目标颜色、追着它走（完成大作品）。

> 💡 小比喻：就像先拼一辆"会走的轮足"，确认它站稳了，再加"摇晃就保护"，最后加"眼睛追球"。一层层加，每层都能跑，就不会卡死。

## 八、三行"万能开头"还记得吗？（连接机器人）
无论做哪个项目，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。每个项目都直接复用，不重复讲啦。

> 小提醒：本周循环里要"歇一下"别让小车太累，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 九、这周学会后，你应该能回答这几个问题
- 轮足跟平衡车比，最不一样的地方是啥？（提示：双足+轮子，腿能屈伸，擅长高低地）
- `get_attitude()` 返回的几个"是/否"里，哪个能告诉我们"快倒了"？（提示：第7个"摇晃"）
- 第 11 周的大作品是哪两块老积木拼起来的？（提示：第4周 姿态感知 + 第6/7周 AI 视觉）
- "AI 跟随"靠目标的哪个数字判断左右？（提示：x，画面里的左右位置）
- 验收标准是哪两件事？（提示：过起伏不摔 + 追着目标走）

> 答得上来，方向就定了！翻《实践手册》去拼轮足、三关跑通"过坡 + 追球" 💪
