# 第16周 · 实践手册（动手课）🔧
### 给七年级小朋友的「自由综合周 · 收官大作品」第十六课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> ⚠️ **这周不一样**：小导师只当"场外教练"——卡住先自己想、再问，我给线索，实在卡死才给一小段提示，**不主动给整段答案**。作品是你的，思路也是你的！

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。机械臂/高车身形态注意活动范围，别打到人或易碎品。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态：你自己选！
> 这周**你定**拼哪台。一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 你选的形态结构件包；②固定主控、装对应轮子/腿/臂、接好线；③盯三点（螺丝拧紧 / 方向装对 / 线别卡住）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
>
> **前缀对照（写代码前先确认你选的那台）**：
> 麦轮车 `mecanum_` / 平衡车 `balance_` / 轮足 `wheelleg_` / 四足狗 `dog_` / 蜘蛛 `spider_` / 变形车 `transform_` / 工程车 `mechanical_`

## 步骤 ⓪ 确认环境已就绪 ✅
> 这周不用再装软件啦！只要"地基"还在：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

---

## 步骤 ① 填一份"我的项目计划书" ✅
照《教案》第六节，先把下面这张表填好（直接写在编辑器最上面当注释也行）。**这是你这周最重要的"施工图"**，填完再写代码：

```
【我的项目计划书】
① 我选的形态：________   运动前缀：________
② 我的主题：________________________________
③ 我至少用的 2 样本领：________、________（从工具箱挑）
④ 动作清单（一步步）：
   1) 连接机器人
   2) ________________________________
   3) ________________________________
   4) ________________________________
   5) ________________________________
⑤ 三关叠加顺序：
   关1（核心）：________________________
   关2（加本领）：______________________
   关3（能演示）：______________________
⑥ 我的验收标准（演示做到哪几步算我通关）：
   ________________________________________
```

> 💡 卡住先想：你的"动作清单"里，哪一步是**核心**（关1 先跑通）？哪一步是**第二样本领**（关2 加）？别一次全堆上。

---

## 步骤 ② 三关叠加，跑通你的最小版 ✅
下面三个"脚手架"是**骨架**（连接 + 常用积木都备好），**中间的"你的动作"要你自己填**。每关 IP 都换成你的、前缀换成你选的形态。

### 关 1 · 核心动作（先让它跑起来）
只做你计划书里"关1"那一步，比如"比手势→前进"、"巡线走"、"测距停"：
```python
# 关1：我的核心动作（________ 形态）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成你的 IP
ug.set_volume(3)

# 👉 这里写你"关1"的核心动作（先只做一件能跑的事）
#    例：ug.mecanum_move_speed(0, 40)   # 前进（前缀换成你选的形态）
try:
    while True:
        # 👉 你的核心循环逻辑写这里
        time.sleep(0.1)
except:
    ug.stop_chassis()          # 不管出啥意外，先停车最安全
    ug.turn_off_lights()
```
**应当看到**：你计划书"关1"那一步真的跑起来了。🎉
> 💡 卡住先想：是"连不上"还是"连上了但不动"？连不上回步骤⓪；不动多半是**前缀写错**（麦轮代码直接抄来没改 `mecanum_`）。这是综合周最常踩的坑！

### 关 2 · 加第二样本领
把计划书"关2"加上（比如再加灯光提示 / 语音 / 函数按钮 / 避障判断）：
```python
# 关2：加第二样本领（________ + ________）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成你的 IP
ug.set_volume(3)

# 👉 需要的话，把动作做成"按钮"（第3周 函数）
def 我的动作():
    # ug.mecanum_move_speed(0, 40)   # 前缀换成你的
    pass

try:
    while True:
        # 👉 关1 逻辑 + 关2 新本领，写这里
        #    例：测距 / 亮灯 / 喊话 / 读手势
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：两样本领合在一起，能连续跑。🎉
> 💡 卡住先想：两个本领"打架"了？比如一边前进一边又停——多半是 `if/else` 判断顺序问题（回忆第4周：先保命判断放最前）。

### 关 3 · 串成能演示的完整流程（完成大作品）
把关1+关2 串顺，对照你计划书的"验收标准"跑一遍：
```python
# 关3（我的收官版）：________ 主题（________ 形态）
from ugot import ugot
import json, time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成你的 IP
ug.set_volume(3)

# 👉 需要的"按钮"和"模型"照你计划书加
#    ug.load_models(['手势/颜色/巡线模型名'])
#    def 动作(): ...

try:
    while True:
        # 👉 完整流程：连接→判断→反应，对照验收标准写
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
    ug.screen_clear()
```
**应当看到（对照你的验收标准）**：你计划书"⑥验收标准"写的几步，全做出来了！🎉🎉🎉
> 💡 卡住先想：哪一"步"演示时没出来？回计划书⑥看缺哪步，补那一小段就行，不用重写全部。

---

## 💡 三个"灵感种子"参考脚手架（任选或自创）
> 下面给 3 个主题的**开头框架**，每行 `👉` 都是要你填的。别直接抄，先想清楚再填。

**🌟 A 手势搬运车（麦轮车）**：手势 AI + 函数 + 灯光
- 关1：`load_models(['gesture'])` → 手掌 palm 前进、石头 stone 停
- 关2：把"前进/左转/停"做成 `def` 按钮
- 关3：比手势指挥它开到目标前，亮绿灯"搬到了"

**🌟 B 语音问答车（任意形态）**：语音 TTS + 屏幕 + 传感器
- 关1：`play_audio_tts("你好，我是小车", 0)` + 屏幕显示
- 关2：用 `read_distance_data(1)` 测距，近了就说"你离我X厘米"
- 关3：循环播报状态，演示"会说话的小车"

**🌟 C 巡逻报警车（麦轮车/蜘蛛）**：避障 + 报警 + 巡线
- 关1：测距 <30 就停 + 红灯 + 喊"有障碍"（第4+5周）
- 关2：加 `line_recognition` 让它沿黑线巡逻
- 关3：沿黑线走、遇障报警、过了继续走，演示"巡逻兵"

> 💡 你也可以完全自己想一个！只要**至少用 2 样本领**、能演示、讲得清，就是好作品。

---

## 步骤 ③ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `set_volume(3)` 的 `3` 改成 `1`/`5`，声音变了吗？
- 把某个速度 `40` 改大/改小，动作变快/变慢了？（场地够大再加速！）
- 故意写错一个前缀（如把 `dog_` 写成 `mecanum_`），看报什么错——认识这个错，以后自己就能排。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关 / 收官！）
- [ ] 我选好了形态、拼好、连上电脑
- [ ] 我填完了"我的项目计划书"（形态/主题/≥2本领/清单/三关/验收）
- [ ] 关1 跑通：核心动作能动起来
- [ ] 关2 跑通：第二样本领加上去、合在一起能跑
- [ ] 关3 跑通：对照我的验收标准，完整演示一遍
- [ ] 我能给家人**演示**这个作品，并讲清"我用了哪几样本领"
- [ ] 我独立完成了它（小导师只给线索，没给我整段答案）

---

## 🃏 随身速查卡（工具箱总汇总，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 运动（换前缀） | `ug.mecanum_move_speed(0,40)`（0前/1后，5~80）；`ug.mecanum_turn_speed(2,120)`（2左/3右，5~280） |
| 函数按钮 | `def 名字(): ...` |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 灯带变色 | `ug.show_light_rgb([0,1,2,3], 255,0,0)`（红）/ `(0,255,0)`（绿）/ `(0,0,255)`（蓝） |
| 关灯 | `ug.turn_off_lights()` |
| 女声/男声 | `ug.play_audio_tts("文字", 0)` / `(..., 1)` |
| 音量 | `ug.set_volume(3)`（1~5） |
| 屏幕写字(红) | `ug.screen_print_text("文字", "FFFF0000")` |
| 加载模型 | `ug.load_models(['gesture'/'color_recognition'/'line_recognition'/'face_recognition'])` |
| 读手势(JSON) | `raw = ug.VISION.gesture_inference()` → `json.loads(raw).get("gesture")` |
| 读颜色 | `raw = ug.get_color_total_info()` → `json.loads(raw)['info'][0]['color']` |
| 读巡线 | `ug.get_single_track_total_info()` → `[偏移量, 线型, x, y]` |
| 急停 | `ug.stop_chassis()` |
| 一直循环 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 **前缀对照**：麦轮 `mecanum_` / 平衡 `balance_` / 轮足 `wheelleg_` / 四足狗 `dog_` / 蜘蛛 `spider_` / 变形 `transform_` / 工程车 `mechanical_`。
> ⚠️ 工程车机械臂 `mechanical_*` 方法名以你这版 SDK 为准（见第15周），别编名字。

---

## 🚀 进阶挑战（收官后还能玩）
- **升级你的作品**：在第16周基础上加一样本领（比如手势搬运车再加"颜色认目标"）。
- **换个形态再做一遍**：同一个主题，换台机器人（如把巡逻车从麦轮换成蜘蛛），感受"前缀一换写法不变"。
- **写一份"使用说明书"**：用大白话给家人写"我的小车怎么玩"，像产品说明书一样——超有成就感。

---

## 🎓 收官啦！你已经是"小发明家"
这 16 周，你从"让机器人连上动一下"，走到今天**自己出题、自己解题、自己做出能演示的大作品**——这就是真正学会编程的样子 🎉

> 卡住了随时来问小导师～ 这周我只给线索、不抢你的功劳。你填的计划书、你跑通的三关、你演示时的笑脸，就是这 16 周最好的毕业礼。去亮出你的作品吧，超有面子 😎
