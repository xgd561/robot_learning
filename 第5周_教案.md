# 第5周 · 教案（理论课）🤖
### 给七年级小朋友的「灯光与声音」第五课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"给机器人装上灯带和嗓子、再打包成一个按钮"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：四足狗（运动前缀 `dog_`）**
> 这周的"报警巡逻车"换成**四足狗**来当保安——平时绿灯慢慢走，见障碍就闪红灯、女声喊"前方有障碍"。形态就藏在代码里：灯带、声音、判断这些"表情和脑子"代码是通用的，只有"怎么走"要换成 `dog_`，也就是"狗怎么走"。本周四足狗最萌，拼装时四条腿要对称，别让它一开机就打架。

上周（第4周）我们给机器人装了"眼睛"——它能自己看路、前方有墙就自己停下，对不对？超棒！这周我们给它再升级：**装上灯带和嗓子**，变成一辆"有障碍就闪红灯、还会开口喊话"的**巡逻车**。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"列表"是啥**：知道列表 `[]` 就是一排带编号的数据，灯带 `[0,1,2,3]` 表示四盏灯（顶/左/右/底），想让哪盏亮就列哪几个数。
2. **用 RGB 调颜色**：明白颜色是"红 Red、绿 Green、蓝 Blue"三个 0~255 的数拼出来的，能说出红灯 `(255,0,0)`、绿灯 `(0,255,0)`、蓝灯 `(0,0,255)`。
3. **把灯和声音写进函数**：能写出 `def 报警():` 把"亮红灯 + 喊话"打包成一个按钮，讲得清冒号和缩进。
4. **把函数组合进主循环**：能写出 `while True:` 让小车一直巡逻，遇到障碍就"按一下" `报警()`，平时绿灯前进。
5. **自己改参数玩**：能改闪烁次数（`range`）、换灯色、换喊话内容，并说清改的是哪一行——这就是调试思维。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人只有"眼睛"（第4周会看路自己停）；这周之后，它还多了"表情和嗓子"——灯带会闪、嘴巴会喊话。更厉害的是：你把"一串动作"用函数打包成一个"按钮"，以后想让它报警，只要喊一声按钮名字就行。函数组合（combine）是编程里最实用的"拼装术"——把以前学的零件拼出新本事。记住这种"我让它又会闪又会喊"的惊喜感！

**📌 一句话目标：** 在避障基础上加灯带和语音，做一辆"有障碍就闪红灯、喊'前方有障碍'"的巡逻车，学会列表、RGB、函数组合。这一步通了，后面手势遥控、综合项目都是在"看 + 判断 + 灯 + 声"这棵树上开花。

---

## 一、这周我们要干啥？🚀
第4周那辆避障小车，会看路、会自己刹车，但它"闷不吭声"对不对？这周我们把它升级成**巡逻保安车**：

- 平时：绿灯亮着，慢慢前进巡逻；
- 一旦前方 < 30cm 有障碍：立刻停下 → 屏幕写"警报!"→ 红灯闪 3 下 → 女声喊"前方有障碍，请绕行"。

要做到这个，你要认识三个新朋友：**列表 list**、**RGB 颜色**、**函数组合**（把灯和声打包成一个按钮）。前两个是"新零件"，最后一个是"把旧零件拼起来"的本领。

## 二、列表 list —— 一串带编号的小格子
列表（list）就是"一排排好队的数据"，用中括号 `[]` 括起来，里面用逗号隔开。

机器人身上有一圈**灯带**，分成四盏，分别编了号：
- `0` = 顶灯
- `1` = 左灯
- `2` = 右灯
- `3` = 底灯

所以 `[0, 1, 2, 3]` 就表示"顶、左、右、底四盏一起"：
```python
ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 让四盏灯一起亮绿灯
```
- 想让四盏一起亮，就写 `[0,1,2,3]`；
- 只想让顶灯亮，就写 `[0]`；
- 想让左右两盏亮，就写 `[1,2]`。

> 💡 小比喻：列表就像一排带编号的小抽屉——你报哪几个编号，就打开哪几个抽屉（亮哪几盏灯）。

## 三、RGB 颜色 —— 三个数就能拼出任意颜色
RGB 是**红 Red、绿 Green、蓝 Blue** 三个英文单词的缩写。每种颜色是一个 **0~255** 的数字，三个数叠在一起，就能调出任何颜色：

| 想要的颜色 | 三个数 (R, G, B) |
|---------|----------------|
| 红 | `(255, 0, 0)` |
| 绿 | `(0, 255, 0)` |
| 蓝 | `(0, 0, 255)` |
| 黄 | `(255, 255, 0)` |
| 白 | `(255, 255, 255)` |

在代码里，这三个数就是 `show_light_rgb` 最后三个参数：
```python
ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯（R=255, G=0, B=0）
ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
```
- ⚠️ 数字范围是 **0~255**，不是 0~100。255 就是"最亮"，0 就是"这个颜色关掉"。
- 小实验：把红的 255 调小一点会怎样？试试 `(100, 0, 0)` 看看是不是变成暗红色～这就是"调试（debug）"的乐趣！

> 💡 小比喻：RGB 就像调色盘上的三个旋钮（红、绿、蓝），你拧不同的量，就混出不同的颜色。

## 四、函数组合 —— 把"闪灯+喊话"打包成一个按钮
第3周我们学过 `def`：把一串动作起个名字，做成一个"按钮"（函数）。这周我们**组合**它：把"亮红灯"和"喊话"两件事，塞进同一个按钮 `报警()` 里。

```python
def 报警():                              # 造一个"报警按钮"
    ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # ① 亮红灯
    ug.play_audio_tts("前方有障碍，请绕行", 0)  # ② 女声喊话
```
- 冒号 `:` 不能丢——它像在说"下面就是这个按钮要做的事"。
- **缩进**（前面空几格）表示"这几行属于这个按钮"。
- 写好后，`报警()` 就是一个真正的按钮：主循环里一遇到障碍，只要写 `报警()` 这五个字，红灯和喊话就一起做。

> 💡 为什么要把两件事打包？想象你每次报警都要手写"亮灯+喊话"两行、还在好几个地方写——又长又容易漏。打包成 `报警()` 后，**想改报警动作只改一处**，别的地方喊一声名字就行。这就是"组合"的省事之处。

## 五、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

> 小提醒：本周要让小车"歇 0.1 秒"别太累，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 六、这周学会后，你应该能回答这几个问题
- 列表 `[0,1,2,3]` 里每个数字代表哪盏灯？（提示：顶/左/右/底）
- 红灯的 RGB 是哪三个数？想变蓝灯怎么改？（提示：`(255,0,0)` → `(0,0,255)`）
- `def 报警():` 这行末尾的冒号和下面缩进，在干啥？（提示：冒号说"下面是按钮的事"，缩进的行属于这个按钮）
- 主循环里"遇到障碍就报警"是怎么写出来的？（提示：`if 距离 < 30: 报警()`）
- 为什么把"闪灯+喊话"打包成函数，而不是直接写两行？（提示：以后想改报警动作只改一处）

> 答得上来，理论就过关啦！翻《实践手册》去造你的第一辆报警巡逻车 💪
