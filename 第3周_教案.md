# 第3周 · 教案（理论课）🤖
### 给七年级小朋友的「函数与运动」第三课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"把一连串动作打包成一个按钮、再拼成路线"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：平衡车（运动前缀 `balance_`）**
> 这周你发明的"前进/后退/左转/右转"按钮，是用来指挥**平衡车**走路的——你写 `走(2)`、`转(左转,1)`，它就稳稳直立走完路线。形态就藏在代码里：把 `mecanum_` 换成 `balance_`，机器人就从"四轮麦轮车"变成"双轮平衡车"，按钮和 Python 写法完全不变，变的只是"它用哪套腿脚动"。本周第一次换形态，拼装时重心要居中，否则它站不稳哦。

上周机器人会倒计时、还会开口数数了对不对？超棒！这周咱们给机器人发明几个"遥控器按钮"——前进键、后退键、左转键、右转键，然后像搭积木一样把它们拼成一条路线，让它自己走完。

**✅ 这周结束，你要能：**
1. **说清楚"函数"是啥**：知道函数就像一个打包好的"按钮"，把一串动作起个名字存起来，以后喊一声名字就自动做完。
2. **用 `def` 造一个函数**：能写出 `def 走(秒数):` 这样的按钮，讲得清冒号 `:` 和缩进的意思。
3. **用参数和默认参数**：明白 `秒数` 是"可调的旋钮"，`速度=40` 是"不写就用 40"的默认值。
4. **给方向起好记的名字**：知道 API 里方向是数字（前进=0，后退=1，左转=2，右转=3），并会写成常量 `前进 = 0`。
5. **把按钮拼成路线**：能写出 `走(2)`、`转(左转, 1)`、`走(2)` 这样的组合，让机器人按"前进→左转→前进"走完。

**🚀 这周学完，你会多一个超能力：**
> 以前你让机器人动一下，得写一整行命令；这周之后，你能把动作**封装成按钮**，再用按钮像搭积木一样拼出任意路线。函数（function）是编程里最省事的"发明工具"——发明一次，以后随便按。记住这种"我造了几个按钮，拼一下它就走完全程"的感觉！

**📌 一句话目标：** 把"前进/后退/左转/右转"写成函数，拼出"前进→左转→前进"路线并跑通，拿到"我能指挥它走完全程"的成就感。这一步通了，后面避障、手势遥控都是在这套按钮上加点东西。

---

## 一、这周我们要干啥？🚀
你每天回家都要"掏钥匙→插锁→拧一下→推门"，挺麻烦的吧？如果有个按钮按一下就自动做完这一串，是不是爽多了？

机器人也一样：让它"前进 2 秒"其实要写一行命令。如果路线是"前进→左转→前进→右转"，每次都重写整行，又长又容易写错。

所以咱们这周干一件事：**把每个动作做成一个"按钮"（函数）**，然后只要写 `走(2)`、`转(左转, 1)` 这种短短几个字，就把路线拼出来了。就像用乐高积木拼一条路，机器人照着走。

## 二、函数 function —— 把一串动作打包成一个按钮
大白话：**函数 = 一个起好名字的"按钮"，按下去自动做完一串动作。**

你造按钮要用一个关键词 `def`（英文 **define**，意思就是"我要定义一个按钮了"）：
```python
def 走(秒数):            # def 按钮名(旋钮):
    ug.mecanum_move_speed_times(0, 40, 秒数, 0)   # 前进(0)，速度40，走"秒数"秒
```
- 冒号 `:` 不能丢——它像在说"下面就是这个按钮要做的事"。
- **缩进**（前面空几格）表示"这几行属于这个按钮"。按一下 Tab 或敲 4 个空格。
- 写好以后，`走` 就是一个真正的按钮了。

> 💡 小比喻：函数就像一个"一键宏"——你平时要按好几下的操作，录成一个按钮，以后点一下全做完。

## 三、参数 parameter —— 按钮上能调的旋钮，还有"默认值"
上面 `走(秒数)` 里的 `秒数` 叫**参数**：同一个按钮，传 2 就走 2 秒，传 3 就走 3 秒，不用写两个函数。

还能更省事——**默认参数**：
```python
def 走(秒数, 速度=40):   # 不写速度时，自动用 40
    ug.mecanum_move_speed_times(0, 速度, 秒数, 0)
```
这样 `走(2)` 和 `走(2, 40)` 一样（都走 2 秒、速度 40），而 `走(2, 70)` 就是加速版。

> 💡 小比喻：参数就像游戏里的"难度旋钮"，你不动它就用默认难度，想挑战就拧一下。旋钮（参数）让一个按钮能应付好多情况。

## 四、方向常量 —— 给数字起个好记的名字
API 里方向是用数字写的：前进=0，后退=1，左转=2，右转=3。直接写 0/1/2/3 容易忘、还容易写错。咱们用变量起名，让代码读起来像人话：
```python
前进 = 0
后退 = 1
左转 = 2
右转 = 3
```
以后写 `走(2)` 比写 `mecanum_move_speed_times(0, 40, 2, 0)` 清爽多了，也更不容易写错。**机器人形态默认麦轮车**；如果你手里是别的形态（平衡车、四足狗…），只要把 `mecanum_` 换成对应前缀就行，方向数字不变。

> 💡 这种"给常用数字起名字"的写法，程序员管它叫**常量**（constant）——意思是一旦定好就不改的"标签"。

## 五、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

> 小提醒：转弯要用到"等一会儿"，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 六、这周学会后，你应该能回答这几个问题
- 函数 `def 走(秒数):` 里的 `def` 是干啥的？（提示：define，定义一个按钮）
- `秒数` 是什么？（提示：参数，可调的旋钮）
- `速度=40` 里的 `=40` 是什么意思？（提示：不写速度时默认用 40）
- 为什么要把 `0` 写成 `前进` 这种名字？（提示：好记、不容易写错）
- `走(2)` 和 `转(左转, 1)` 各在让机器人干什么？（提示：前进 2 秒 / 左转 1 秒）

> 答得上来，理论就过关啦！翻《实践手册》去造按钮、拼路线 💪
