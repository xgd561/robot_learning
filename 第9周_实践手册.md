# 第9周 · 实践手册（动手课）🔧
### 给七年级小朋友的「综合项目①：巡线 + 避障 + 手势遥控整合」第九课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。**巡线要铺一条黑线在地上**（宽约 2~3 厘米、别打弯太急、别用斑马线那种花样）。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：麦轮车（重新拼一台）
> 综合阶段第一周，我们回到 **麦轮车** 当项目底盘。前几周你拼过不同形态，这周把机器人**重新拼回麦轮车**就行。一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 麦轮车结构件包；②固定主控到底盘、装四颗麦克纳姆轮、接好线；③盯三点（螺丝拧紧 / 斜纹方向装对 / 线别卡住轮子）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **麦轮车拼法要点**：四颗**麦克纳姆轮（斜纹轮）**装到底盘四个角，注意**对角两轮斜纹方向一致**（装反了不能横着走）；主控横放、摄像头朝前。拼好连上电脑，本周就把"巡线 + 避障 + 手势"都装在它身上。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：综合项目都要"一直看"，所以代码开头基本都要 `import time`（让小车歇一会儿）。写的时候别漏。

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的综合项目①：巡线 + 避障 + 手势遥控整合（麦轮车）
```
> 第 9 周就做这一个"开门红"大项目（课程安排里写好的）。它把第4周避障 + 第6周手势 + 第8周巡线三块拼一起。做完它，你后面几周还能换别的形态接着玩。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第六节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🛤️ 巡线模型 | `ug.load_models(['line_recognition'])` 加载 |
| ③ | 👀 手势模型 | `ug.load_models(['gesture'])` 加载 |
| ④ | 🛤️ 巡线 | `ug.get_single_track_total_info()` 读偏移量，按偏移调速 |
| ⑤ | 👀 传感器 | `ug.read_distance_data(1)` 前方<30 就停 |
| ⑥ | ✋ 手势 | 手掌=进手动、石头=回巡线 |
| ⑦ | 🚗 运动 | 前进/转弯/停 |
| ⑧ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 纯巡线（只用第8周本事）
让麦轮车沿黑线走起来。先加载巡线模型、设成"单轨"，再一直读偏移量调速：
```python
# 关1：纯巡线（麦轮车）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])   # 加载巡线（单/双轨）模型
ug.set_track_recognition_line(0)        # 0=单轨(一条黑线)；1=双轨

前进速度 = 35
修正速度 = 90
try:
    while True:
        信息 = ug.get_single_track_total_info()   # 返回 [偏移量, 线型, x, y]
        偏移量 = 信息[0]
        线型   = 信息[1]      # 0=没线 1=直 2=Y字路口 3=十字路口
        if 线型 == 0:
            ug.stop_chassis()                 # 没看到线，先停（或慢慢找线）
            ug.screen_print_text("没线", "FFFF0000")
        else:
            if 偏移量 > 0:                    # 线偏一边
                ug.mecanum_turn_speed(3, 修正速度)   # 往那一边修（3=右转）
            elif 偏移量 < 0:
                ug.mecanum_turn_speed(2, 修正速度)   # 往另一边修（2=左转）
            else:
                ug.mecanum_move_speed(0, 前进速度)   # 基本居中，直走
        time.sleep(0.1)
except:
    ug.stop_chassis()          # 不管出啥意外，先停车最安全
```
**应当看到**：机器人沿黑线走，线偏了它会自己往回修。🎉
> 💡 卡住先想：`偏移量` 的正负代表线偏左还是偏右。如果车往线反方向拐（越修越歪），把上面 `3` 和 `2` 对调一下就行——第一次跑让娃试一下方向。

### 关 2 · 加避障（叠第4+5周本事）
在关 1 基础上，加"前方<30cm 自动停 + 红灯 + 语音"。注意**避障判断要放在巡线前面**（先保命再跟线）：
```python
# 关2：巡线 + 避障（麦轮车）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition'])
ug.set_track_recognition_line(0)
安全距离 = 30
前进速度 = 35
修正速度 = 90
try:
    while True:
        距离 = ug.read_distance_data(1)          # 先读障碍
        if 距离 == -1 or 距离 < 安全距离:        # 有障碍 → 停+报警
            ug.stop_chassis()
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
            ug.play_audio_tts("前方有障碍", 0)
            ug.screen_print_text("报警", "FFFF0000")
        else:                                     # 没障碍 → 继续巡线
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
            ug.screen_print_text("巡线中", "FF00FF00")
            信息 = ug.get_single_track_total_info()
            偏移量 = 信息[0]; 线型 = 信息[1]
            if 线型 == 0:
                ug.stop_chassis()
            elif 偏移量 > 0:
                ug.mecanum_turn_speed(3, 修正速度)
            elif 偏移量 < 0:
                ug.mecanum_turn_speed(2, 修正速度)
            else:
                ug.mecanum_move_speed(0, 前进速度)
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：前面有墙（<30cm）就停、闪红灯、喊"前方有障碍"；墙挪开又接着巡线。🎉
> 💡 卡住先想：`距离 == -1` 放最前面，是因为"读不到=最危险"，要第一个判断（忘了看《第4周_实践手册》）。

### 关 3 · 加手势切换手动遥控（叠第6周本事）→ 完成整合
最后叠手势：比**手掌(palm)** 切到"手动模式"，手动里用手势指挥前进/转弯绕过障碍；比**石头(stone)** 退出手动、回到巡线。这就是课程安排里的"验收流程"：
```python
# 关3（终极整合）：巡线 + 避障 + 手势遥控（麦轮车）
from ugot import ugot
import json, time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['line_recognition', 'gesture'])  # 一次加载两个模型
ug.set_track_recognition_line(0)
安全距离 = 30
前进速度 = 35
修正速度 = 90
手动模式 = False          # 一开始是"巡线模式"
try:
    while True:
        # —— 先读手势，决定要不要切模式 ——
        原始 = ug.VISION.gesture_inference()
        手势 = json.loads(原始).get("gesture")
        if 手势 == "palm":       # 手掌 → 进手动
            手动模式 = True
            ug.screen_print_text("手动", "FFFFFF00")
        elif 手势 == "stone":    # 石头 → 回巡线
            手动模式 = False
            ug.screen_print_text("巡线", "FF00FF00")

        if 手动模式:
            # —— 手动遥控：你用手势指挥 ——
            if 手势 == "palm":
                ug.mecanum_move_speed(0, 前进速度)     # 手掌=前进
            elif 手势 == "good":
                ug.mecanum_turn_speed(2, 120)          # 点赞=左转（你来定）
            else:
                ug.stop_chassis()                      # 其它手势先停
        else:
            # —— 巡线模式（含避障）——
            距离 = ug.read_distance_data(1)
            if 距离 == -1 or 距离 < 安全距离:
                ug.stop_chassis()
                ug.show_light_rgb([0,1,2,3], 255, 0, 0)
                ug.play_audio_tts("前方有障碍", 0)
            else:
                ug.show_light_rgb([0,1,2,3], 0, 255, 0)
                信息 = ug.get_single_track_total_info()
                偏移量 = 信息[0]; 线型 = 信息[1]
                if 线型 == 0:
                    ug.stop_chassis()
                elif 偏移量 > 0:
                    ug.mecanum_turn_speed(3, 修正速度)
                elif 偏移量 < 0:
                    ug.mecanum_turn_speed(2, 修正速度)
                else:
                    ug.mecanum_move_speed(0, 前进速度)
        time.sleep(0.2)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到（验收五步）**：巡线中 → 遇障自动停+红灯+喊话 → 你比手掌切手动、用手势遥控绕过 → 比石头回巡线继续走。🎉🎉🎉
> 💡 卡住先想：`json.loads(原始).get("gesture")` 是取出手势名字；认不出多半是手没对准摄像头。手动模式里"点赞 good 左转"你可以自己改成右转、或加"ok 后退"。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关1：把 `前进速度=35` 改成 `55`，走更快了吗？（场地要够长！）
- 关1：把 `3` 和 `2` 对调，看它是不是走反了——这就验证偏移量正负方向。
- 关2：把 `安全距离=30` 改成 `15` 或 `50`，停的位置变了吗？
- 关3：把手动模式里的 `good=左转` 改成 `ug.mecanum_turn_speed(3,120)`（右转）。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把麦轮车重新拼好、连上电脑
- [ ] 我画出了"巡线+避障+手势"的动作清单
- [ ] 关1 纯巡线跑通：机器人沿黑线走、会修偏
- [ ] 关2 加避障跑通：遇墙自动停+红灯+喊话
- [ ] 关3 整合跑通：比手掌切手动遥控、比石头回巡线
- [ ] 我能给家人演示验收五步：**巡线中→遇障停→比手势切手动→遥控通过→恢复巡线**

---

## 🃏 随身速查卡（综合项目常用 API 汇总，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 加载巡线模型 | `ug.load_models(['line_recognition'])` |
| 设单/双轨 | `ug.set_track_recognition_line(0)`（`0`单轨/`1`双轨） |
| 读巡线结果 | `ug.get_single_track_total_info()` → `[偏移量, 线型, x, y]`（`线型`：0没线/1直/2Y路口/3十字） |
| 加载手势模型 | `ug.load_models(['gesture'])` |
| 读手势(JSON) | `raw = ug.VISION.gesture_inference()` → `json.loads(raw).get("gesture")` |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 前进 / 后退 | `ug.mecanum_move_speed(0, 40)` / `(1, 40)` |
| 左转 / 右转 | `ug.mecanum_turn_speed(2, 120)` / `(3, 120)` |
| 急停 | `ug.stop_chassis()` |
| 屏幕写字(红) | `ug.screen_print_text("文字", "FFFF0000")` |
| 灯带全红 | `ug.show_light_rgb([0,1,2,3], 255, 0, 0)` |
| 关灯 | `ug.turn_off_lights()` |
| 女声 / 男声 | `ug.play_audio_tts("文字", 0)` / `(..., 1)` |
| 一直循环 | `while True:` |
| 歇 0.2 秒 | `time.sleep(0.2)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `mecanum_move_speed(方向, 速度)`：方向 0前/1后，速度 5~80；`mecanum_turn_speed(方向, 速度)`：2左转/3右转，速度 5~280。
> 其它形态把 `mecanum_` 换成 `balance_`(平衡车) / `wheelleg_`(轮足) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车)。

---

## 🚀 进阶挑战（下周迭代方向，先想想）
- **双轨 / 路口**：把 `set_track_recognition_line(1)` 改成双轨；`线型==2/3` 时做"遇到 Y 字路口随机选一边"。
- **语音播报状态**：巡线时屏幕/语音报"我在巡线"，手动时报"手动模式"。
- **换形态**：把 `mecanum_` 换成 `transform_`(变形车) 做"越障+巡线"（第14周预告）。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"三关叠加把三块积木拼成大项目"，一层层来最稳！
