# 第2周 · 教案（理论课）🤖
### 给七年级小朋友的「屏幕与循环」第二课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让机器人会数数、会说话"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：麦轮车（运动前缀 `mecanum_`）**
> 这周我们用**麦轮车**做"倒计时火箭"——你写的 `for` 循环让它的屏幕 5→1 数数、女声报数，最后每数一下用 `mecanum_move_speed_times` 往前走 1 秒。形态就藏在代码里：那个 `mecanum_` 就是"麦轮车专属"前缀，换别的形态只要把前缀换了，Python 写法一模一样。本周是连续第二周用麦轮车，先把这台"四轮小车"摸熟。

这周我们让机器人像火箭一样 5、4、3、2、1 倒计时，还能开口念出数字。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"变量"是啥**：知道变量就像一个贴了标签的盒子，里面能放数字也能放文字，想换内容改盒子就行，不用满篇改。
2. **用 `str()` 把数字变文字**：明白屏幕和语音都只收"文字"，所以显示数字前要用 `str(n)` 把数字 3 变成文字 "3"。
3. **写出 `for` 循环做倒计时**：用 `for n in range(5, 0, -1)` 让机器人自动 5→1 数数，讲得清三个参数（从 5 开始／到 0 之前停／每次 -1=倒数）。
4. **让屏幕和声音一起动**：用 `ug.screen_print_text(文字, "FFFF0000")` 显示红字，用 `ug.play_audio_tts(文字, 0)` 让机器人开口报数。
5. **自己改参数玩**：能改成数到 10、换蓝字、换男声，并说清改的是哪一行——这就是调试思维。

**🚀 这周学完，你会多一个超能力：**
> 以前你只会让机器人"动一下"；这周之后，你能让它**按节奏自己重复干活、还会开口说话**。循环（loop）是编程里最厉害的"偷懒神器"——一次写好，电脑帮你重复一万遍都不累。记住这种"我让它一遍遍数数，它真听话"的感觉！

**📌 一句话目标：** 用 for 循环让机器人屏幕 5→1 红字倒计时、同时女声报数，拿到"我能让它重复干活"的成就感。这一步通了，后面每周我们都能让它做更聪明的重复任务。

---

## 一、这周我们要干啥？🚀
上周机器人动了一下、还亮了字对不对？超酷！这周我们让它**像个真火箭一样倒计时**：
- 屏幕跳出来一个大大的 **5、4、3、2、1**；
- 每跳一个数，它还**用女声念出来**；
- 最后喊一声"出发！"再往前冲。

要做到这个，你要认识三个新朋友：**变量**、**屏幕显示**、**for 循环**。

## 二、变量 variable —— 给数据起个名字
你玩游戏要给角色起名吧？代码也一样。
```python
n = 5
```
意思是：建一个叫 `n` 的小盒子，里面先装个数字 5。以后写 `n` 就等于写 5。**想换数字，改盒子里的东西就行**，不用满篇改。

> 💡 小比喻：`n` 就像一个贴了标签的抽屉，标签写 "n"，里面放的东西（数字）随时能换。

## 三、把东西"显示"出来 —— 这次是显示在机器人屏幕上
平时学 Python，`print` 是把字打在电脑上。但在 UGOT 里，我们用 `ug.screen_print_text` 把字**打到机器人的屏幕上**：
```python
ug.screen_print_text("你好", "FFFF0000")   # 红字显示在机器人屏幕
```
最后那一串 `"FFFF0000"` 是颜色代码（ARGB 十六进制）：
- 红 `"FFFF0000"` ／ 绿 `"FF00FF00"` ／ 蓝 `"FF0000FF"` ／ 黄 `"FFFFFF00"` ／ 白 `"FFFFFFFF"`

> 💡 记不住也没事，动手手册最后有「随身速查卡」，直接抄。

## 四、for 循环 loop —— 让电脑"复读"干活
"倒计时"就是不停地做同一件事：显示一个数字 → 念出来 → 数字减 1。
如果手动写，要写 5 遍，太累。循环帮我们一遍遍自动做：
```python
for n in range(5, 0, -1):   # 从 5 数到 1，每次减 1
    ug.screen_print_text(str(n), "FFFF0000")
    ug.play_audio_tts(str(n), 0)
```
- `range(5, 0, -1)` 翻译：从 5 开始，数到 0 之前停下（就是 5,4,3,2,1），每次 **-1**（倒数）。
- **缩进（前面空几格）的部分**，就是每次都要重复做的事。
- `str(n)`：把数字变成"文字"，因为屏幕和语音都只收文字。`str` 就是"变文字"的意思。

> 💡 小比喻：`for` 就像一个"复读机"——你告诉它"从 5 到 1、每次减 1"，它就帮你一遍遍执行缩进里的动作。

## 五、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

## 六、这周学会后，你应该能回答这几个问题
- 变量 `n` 是干什么用的？（提示：给数据起名字）
- `range(5, 0, -1)` 会数出哪些数？（提示：5,4,3,2,1）
- `for` 下面缩进的那几行，会被做几遍？（提示：5 遍）
- 为什么屏幕上要先写 `str(n)` 而不是 `n`？（提示：屏幕只收文字）

> 答得上来，理论就过关啦！翻《实践手册》去让它真的数起来 💪
