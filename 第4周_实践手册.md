# 第4周 · 实践手册（动手课）🔧
### 给七年级小朋友的「传感器与判断」第四课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：蜘蛛（新拼一台）
> 这周换 **蜘蛛**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 蜘蛛结构件包；②固定主控到机身、装四条腿的舵机、接好线；③盯三点（螺丝拧紧 / 腿要对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **蜘蛛拼法要点**：四条仿生腿向四周伸开，每条腿一个舵机；机身低、重心低。腿要**对称**（左前/右前/左后/右后别装错），装完四条腿自然张开、不打架。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本次要把机器人**正面对着一面墙**（离个 1~2 米），这样它才有"墙"可看。场地要够长，别让它一启动就顶到墙。

## 步骤 ① 先认识"眼睛"：读一次距离 ✅
先不急着装脑子，我们让机器人**报出它看到的距离**，确认眼睛是好的：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

距离 = ug.read_distance_data(1)   # 读一次前方距离（厘米）
ug.screen_print_text(str(距离), "FFFF0000")  # 把距离显示在屏幕（红字）
ug.play_audio_tts(str(距离), 0)               # 顺便念出来（女声）
ug.stop_chassis()
```
**应当看到/听到**：屏幕显示一串数字（比如 120.5），同时女声念出这个数。把机器人慢慢靠近墙，数字应该越来越小。✅
> 💡 卡住先想：`read_distance_data(1)` 读不到时会返回什么？—— 返回 `-1`。如果一直显示 -1，说明传感器没对准墙，或前面没障碍物，换个方向试试。

## 步骤 ② 用 if 做"判断"：近了就停（本周核心概念！）✅
现在让机器人**自己决定**：距离 < 30 就停，否则前进。先试用"判断一次"的版本：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

安全距离 = 30   # 厘米，前方小于这个就停

距离 = ug.read_distance_data(1)
if 距离 < 安全距离:
    ug.stop_chassis()                  # 离墙太近，停！
else:
    ug.spider_move_speed(0, 40)       # 安全，前进
```
**应当看到**：机器人看一眼前方——如果离墙远就往前走，离墙近就停在原地。✅
> 💡 这里只判断了"一次"。要让它一直看，得加上循环，下一步就来。

## 步骤 ③ 加 while 循环，让它一直看路（本周最核心！）✅
把"看一眼"变成"一直看"，用 `while True:` 套住判断。完整可跑版：
```python
from ugot import ugot
import time                  # 让小车"歇 0.1 秒"要用到

ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

安全距离 = 30   # 厘米，前方小于这个就停

while True:
    距离 = ug.read_distance_data(1)
    if 距离 < 安全距离:
        ug.stop_chassis()                  # 离墙太近，停！
    else:
        ug.spider_move_speed(0, 40)       # 安全，前进
    time.sleep(0.1)                       # 歇 0.1 秒，别让小车太累
```
**应当看到**：机器人对着墙开，会在离墙约 30cm 处**自动停下**，不撞墙！把墙挪近/挪远，它都会保持这个距离停住。🎉
> 💡 重点：`while True` 会一直跑。想让它停下，关掉编辑器运行、或按急停。下一步我们给它加"安全网"。

## 步骤 ④ 加固：处理 "-1" + 加急停安全网（稳健版）✅
前面有个漏洞：如果传感器**读不到**（返回 -1），`距离 < 30` 是 `False`，机器人会以为"安全"继续往前冲——这不行！所以我们要**特别照顾 -1：读不到就保守停下**。再加 `try/except` 急停：
```python
from ugot import ugot
import time

ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

安全距离 = 30

try:
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1:
            ug.stop_chassis()                 # 读不到距离，"瞎了"，最安全是停
        elif 距离 < 安全距离:
            ug.stop_chassis()                 # 离墙太近，停！
        else:
            ug.spider_move_speed(0, 40)      # 安全，前进
        time.sleep(0.1)
except:
    ug.stop_chassis()       # 不管发生什么，先停车最安全
    print("急停！")
```
**应当看到**：和步骤③一样能避障；但即使传感器短暂失灵（显示 -1），它也会停下而不是乱冲。这就是"稳"的小车！✅
> 💡 卡住先想：`if 距离 == -1:` 为什么放最前面用 `if` 而不是 `elif`？—— 因为"-1"是最危险的，要**第一个**判断、优先处理。

## 步骤 ⑤ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `安全距离 = 30` 改成 `15`，小车会离墙更近还是更远才停？
- 把 `安全距离` 改成 `50`，小车是不是更早停下来？
- 把 `ug.spider_move_speed(0, 40)` 的速度 `40` 改成 `70`，前进更快了吗？（注意场地要更大！）
- 把 `spider_` 换成你其它形态的前缀试试（见速查卡底部）。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 机器人靠近墙壁（< 30cm）自动停车，不撞
- [ ] 读不到距离（-1）时保守停下，不乱冲
- [ ] 你能自己改 `安全距离` 的数字，观察小车反应变化
- [ ] 你能给家人演示："看，它自己看路、自己刹车，不撞墙！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 读前方距离(厘米) | `ug.read_distance_data(1)` |
| 读不到距离时的值 | 返回 `-1` |
| 前进 | `ug.spider_move_speed(0, 40)` |
| 急停 | `ug.stop_chassis()` |
| 判断(近就停) | `if 距离 < 30: ug.stop_chassis()` |
| 否则前进 | `else: ug.spider_move_speed(0, 40)` |
| 一直循环看路 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `spider_move_speed(方向, 速度)`：方向 0前/1后/2左转/3右转，速度 5~80。
> 其它形态把 `spider_` 换成 `balance_`(平衡车) / `wheelleg_`(轮足) / `dog_`(四足狗) / `transform_`(变形车) / `mechanical_`(工程车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **撞墙前拐弯躲开**：把"停下"换成右转一下，看看它能不能自己绕开墙：
  ```python
  elif 距离 < 安全距离:
      ug.spider_turn_speed(3, 120)   # 右转躲开（3=右转，速度 120）
      time.sleep(0.5)
  ```
- **屏幕一直显示距离**：在循环里加 `ug.screen_print_text("距离:" + str(距离), "FFFF0000")`，让它"心里有数"。
- **接第5周预告**：把"停"的时候顺便闪红灯 + 喊"前面有障碍"（灯光/语音函数见第5周），做成"会报警的巡逻车"。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
