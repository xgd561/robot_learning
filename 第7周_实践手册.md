# 第7周 · 实践手册（动手课）🔧
### 给七年级小朋友的「颜色与人物识别」第七课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：工程车（新拼一台，带机械臂）
> 这周换 **工程车**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 工程车结构件包（含机械臂）；②固定主控到底盘、装 3 轴机械臂、接好线；③盯三点（螺丝拧紧 / 机械臂舵机角度归中 / 走线理好别缠轮子）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **工程车拼法要点**：在车体上加 **3 轴机械臂**（底座旋转 + 大臂 + 小臂 + 夹爪）。机械臂舵机多、**走线要理好**别缠轮子，夹爪先调松紧适中（能夹住球又不会捏太死）。这是七种里零件最多的，慢慢拼不急。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：这周要用**摄像头**，所以把机器人放在光线充足的地方，正面对着——颜色版：几个彩色球/积木（红、绿最直观）；人脸版：一张人脸。背景越干净，认得越准。

## 步骤 ① 先打印识别结果，认识"眼睛"看到的数据 ✅
> 这周机器人返回的是"一段文字"，不像距离那样直接是个数字。所以我们**第一步先别急着做反应，先把它打印出来看看长啥样**——这能帮你之后写对代码，少踩坑。

```python
from ugot import ugot
import json

ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['color_recognition'])   # 先装颜色眼睛
ug.screen_print_text("看颜色", "FFFFFF00")

raw = ug.get_color_total_info()   # 读一次看到的颜色
print(raw)                         # ← 把原始数据打印出来看
```
**应当看到**：编辑器下方输出区打印出一段像下面这样的文字：
```json
{"total": 1, "info": [{"color": "red", "x": 150, "y": 120, "width": 40, "height": 40}]}
```
- `total` = 看到了几个颜色块（这里是 1 个）；
- `info` 里面是每个颜色的信息，`info[0]['color']` 就是第 1 个的颜色名 `"red"`（红）。
✅ 说明机器人的"颜色眼睛"工作正常。

> 💡 确认你是哪类版本：
> - 如果打印出来是**带引号的纯文字**（像上面那样）→ 后面用 `data = json.loads(raw)` 翻译。
> - 如果打印出来已经是 `{total:...}` 这种**真字典**（没有外层引号）→ 你这版直接返回字典，后面把 `data = json.loads(raw)` 改成 `data = raw` 就行。
> 实在分不清，把打印的内容念给小导师听。

## 步骤 ② 用 if 做"分类反应"：认红/绿做不同反应（本周核心！）✅
把"看一眼"变成"一直看 + 分类反应"，用 `while True` 套住。完整可跑版（**颜色版，推荐先玩**）：
```python
from ugot import ugot
import json
import time

ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['color_recognition'])   # 装"颜色眼睛"
ug.set_volume(3)

ug.screen_print_text("我在看颜色啦", "FFFFFF00")

上一次 = ""   # 记住上次看到的颜色，避免一直重复念
try:
    while True:
        raw = ug.get_color_total_info()
        data = json.loads(raw)          # 翻译：文字→字典（见步骤①确认版本）

        if data['total'] > 0:           # 至少看到 1 个颜色块
            颜色 = data['info'][0]['color']
            if 颜色 != 上一次:          # 只有颜色变了才反应
                上一次 = 颜色
                if 颜色 == 'red':
                    ug.screen_print_text("红色!", "FFFF0000")
                    ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯
                    ug.play_audio_tts("这是红色", 0)
                elif 颜色 == 'green':
                    ug.screen_print_text("绿色!", "FF00FF00")
                    ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
                    ug.play_audio_tts("这是绿色", 0)
                else:
                    ug.screen_print_text("看到:" + 颜色, "FFFFFFFF")
                    ug.turn_off_lights()
        else:
            ug.screen_print_text("没看到颜色", "FFFFFFFF")
            ug.turn_off_lights()
        time.sleep(0.1)
except:
    ug.turn_off_lights()
    ug.screen_clear()
    ug.stop_chassis()
```
**应当看到/听到**：把红球放到摄像头前 → 屏幕红字"红色!"、灯带变红、女声"这是红色"；换成绿球 → 全变绿。颜色不变时它不啰嗦（靠 `上一次` 记住）。🎉
> 💡 重点：`上一次` 这个变量（第2周学的）在这里大显身手——它记住"上一次看到的颜色"，只有变了才反应，不然机器人会一直念"红色红色红色"。这就是变量的真实用处！

## 步骤 ③ 选项B：人脸识别喊名 ✅
这个要先做一步准备：在 UGOT APP（或网页编辑器里"人脸识别"管理页）把家人的脸**录进去、起个名字**（比如给爸爸起名 `baba`）。录好后机器人认出来，`name` 字段就是那个名字。

完整可跑版（**人脸版**）：
```python
from ugot import ugot
import json
import time

ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.load_models(['face_recognition'])   # 装"人脸眼睛"
ug.set_volume(3)

ug.screen_print_text("谁来了?", "FFFFFF00")

上一次 = ""
try:
    while True:
        raw = ug.get_face_recognition_total_info()
        data = json.loads(raw)          # 翻译（版本问题见步骤①）

        if data['total'] > 0:
            名字 = data['info'][0]['name']
            if 名字 != 上一次:
                上一次 = 名字
                if 名字 == "":
                    ug.screen_print_text("脸熟但没名字", "FFFFFF00")
                    ug.play_audio_tts("你是谁呀", 0)
                else:
                    ug.screen_print_text("你好 " + 名字, "FF00FF00")
                    ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯欢迎
                    ug.play_audio_tts("你好 " + 名字, 0)
        else:
            ug.screen_print_text("没看到脸", "FFFFFFFF")
            ug.turn_off_lights()
        time.sleep(0.1)
except:
    ug.turn_off_lights()
    ug.screen_clear()
    ug.stop_chassis()
```
**应当看到/听到**：录入过的脸出现在摄像头前 → 屏幕"你好 baba"、绿灯、女声"你好 baba"。没录入过的脸，`name` 通常是空字符串 `""`，就喊"你是谁呀"。✅
> 💡 卡住先想：如果一直喊"你是谁呀"，说明脸没录进去或名字对不上——回 APP 确认录入成功。打印 `raw` 看 `name` 实际是啥，按真实值改判断。

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 颜色版：把 `elif 颜色 == 'green':` 复制一行改成 `elif 颜色 == 'blue':`，灯改成 `(0, 0, 255)`，放个蓝球试试认不认得出。
- 颜色版：把 `show_light_rgb` 的红色 `(255, 0, 0)` 改成 `(0, 0, 255)`，灯变什么颜色？
- 人脸版：把 `ug.play_audio_tts("你好 " + 名字, 0)` 的 `0`（女声）改成 `1`（男声）。
- 两个版都能加：在镜头前把球/脸前后移动，看机器人反应跟不跟得上（歇 0.1 秒那行调小 = 更灵敏）。
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 装上"颜色眼睛"或"人脸眼睛"，机器人真实认出东西
- [ ] 认出红→红灯红字、绿→绿灯绿字（或认出人脸→喊名字）
- [ ] 颜色/人脸没变时它不啰嗦（靠 `上一次` 记住）
- [ ] 你能自己改：比如加一个"蓝球"分支，或换男声喊名
- [ ] 你能给家人演示："看，它认出颜色 / 认出我了！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 装"颜色眼睛" | `ug.load_models(['color_recognition'])` |
| 装"人脸眼睛" | `ug.load_models(['face_recognition'])` |
| 读颜色结果(文字) | `ug.get_color_total_info()` |
| 读人脸结果(文字) | `ug.get_face_recognition_total_info()` |
| 文字变字典 | `data = json.loads(raw)` |
| 取颜色名 | `data['info'][0]['color']` |
| 取人名 | `data['info'][0]['name']` |
| 红灯(整圈) | `ug.show_light_rgb([0,1,2,3], 255, 0, 0)` |
| 女声说话 | `ug.play_audio_tts("文字", 0)` |
| 关灯 | `ug.turn_off_lights()` |
| 一直循环看 | `while True:` |
| 急停安全网 | `try: ... except: ug.stop_chassis()` |

> 💡 颜色名常见有 `"red"`(红) `"green"`(绿) `"blue"`(蓝) `"yellow"`(黄)，以你步骤①打印的真实值为准。
> 人脸没录入过时 `name` 多为 `""`（空字符串），也可能返回 `"stranger"`，按你打印的真实值改判断。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **颜色分拣"升级版"**：看到红往前走、看到绿往后退，做个会自动分类移动的小车（运动函数见《第3周_实践手册》速查卡）。
- **人脸"迎宾"**：认出爸爸亮绿灯喊名、认出妈妈亮粉灯喊名（粉灯 RGB 自己调：`(255,105,180)`）。
- **工程车机械臂（选修）**：认出颜色后，可用机械臂 `mechanical_*` 方法让夹爪指向 / 夹起目标球——机械臂具体方法名看你这版 SDK（拼装时已在手臂接好线），不会就问小导师。
- **接第8周预告**：把摄像头对着地上的黑线，认出线往哪边偏就往哪边转——巡线小车。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
