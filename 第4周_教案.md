# 第4周 · 教案（理论课）🤖
### 给七年级小朋友的「传感器与判断」第四课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"让机器人自己看路、自己决定停不停"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

> 🤖 **本周要拼的形态：蜘蛛（运动前缀 `spider_`）**
> 这周的"看路自己停"让**蜘蛛**来当巡逻兵——TOF 眼睛盯着前方，蜘蛛四足全向移动，靠近墙就自己停。形态就藏在代码里：读距离、做判断（`if 距离 < 30`）这些"脑子"代码是通用的，只有"怎么动"要换成 `spider_`，也就是"蜘蛛怎么走"。你会第一次发现：同一个避障逻辑，套在麦轮车或蜘蛛身上都跑得通。

前几周，机器人都是"听我们命令行事"——我们让它前进它就前进。这周要让它**自己看路、自己决定**：前方有墙（距离 < 30cm）就自己停下，不撞墙。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚"传感器"是啥**：知道机器人靠 TOF 测距"眼睛"算出前方距离（厘米），读不到时返回 -1。
2. **用 `if / else` 做判断**：能写出"如果距离 < 30 就停，否则前进"，讲得清冒号 `:` 和缩进。
3. **用比较运算和布尔值**：明白 `距离 < 30` 算出来不是数字，而是"对 / 错"（True / False）。
4. **用 `while` 一直反复做**：能写出 `while True:` 让小车"一直盯着前方看"。
5. **加一张"安全网" `try / except`**：知道一旦出意外，第一件事就是 `stop_chassis()` 急停。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人是"你推一步它走一步"；这周之后，它**有了眼睛和脑子**——自己看、自己判断、自己停下。这就是编程里最酷的"自动化"：你写好规则，电脑替你一直盯着、一直做决定。记住这种"它自己躲开了墙"的惊喜感！

**📌 一句话目标：** 用 TOF 测距 + if 判断，做出第一辆"会自己决策"的避障小车：前方 < 30cm 自动停，读不到距离就保守停。这一步通了，避障、手势遥控、巡逻报警都是在"看 + 判断"这棵树上开花。

---

## 一、这周我们要干啥？🚀
前几周我们让机器人动、数数、还拼了路线对不对？超棒！这周给它升级成**有大脑的小车**：

- 它前面装了一只"眼睛"（测距传感器）；
- 眼睛一直盯着前方，算出墙离它还有多远；
- 一旦墙太近，它就**自己刹车**，不撞上去。

要做到这个，你要认识四个新朋友：**传感器**、**判断 if/else**、**比较运算**、**while 循环**，再加一张**安全网 try/except**。

## 二、机器人的"眼睛"：TOF 测距传感器
你走路会看前面有没有墙对吧？机器人靠一个叫 **TOF** 的"眼睛"。

- **TOF** = "Time Of Flight"（飞行时间），中文叫"飞行时间测距"。它往前方发射一束看不见的光，光撞到墙再弹回来，机器人算"去 + 回"用了多久，就知道墙离它多远。最远能看 **150 厘米**。
- 怎么读？一行就够：
```python
距离 = ug.read_distance_data(1)   # 括号里填 1（传感器编号）
```
- 它返回一个**数字**，单位是**厘米**。
- ⚠️ 特殊情况：如果它"没看到"或"读不出来"，会返回 `-1`。这个数字后面我们要**特别照顾**它——因为这时候机器人"瞎了"，最安全的选择就是停下。

> 💡 小比喻：TOF 就像机器人的"手电筒 + 尺子"——它打一束光，光弹回来花的时间，就是墙的距离。

## 三、做"决定"：if / else（如果……否则……）
人过马路会看红绿灯："如果是绿灯 → 走；否则 → 停"。机器人也一样，它靠 `if / else` 做决定。

```python
if 距离 < 30:
    ug.stop_chassis()                 # 离墙太近，停！
else:
    ug.mecanum_move_speed(0, 40)      # 安全，前进
```
- 注意 `if` 那一行末尾的 **冒号 `:`**，还有下面**缩进**（前面空 4 个空格）！这是 Python 的规矩：冒号后面、缩进的内容，就是"满足条件才做的事"。
- `else:` 后面缩进的，是"前面条件不成立时才做"的事。

> 💡 小比喻：`if` 就像一个岔路口的红绿灯——条件成立走左边（停），不成立走右边（前进）。

## 四、比较运算（比大小）
`if` 后面的 `距离 < 30` 是在"比大小"：
- `<` 小于、`>` 大于、`==` 等于（**两个等号！** 一个等号是"把值装进变量"）、`<=` 小于等于。
- 这些符号算出来的结果**不是数字**，而是下面要讲的"对 / 错"。

## 五、布尔值（Boolean，真/假）
计算机里"对/错"叫 **布尔值 (Boolean)**：`True`（真）和 `False`（假）。
- 离墙 20cm 时，`距离 < 30` 等于 `True`；
- 离墙 80cm 时，`距离 < 30` 等于 `False`。
- `if` 就盯着这个真假，决定走哪条路：真就进 `if`，假就进 `else`。

> 💡 小比喻：布尔值就像一道判断题的答案——要么"√ 对"，要么"× 错"，没有中间值。机器人每看一次前方，就先打个勾或叉。

## 六、一直反复做：while 循环
第2周我们学过 `for`：数着数做 N 次。这周用 `while`：**只要条件成立，就一直做**。

避障小车要**一直看前方**，所以我们用 `while True:` —— "永远成立，所以永远循环"，直到我们让它停：
```python
while True:
    距离 = ug.read_distance_data(1)
    if 距离 < 30:
        ug.stop_chassis()
    else:
        ug.mecanum_move_speed(0, 40)
```
> 💡 小比喻：`while True` 就像一个停不下来的复读机——"看一眼 → 判断 → 看一眼 → 判断……"，直到你按急停。

## 七、三行"万能开头"还记得吗？（连接机器人）
无论写啥，代码最前面永远这 3 行——就是"先连上机器人"：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
忘了它们干嘛的？回去翻《第1周_教案》第五节，IP 就是机器人在 WiFi 里的"门牌号"。这周直接复用，不重复讲啦。

> 小提醒：本周循环里要"歇一下"别让小车太累，得在开头加一行 `import time`（Python 自带的计时工具），待会儿实践手册里会看到。

## 八、安全网：try / except（试试看 / 出错了怎么办）
小车跑起来后可能出意外（比如我们想让它立刻停手）。我们给整个循环套一张"安全网"：**一旦出错，第一件事就是停车**。
```python
try:
    while True:
        距离 = ug.read_distance_data(1)
        ...
except:
    ug.stop_chassis()   # 不管发生什么，先停车最安全
    print("急停！")
```
- `try:` 里放"想做的事"；`except:` 里放"出错了怎么办"。
- 这样哪怕程序哪里出错，小车也**先停下**，不会乱冲。这是写 robot 程序的好习惯。

## 九、这周学会后，你应该能回答这几个问题
- 机器人的"眼睛"TOF 是干嘛的？返回什么？（提示：测距，厘米；读不到返回 -1）
- `if 距离 < 30:` 下面缩进的那行，什么时候会执行？（提示：离墙不到 30cm 时）
- `距离 < 30` 算出来是数字还是"对/错"？（提示：对/错，布尔值）
- `while True:` 会做几遍？（提示：一直做，直到急停）
- 为什么要在循环外套 `try / except`？（提示：出意外先停车最安全）

> 答得上来，理论就过关啦！翻《实践手册》去造你的第一辆避障小车 💪
