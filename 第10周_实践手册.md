# 第10周 · 实践手册（动手课）🔧
### 给七年级小朋友的「平衡车直立过障 + 灯光报警」第十课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽、没人的直通道**上玩；别在湿/冰/雪/沙地上用。平衡车站得高、重心高，比四足狗更容易摔，**速度别拉太猛**，前方留足空间。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：平衡车（新拼一台）
> 这周换 **平衡车**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 平衡车结构件包；②固定主控到车身（**竖着装**、重心居中）、装两个大轮、接好线；③盯三点（螺丝拧紧 / 两轮对称 / 线别卡住轮子）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **平衡车拼法要点**：只有两个轮子，主控**竖直**固定、让机身站直；两轮要**左右对称**装正，否则走起来会偏。拼好连上电脑，本周就让它在你面前"直立过障 + 报警"。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本次要把平衡车**正面对着前方一个障碍**（比如一个纸箱），离个 1~2 米，这样它才有"障碍"可看。找一条直直的过道，别让它一启动就顶到墙。

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：平衡车直立过障 + 灯光报警（balance_）
```
> 第 10 周就做这一个场景（课程安排里写好的）。它把第4周 测距判断 + 第5周 灯光声音，拼到平衡车上。做完它，下周换轮足机器人还能接着玩。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第五节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🚗 运动 | `ug.balance_move_speed(0, 速度)` 让平衡车直立前进 |
| ③ | 👀 传感器 | `ug.read_distance_data(1)` 读前方距离 |
| ④ | 🚦 三档判断 | `if/elif/else`：远=绿走 / 中=黄减速 / 近=红停 |
| ⑤ | 💡 灯光 | 绿/黄/红灯带，红灯还能"闪" |
| ⑥ | 🔊 声音 | 危险时女声喊"注意障碍" |
| ⑦ | 🧱 函数 | 把"闪红+喊话"打包成 `报警()` |
| ⑧ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 平衡车直立前行（只用运动）
先让平衡车**自己站直、慢慢往前走**，确认它稳稳站住、能走。亮绿灯表示"我在前进"：
```python
# 关1：平衡车直立前进（balance_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

正常速度 = 35
try:
    while True:
        ug.balance_move_speed(0, 正常速度)        # 0=前进，速度 5~80
        ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯 = 我在前进
        time.sleep(0.1)
except:
    ug.stop_chassis()          # 不管出啥意外，先停车最安全
    ug.turn_off_lights()
```
**应当看到**：平衡车自己站直、亮着绿灯慢慢往前走。🎉
> 💡 卡住先想：平衡车要是歪了或走偏，先别慌——按急停，确认两轮装正、主控竖直。第一次跑让它**在直通道里**走，别撞墙。

### 关 2 · 加测距，做出"减速 / 停"两档（叠第4周本事）
在关 1 基础上，加 TOF 测距：远了正常走（绿）、近了减速慢走（黄）、太近停下（不报警先）。注意**先判最危险的"太近"**：
```python
# 关2：平衡车 直立 + 测距减速/停（balance_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

停止距离 = 20     # 厘米，前方小于这个就停
减速距离 = 50     # 厘米，前方小于这个就减速
正常速度 = 35
慢速     = 15

try:
    while True:
        距离 = ug.read_distance_data(1)          # 读前方距离
        if 距离 == -1 or 距离 < 停止距离:        # 最危险：太近或读不到 → 停
            ug.stop_chassis()
            ug.screen_print_text("停!", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯（先不喊话）
        elif 距离 < 减速距离:                      # 中等近：减速慢走
            ug.balance_move_speed(0, 慢速)
            ug.screen_print_text("减速", "FFFFFF00")
            ug.show_light_rgb([0,1,2,3], 255, 255, 0) # 黄灯
        else:                                      # 安全：正常前进
            ug.balance_move_speed(0, 正常速度)
            ug.screen_print_text("前进", "FF00FF00")
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：前面没障碍时绿灯正常走；你把纸箱推近到 50cm 内，它变黄灯、慢下来；推到 20cm 内，它红灯停下。把箱子挪远，它又绿灯走起来。🎉
> 💡 卡住先想：`距离 == -1` 放最前面，是因为"读不到=最危险"，要第一个判断（忘了看《第4周_实践手册》）。想让它在"停"之前更早减速？把 `减速距离=50` 调大一点试试。

### 关 3 · 加闪红 + 语音"注意障碍" + 障碍移开继续走（叠第5周本事）→ 完成过障报警
最后叠灯光语音：危险档除了停 + 红灯，再**闪红 3 下 + 女声喊"注意障碍"**；并且只用 `报警中` 这个开关变量，让"喊话"在**刚遇到障碍时只喊一次**，不会每一圈都喊（刷屏）。障碍一挪开，自动回到绿灯前进。这就是课程安排里的"验收流程"：
```python
# 关3（终极版）：平衡车 直立过障 + 灯光报警（balance_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)               # 音量 1~5，先调好

停止距离 = 20     # 厘米，前方小于这个就停 + 报警
减速距离 = 50     # 厘米，前方小于这个就减速
正常速度 = 35
慢速     = 15
报警中   = False  # 开关变量：True=正在报警中，用来保证"只喊一次"

def 报警():                              # 把"闪红 + 喊话"打包成一个按钮
    for _ in range(3):                   # 红灯闪 3 下
        ug.show_light_rgb([0,1,2,3], 255, 0, 0)
        time.sleep(0.2)
        ug.turn_off_lights()
        time.sleep(0.2)
    ug.play_audio_tts("注意障碍", 0)     # 女声喊话（0=女声）

try:
    while True:
        距离 = ug.read_distance_data(1)
        if 距离 == -1 or 距离 < 停止距离:        # —— 危险档：停 + 闪红 + 喊话 ——
            ug.stop_chassis()
            ug.screen_print_text("警报!", "FFFF0000")
            if not 报警中:               # 刚进入危险才报警一次，避免一直喊
                报警中 = True
                报警()
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 报警后保持红灯亮着
        elif 距离 < 减速距离:                      # —— 减速档：慢走 + 黄灯 ——
            报警中 = False
            ug.balance_move_speed(0, 慢速)
            ug.screen_print_text("减速", "FFFFFF00")
            ug.show_light_rgb([0,1,2,3], 255, 255, 0)
        else:                                      # —— 安全档：正常前进 + 绿灯 ——
            报警中 = False
            ug.balance_move_speed(0, 正常速度)
            ug.screen_print_text("前进", "FF00FF00")
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到（验收五步）**：绿灯正常前进 → 靠近纸箱变黄灯减速 → 太近红灯停下、闪红 3 下 + 女声"注意障碍"、屏幕"警报!" → 你把纸箱挪开 → 它自己变回绿灯继续往前走。🎉🎉🎉
> 💡 卡住先想：`if not 报警中:` 是"如果还没在报警"，所以只在**从安全变危险的那一刻**喊一次；之后 `报警中=True`，循环再转就不会重复喊。`报警中=False` 写在减速档和安全档里，意思是"障碍远了，警报解除，下次再近还能重新喊"。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关1：把 `正常速度=35` 改成 `55`，走更快了吗？（场地要够长！平衡车别撞墙）
- 关2：把 `停止距离=20` 改成 `10`、`减速距离=50` 改成 `30`，停和减速的位置变了吗？
- 关3：把 `报警()` 里 `for _ in range(3):` 的 `3` 改成 `5`，红灯会闪几次？
- 关3：把 `ug.play_audio_tts("注意障碍", 0)` 的 `0`（女声）改成 `1`（男声），声音变了吗？
- 关3：把喊话内容 `"注意障碍"` 改成 `"小心，有障碍"` ，机器人会喊啥？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把平衡车拼好（两轮对称、主控竖直）、连上电脑
- [ ] 我画出了"直立过障 + 报警"的动作清单
- [ ] 关1 跑通：平衡车直立、亮绿灯往前走
- [ ] 关2 跑通：靠近减速(黄灯)、太近停下(红灯)
- [ ] 关3 跑通：危险时闪红 3 下 + 喊"注意障碍" + 屏幕"警报!"，障碍移开自动继续走
- [ ] 我能给家人演示验收五步：**前进→减速→停+闪红喊话→挪开→又走了**

---

## 🃏 随身速查卡（平衡车过障报警常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 平衡车前进 / 后退 | `ug.balance_move_speed(0, 40)` / `(1, 40)` |
| 平衡车左转 / 右转 | `ug.balance_turn_speed(2, 120)` / `(3, 120)` |
| 读前方距离(厘米) | `ug.read_distance_data(1)`（读不到返回 `-1`）|
| 红灯 / 绿灯 / 黄灯 | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` / `(255,255,0)` |
| 灭灯 | `ug.turn_off_lights()` |
| 女声喊话 | `ug.play_audio_tts("注意障碍", 0)` |
| 男声喊话 | `ug.play_audio_tts("注意障碍", 1)` |
| 调音量(1~5) | `ug.set_volume(3)` |
| 屏幕红字 | `ug.screen_print_text("警报!", "FFFF0000")` |
| 造"报警"按钮 | `def 报警():` |
| 急停 | `ug.stop_chassis()` |
| 一直循环 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 `balance_move_speed(方向, 速度)`：方向 0前/1后，速度 5~80；`balance_turn_speed(方向, 速度)`：2左转/3右转，速度 5~280。
> 💡 灯带编号：`0`顶 / `1`左 / `2`右 / `3`底；RGB 三个数范围 **0~255**：红 `(255,0,0)`、绿 `(0,255,0)`、蓝 `(0,0,255)`、黄 `(255,255,0)`、白 `(255,255,255)`。
> 💡 其它形态把 `balance_` 换成 `wheelleg_`(轮足) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车) / `mecanum_`(麦轮车)。

---

## 🚀 进阶挑战（下周迭代方向，先想想）
- **报警后绕开障碍**：危险时别只停，改成闪红喊话后，右转一下再继续走——把 `ug.stop_chassis()` 换成 `ug.balance_turn_speed(3, 120)` + `time.sleep(0.5)` + `ug.stop_chassis()`（接第4周 进阶"绕墙"思路）。
- **记录报警次数**：加一个变量 `次数 = 0`，每次报警 `次数 = 次数 + 1`，用 `ug.screen_print_text("报警"+str(次数), "FFFF0000")` 显示（接第2周 变量与 `str()`）。
- **呼吸绿灯**：平时把 `ug.show_light_rgb(...,0,255,0)` 换成 `ug.show_light_rgb_effect(0, 255, 0, 2)`（effect：0常亮/1关/2呼吸/3闪烁），巡逻灯一呼一吸更带感（接第5周 进阶）。
- **换形态**：把 `balance_` 换成 `transform_`(变形车) 做"越障 + 报警"（第14周预告）。

---

## 🔮 接第11周预告
下一周我们换 **轮足机器人（wheelleg_）**：它腿能屈伸，能在**有高低起伏**的场地走，机身靠陀螺仪保持不摔；还能用"比手势"或"认一个颜色"当目标，让它**追着目标走**（AI 跟随）。这周的"测距判断 + 灯带报警"正好能拿去给轮足做"崎岖路面报警"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把测距 + 灯光语音拼成一条会自动重复的平衡车过障流水线"，一层层来最稳！
