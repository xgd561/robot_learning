# 第2周 · 实践手册（动手课）🔧
### 给七年级小朋友的「屏幕与循环」第二课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整答案。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：麦轮车（沿用上周，基本不重拼）
> 这周还是**麦轮车**，第1周已经拼好啦，本周**不用重新拆装**，直接跳到步骤⓪开机连电脑就行。
> 想练手拆装、或上周没拼利索的，照下面的"麦轮车拼法"再来一遍：四颗**麦克纳姆轮（斜纹轮）**装到底盘四个角，注意**对角两轮斜纹方向一致**（装反了不能横着走）；主控横放到底盘、摄像头朝前。
> 通用流程：关机拔电池 → 按 APP「3D 搭建指引」固定主控、装轮接线 → 盯三点（螺丝拧紧 / 方向装对 / 线别卡轮）→ 装回电池开机连 WiFi 看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要上周的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

## 步骤 ① 先跑通"万能开头 + 一句显示" ✅
把下面这段粘进编辑器（**IP 换成你的**），点 Run，确认屏幕亮字：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

ug.screen_print_text("你好", "FFFF0000")  # 屏幕红字
ug.stop_chassis()
```
**应当看到**：机器人屏幕出现红色"你好"。说明"连接 + 显示"通道都通了 ✅

## 步骤 ② 加一个变量，让数字显示出来 ✅
改一行，把写死的"你好"换成一个会变的变量 `n`：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")

n = 3
ug.screen_print_text(str(n), "FFFF0000")  # 显示数字 3（str 把数字变文字）
ug.play_audio_tts(str(n), 0)              # 女声念"三"
ug.stop_chassis()
```
**应当看到/听到**：屏幕显示 3，同时女声念"三"。✅
> 💡 卡住先想：`str(n)` 是在干啥？—— 把数字 3 变成文字 "3"，屏幕才认得。

## 步骤 ③ 用 for 循环做倒计时（本周核心！）✅
把"手动写 5 遍"换成循环，让它自己 5→1 数：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")

ug.set_volume(3)               # 音量 1~5，先调好

for n in range(5, 0, -1):      # 5,4,3,2,1 每次 -1
    ug.screen_print_text(str(n), "FFFF0000")  # 屏幕显示数字（红）
    ug.play_audio_tts(str(n), 0)              # 女声报数
    ug.mecanum_move_speed_times(0, 30, 1, 0)  # 每数一下前进 1 秒（想纯数数可删这行）

ug.screen_print_text("GO!", "FF00FF00")  # 最后绿字 GO
ug.play_audio_tts("出发", 0)             # 喊"出发"
ug.stop_chassis()                        # 停！安全第一
```
**应当看到/听到**：屏幕 5→1 红字跳动、女声跟着念、每数一下往前走 1 秒，最后绿字 GO + "出发" + 停下。🎉

> 💡 卡住先问自己："for 这行在重复什么？n 每次变多少？"——想清楚再改。

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 把 `range(5, 0, -1)` 改成 `range(10, 0, -1)`，倒计时数到几？
- 把 `"FFFF0000"`（红）全改成 `"FF0000FF"`（蓝），屏幕字变什么颜色？
- 把 `ug.play_audio_tts(str(n), 0)` 的 `0`（女声）改成 `1`（男声），声音变了吗？
- 把前进那行删掉，是不是就"只数数不走路"了？
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 屏幕从 5 倒数到 1，每数一个声音念出来
- [ ] 你能自己改数字：比如改成数到 10（`range(10,0,-1)`）
- [ ] 你能换颜色：比如全改成蓝色 `"FF0000FF"`
- [ ] 你能给家人演示："看，它听我的，还会数数！"

---

## 🃏 随身速查卡（截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 屏幕写字(红) | `ug.screen_print_text("文字", "FFFF0000")` |
| 屏幕写字(蓝) | `ug.screen_print_text("文字", "FF0000FF")` |
| 女声报数 | `ug.play_audio_tts("文字", 0)` |
| 男声报数 | `ug.play_audio_tts("文字", 1)` |
| 调音量(1~5) | `ug.set_volume(3)` |
| 数字变文字 | `str(数字)` |
| 倒计时循环 | `for n in range(5, 0, -1):` |
| 前进 N 秒 | `ug.mecanum_move_speed_times(0, 30, 1, 0)` |
| 急停 | `ug.stop_chassis()` |

---

## 🚀 进阶挑战（选修，学有余力再玩）
想让倒计时更花哨？试试：
- 数到偶数用蓝字、奇数用红字（提示：用 `if n % 2 == 0` 判断奇偶，这是下周要学的"判断"哦，先试着猜一猜）。
- 把 `mecanum_` 换成 `dog_`（四足狗）或 `balance_`（平衡车），换种机器人形态来倒计时（记得你手里是哪种形态）。

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。
