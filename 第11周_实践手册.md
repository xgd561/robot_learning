# 第11周 · 实践手册（动手课）🔧
### 给七年级小朋友的「轮足高低地形适应 + AI 跟随」第十一周课 · 第二节 20:00–20:50

> 📗 这是**实践手册**：理论（教案）搞清楚了，就照下面一步步做。每完成一步打 ✅。
> 卡住了别慌，先自己想、再问小导师；小导师会先给线索，卡死才给完整代码。

---

## ⚠️ 动手前的安全三件事（必看）
- 电脑和机器人要在**同一个 WiFi**。
- 在**空旷、平整、干爽**的地上玩；别在湿/冰/雪/沙地上用。轮足会爬小坡/小坎，但**别找太陡、太滑**的地方，摔了零件容易散。
- 任何时候慌了，就运行 `ug.stop_chassis()` —— 它是急停，最常用。

---

## 🤖 本周形态拼装：轮足（新拼一台）
> 这周换 **轮足**！一套 UGOT 主控能换结构件拼出 7 种形态，**最省事是用手机/平板「UGOT APP → 3D 搭建指引」**跟着 3D 动画拼。通用流程：①关机拔电池、清空桌面、拿出主控 + 轮足结构件包；②固定主控到机身、装两条腿+轮子、接好线；③盯三点（螺丝拧紧 / 腿对称 / 线别卡住腿）；④装回电池开机→连同一 WiFi→设置看 IP。⚠️ 干、平整、室内地面最安全；水/冰/雪/泥/沙别去。
> **轮足拼法要点**：双足 + 轮子混合，两条"腿"装到机身两侧、能屈伸；舵机角度**先归中**再装，避免一开机乱蹬。装完先不通电，用手轻轻掰一下腿，确认能屈伸顺畅。本周它要爬小坡，拼结实点。

## 步骤 ⓪ 确认环境已就绪 ✅
> 这一周不用再装软件啦！只要前面的"地基"还在就行：
1. 右下角 **uCode Link** 还在运行（托盘里那个图标亮着）。
2. 机器人开机、连上和电脑**同一个 WiFi**，IP 记在手边（设置→系统信息）。
3. X240 用 **Chrome 或 Firefox** 打开 https://py.ubtrobot.com ，点黄色 **UGOT → Connect to UGOT** 填 IP 连上。
> 连不上？回《第1周_实践手册》步骤⓪~④ 复查。地基稳，这周才顺。

> 💡 小提醒：本次要准备①一块**有小坡/小坎**的平地（书垫几本当小坡也行，别太高）；②一个**红球/绿积木**当跟随目标。光线亮一点，摄像头才认得准。

---

## 步骤 ① 确定本周大项目 ✅
在编辑器最上面，先写一行注释，把本周项目记下来：
```python
# 我的项目：轮足 高低地形适应 + AI 跟随（wheelleg_）
```
> 第 11 周就做这一个场景（课程安排里写好的）。它把第4周 姿态感知 + 第6/7周 AI 视觉，拼到轮足上。做完它，下周换四足狗还能接着玩。

---

## 步骤 ② 画一张"动作清单"（分解任务）✅
照《教案》第六节，把项目拆成一步步。你可以照着抄、再改成自己的：

| 步骤 | 用哪块积木 | 做啥 |
|---|---|---|
| ① | 🔌 连接 | 万能三行，填 IP |
| ② | 🚗 运动 | `ug.wheelleg_move_speed(0, 速度)` 让轮足走起来 |
| ③ | 🤸 姿态 | `ug.get_attitude()` 读"摇晃"等 7 个是否 |
| ④ | 🛡️ 保护 | 摇晃=True → 减速/停 + 红灯报警（过起伏不摔） |
| ⑤ | 🔴 颜色 | `ug.load_models(['color_recognition'])` 认目标球 |
| ⑥ | 👀 跟随 | 读目标 x：偏左左转 / 偏右右转 / 中间前进 |
| ⑦ | 🛑 安全 | `try/except` + `stop_chassis()` 急停 |

> 💡 卡住先想：你选的大项目，最少需要上面表格里的哪几行？咱们用"三关叠加"一步步加。

---

## 步骤 ③ 三关叠加，跑通最小版 ✅
下面三关**一关一关来**：先跑通关 1，再加关 2，最后加关 3。每关 IP 都换成你的。

### 关 1 · 轮足走起来 + 读出"姿态"（认识高低地）✅
先让轮足**走起来、亮绿灯**，同时把"姿态"读出来看看——把它放到小坡上，看 `get_attitude()` 和 `read_gyro_data()` 怎么变：
```python
# 关1：轮足行走 + 读姿态（wheelleg_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

try:
    while True:
        ug.wheelleg_move_speed(0, 35)             # 0=前进，速度 5~80
        ug.show_light_rgb([0,1,2,3], 0, 255, 0)  # 绿灯=平稳前进
        姿态 = ug.get_attitude()                  # 读 7 个"是/否"：前倾/后倾/左倾/右倾/屏朝上/屏朝下/摇晃
        陀螺 = ug.read_gyro_data()                # 读一串倾斜数字
        ug.screen_print_text("俯仰:" + str(陀螺[0]), "FF00FF00")  # 把"俯仰"数字显示出来看
        time.sleep(0.1)
except:
    ug.stop_chassis()          # 不管出啥意外，先停车最安全
    ug.turn_off_lights()
```
**应当看到**：轮足走起来、绿灯亮；你把它放到小坡上，屏幕上的"俯仰"数字会变（平地上接近 0，上坡变大）。🎉
> 💡 卡住先想：`get_attitude()` 返回 7 个"是/否"，第 7 个（索引 6）是"摇晃"——记住它，关 2 就要用它来护住轮足。先别管前面 6 个，重点认"摇晃"。

### 关 2 · 加"摇晃保护"，过起伏不摔（叠第4周 姿态判断）✅
在关 1 基础上，加一个判断：一旦 `姿态[6]`（摇晃）变 True，说明它快保不住平衡了 → **立刻停 + 闪红灯报警**，护住它不摔。稳了再继续走：
```python
# 关2：轮足 过起伏 + 摇晃保护（wheelleg_）
from ugot import ugot
import time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP

正常速度 = 35

try:
    while True:
        姿态 = ug.get_attitude()       # 7个布尔：前倾/后倾/左倾/右倾/屏朝上/屏朝下/摇晃
        摇晃 = 姿态[6]                 # 第7个(索引6) = "摇晃"
        if 摇晃:                        # 太晃了，快保不住平衡
            ug.stop_chassis()           # 先停，最安全
            ug.screen_print_text("晃!", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)   # 红灯报警
        else:                           # 平稳
            ug.wheelleg_move_speed(0, 正常速度)        # 继续走
            ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 绿灯
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到**：轮足在平地/小坡上绿灯走；你故意轻推它一下让它晃，它立刻红灯停下喊"晃!"，不摔；你松手它稳了又绿灯接着走。🎉
> 💡 卡住先想：`if 摇晃:` 就是"如果摇晃是 True（是）就保护"。轮足自己会用腿保持平衡，我们只是"监工"——它一晃我们就帮它停一下，这就是"过起伏不摔"的保险。

### 关 3 · 加 AI 跟随：追着颜色块走（叠第7周 颜色 + 完成大作品）✅
最后叠"AI 跟随"：加载颜色模型，认出你的目标球（比如红球），读它在画面里的左右位置 `x`，偏左就左转、偏右就右转、中间就前进——一路追着球跑。危险时（摇晃）仍先保护。这就是课程安排里的"验收流程"：
```python
# 关3（终极版）：轮足 过起伏保护 + 颜色块跟随（wheelleg_）
from ugot import ugot
import json, time
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # ← 换成机器人真实 IP
ug.set_volume(3)               # 音量 1~5，先调好

正常速度 = 35
目标颜色 = "red"              # ← 你想让它追的颜色，比如红球（写 "red"）

try:
    ug.load_models(['color_recognition'])   # 唤醒"颜色眼睛"
    while True:
        # —— 第一部分：摇晃保护（过起伏不摔）——
        姿态 = ug.get_attitude()
        if 姿态[6]:                          # 太晃 → 先停 + 报警，这圈不追
            ug.stop_chassis()
            ug.screen_print_text("晃!", "FFFF0000")
            ug.show_light_rgb([0,1,2,3], 255, 0, 0)
            time.sleep(0.1)
            continue                         # 跳过下面，等稳了再追

        # —— 第二部分：AI 跟随（追颜色块）——
        raw = ug.get_color_total_info()
        data = json.loads(raw)
        if data['total'] > 0:
            颜色 = data['info'][0]['color']
            x = data['info'][0]['x']         # 目标在画面里的左右位置（小=左，大=右）
            if 颜色 == 目标颜色:
                ug.show_light_rgb([0,1,2,3], 0, 255, 0)   # 看到目标→绿灯
                if x < 120:                  # 目标在左 → 左转对准
                    ug.wheelleg_turn_speed(2, 100)
                    ug.screen_print_text("左转追", "FFFFFF00")
                elif x > 200:                # 目标在右 → 右转对准
                    ug.wheelleg_turn_speed(3, 100)
                    ug.screen_print_text("右转追", "FFFFFF00")
                else:                        # 目标在中间 → 前进追上
                    ug.wheelleg_move_speed(0, 正常速度)
                    ug.screen_print_text("前进追", "FF00FF00")
            else:
                ug.stop_chassis()            # 看到别的颜色，先停等
                ug.screen_print_text("等目标", "FFFFFFFF")
        else:
            ug.stop_chassis()                # 没看到目标，先停
            ug.screen_print_text("找目标", "FFFFFFFF")
        time.sleep(0.1)
except:
    ug.stop_chassis()
    ug.turn_off_lights()
```
**应当看到（验收两步）**：① 轮足过起伏地不摔（摇晃会自动红灯保护）；② 你把红球放在它眼前移动，它**盯着红球、一路追着跑**——球往左它左转、往右它右转、正前方它就冲上去。🎉🎉🎉
> 💡 卡住先想：`x < 120` / `x > 200` 是"偏左/偏右"的门槛，相机画面宽约几百、中心在中间。如果你的球在中间它却乱转，把 120 调小、200 调大（比如 100 / 220）试试。认不出球？光线亮一点、球离镜头 30~60 厘米最舒服（见《第7周_实践手册》）。

---

## 步骤 ④ 改造它（调试思维练习）✅
改一改、看看会发生什么（改完点 Run）：
- 关2：把 `正常速度=35` 改成 `20`，过坡是不是更稳、更不容易晃？
- 关3：把 `目标颜色 = "red"` 改成 `"green"`，换成绿球它才追，红球就不理了——试试！
- 关3：把 `x < 120` 的 `120` 改成 `80`、`x > 200` 的 `200` 改成 `240`，"正前方"的范围变宽还是变窄了？
- 关3：把前进速度 `正常速度` 调到 `50`，追球更猛了吗？（场地要够大！）
- 报错也别怕——**报错是老师在说话**，把红字念给小导师听。

---

## 📌 本周完成标准（打勾 = 通关）
- [ ] 我把轮足拼好（两腿对称、舵机归中）、连上电脑
- [ ] 我读出了"姿态"，知道第7个"摇晃"代表快倒了
- [ ] 关2 跑通：轮足过起伏地，摇晃时红灯停下保护、不摔
- [ ] 关3 跑通：轮足追着指定颜色块走（偏左左转/偏右右转/中间前进）
- [ ] 我能给家人演示验收两步：**过起伏不摔 + 追着目标走**

---

## 🃏 随身速查卡（轮足高低地形 + AI 跟随常用 API，截图保存）
| 想做的事 | 代码 |
|---|---|
| 连接机器人 | `ug.initialize("你的IP")` |
| 轮足前进 / 后退 | `ug.wheelleg_move_speed(0, 40)` / `(1, 40)` |
| 轮足左转 / 右转 | `ug.wheelleg_turn_speed(2, 120)` / `(3, 120)` |
| 读姿态(7个是否) | `姿态 = ug.get_attitude()` → 前倾/后倾/左倾/右倾/屏朝上/屏朝下/**摇晃** |
| 判"摇晃"(第7个) | `if 姿态[6]:` （True=正在晃，快保护） |
| 读陀螺仪数字 | `陀螺 = ug.read_gyro_data()`（[俯仰,横滚,偏航,...]） |
| 唤醒颜色眼睛 | `ug.load_models(['color_recognition'])` |
| 读颜色结果 | `raw = ug.get_color_total_info()` → `data = json.loads(raw)` |
| 取颜色名 | `data['info'][0]['color']` |
| 取目标左右位置 | `x = data['info'][0]['x']`（小=左，大=右） |
| 红灯 / 绿灯 | `ug.show_light_rgb([0,1,2,3], 255,0,0)` / `(0,255,0)` |
| 女声说话 | `ug.play_audio_tts("文字", 0)` |
| 急停 | `ug.stop_chassis()` |
| 一直循环 | `while True:` |
| 歇 0.1 秒 | `time.sleep(0.1)` |
| 安全网急停 | `try: ... except: ug.stop_chassis()` |

> 💡 方向数字：前进=0，后退=1，左转=2，右转=3；`wheelleg_move_speed(方向, 速度)` 速度 5~80，`wheelleg_turn_speed(方向, 速度)` 速度 5~280。
> 💡 灯带编号：`0`顶 / `1`左 / `2`右 / `3`底；RGB 三个数范围 **0~255**：红 `(255,0,0)`、绿 `(0,255,0)`、蓝 `(0,0,255)`、黄 `(255,255,0)`、白 `(255,255,255)`。
> 💡 其它形态把 `wheelleg_` 换成 `balance_`(平衡车) / `dog_`(四足狗) / `spider_`(蜘蛛) / `transform_`(变形车) / `mecanum_`(麦轮车)。

---

## 🚀 进阶挑战（选修，学有余力再玩）
- **手势跟随（你就是目标）**：把颜色模型换成手势 `ug.load_models(['gesture'])`，比"手掌 palm"它就前进追你、比"点赞 good"就停、比"石头 stone"后退——做成"你走到哪它跟到哪"：
  ```python
  ug.load_models(['gesture'])
  raw = ug.VISION.gesture_inference()
  手势 = json.loads(raw).get('gesture')
  if 手势 == 'palm':    ug.wheelleg_move_speed(0, 35)   # 手掌→追你
  elif 手势 == 'good':  ug.stop_chassis()               # 点赞→停
  elif 手势 == 'stone': ug.wheelleg_move_speed(1, 35)   # 石头→后退
  ```
- **边追边报状态**：追球时语音播报"我在追红球"，摇晃时喊"小心，我晃了"（声音函数见第5周速查卡）。
- **记录爬坡次数**：加变量 `次数=0`，每次检测到"前倾或后倾"（姿态[0]/姿态[1] 为 True）就 `次数=次数+1`，屏幕显示"爬坡 N 次"（接第2周 变量）。
- **换形态**：把 `wheelleg_` 换成 `transform_`(变形车) 做"翻台阶 + 跟随"（第14周预告）。

---

## 🔮 接第12周预告
下一周我们换 **四足狗（dog_）**：用仿生步态走路，还能把"走/跑/转身"封装成 `walk()` / `run()` / `turn()` 函数；每切换一个动作就用语音播报"我在走 / 我在跑"。这周的"姿态保护 + AI 跟随"正好能拿去给四足狗做"不平地面不摔 + 追目标"哦～

---

> 卡住了随时来问小导师～ 先给线索，实在卡死我再给你完整代码，不急。这周重点是"把姿态感知 + AI 视觉拼成一台会爬坡、会追球的轮足"，一层层来最稳！
