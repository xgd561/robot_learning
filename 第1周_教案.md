# 第1周 · 教案（理论课）🤖
### 给七年级小朋友的「让机器人听我话」第一课 · 第一节 19:00–19:50

> 📘 这是**教案**：先把它读完、搞清楚下面的道理，再去翻《实践手册》动手操作。
> 本节不要求你当场写代码，先听懂"电脑怎么让机器人动起来"这件事。

---

## 🎯 本周学习目标（先看清这周要干成啥）

这周是我们和 UGOT 机器人的**第一次握手**。学完这一周，你要能做到这些——

**✅ 这周结束，你要能：**
1. **说清楚**电脑是怎么"命令"机器人动起来的（关键词：写代码 → 发命令 → 机器人动，靠 WiFi 传）。
2. **装好并跑起** uCode Link 这个"传声筒"小软件（这是连上机器人的前提）。
3. **知道 IP 是啥**：能说出"一串数字 = 机器人在 WiFi 里的门牌号"，并且会从机器人「设置→关于」里找到它、抄下来。
4. **亲手连上机器人**，让它前进 2 秒、屏幕上亮出"我连上啦"——也就是**真真切切控制它动一下**。
5. **懂"烧录并运行"**：知道点那个按钮是在"把程序存进机器人 + 让它立刻跑"，红灯闪一下表示"收到"。

**🚀 这周学完，你会多一个超能力：**
> 以前机器人对你是个"黑盒子"；这周之后，你知道了——**只要在电脑上写几行字，它就得听你的**。这就是编程最爽的地方，记住这种"我能控制它"的感觉！

**📌 一句话目标：** 把电脑和机器人说上话，让它真的动一下，拿到"我能控制它"的成就感。这一步通了，后面每一周我们都能让它做更厉害的事。

---

## 一、电脑和机器人怎么"对话"？
想象机器人是个听不懂人话、但听得懂"数字命令"的小 puppet（木偶 / 提线娃娃）。
- 你在电脑上写一行 Python 代码，意思是"前进"；
- 这行代码变成一条小消息，通过 **WiFi** 飞到机器人那里；
- 机器人收到，就真的往前走啦！

所以整件事就是：**写代码 → 发命令 → 机器人动**。就这么简单。

## 二、为什么用"在线编辑器"，不装 Python？
你这台电脑是 **Windows 7** 的老机器（ThinkPad X240）。如果在它上面自己装 Python 和 ugot 库，很容易装到一半报错、卡死，会很打击信心。

所以我们走**官方给的零安装路线**：
- 装一个叫 **uCode Link** 的小软件，让它一直开着（右下角托盘里那个图标）；
- 然后用 **Chrome 或 Firefox 浏览器**打开 https://py.ubtrobot.com/ 写代码；
- 网页编辑器是借着本机的 uCode Link 才能连上机器人的。

> 💡 一句话记住：**uCode Link 是"传声筒"，网页编辑器是"写字的地方"。**
> 📦 它怎么来的？从优必选官网下载一个安装包（Windows 叫 `uCodeLink-win...v4.4.1.exe`），双击装好就行——具体下载地址和"装好后去右下角看图标"的操作，写在《实践手册》「步骤⓪ 安装 uCode Link」里，动手时照着做。

## 三、两个千万别踩的坑
- 🔴 **WiFi 必须同一个**：电脑和机器人要连同一个路由器（或者电脑连机器人自己发出的热点）。不在同网，命令就飞不过去。
- 🔴 **浏览器绝不能用 IE / 旧版 Edge**：Win7 上请用 **Firefox ESR 115** 或 **Chrome 109**（这是最后支持 Win7 的版本）。用 IE 会打不开编辑器。

## 四、我们有两台电脑，怎么分工？（重点）
- **X240（Win7）**：负责本周"地基"——用网页编辑器把机器人连上、跑通。这台别硬装 Python。
- **HP 新电脑（Win10）**：✅ 可以完整体验"第三方编程工具"路线（自己装 Python + ugot SDK 写代码）。等你想玩更自由的玩法时，换这台机器。
- 本周先用 X240 把网页编辑器跑通，拿到成就感最重要。

## 五、连接器：为什么那几行代码就能"抓住"机器人？

无论用哪种方式，代码最前面都有这三行，它们就是"先连上机器人"的意思：
```python
from ugot import ugot
ug = ugot.UGOT()
ug.initialize("192.168.x.x")   # 换成机器人设置→关于里的真实 IP
```
下面一个一个讲，为什么就这三行、孩子也能懂。

### 1）先搞懂：IP 是什么？一串数字凭什么就是"机器人"？
你肯定知道：家家户户都有**门牌号**，快递员按门牌号才能把包裹送到你家，不会送错。
电脑、手机、机器人连上 WiFi 以后，路由器（就是家里那个发 WiFi 的小盒子）也会给它们每个人发一个**"网络门牌号"**——这个门牌号就是一串数字，比如 `192.168.1.25`。这串数字有个名字叫 **IP 地址**（英文 IP Address，你就记成"网络门牌号"就行）。

所以：
- 机器人连上 WiFi → 路由器给它发了一个 IP（门牌号）；
- 你的电脑也连同一个 WiFi → 电脑也有自己的 IP；
- 电脑要"喊"机器人，就得**先知道机器人的门牌号**，才能把命令准确送到它家门口。
- 那串你看着莫名其妙的数字，不是随便写的——它**就是机器人在这个 WiFi 里的"家庭住址"**。

> 🔍 在哪看这个门牌号？机器人开机 → 「设置 → 系统信息（关于）」里就写着。把它抄下来，待会儿填进代码。

### 2）为什么"同一个 WiFi"那么重要？
想象机器人住 A 小区、电脑在 B 小区，两个小区门牌号各发各的，快递（命令）根本送不过去。
只有**电脑和机器人连同一个 WiFi（同一个小区）**，它们的门牌号才在同一套体系里，命令才能"按门牌号找到机器人"。
这也就是为什么前面一直强调：连机器人前，先确认电脑和它在同一个 WiFi。

### 3）逐行看懂这三行在干啥
- **`from ugot import ugot`**
  意思是"把控制机器人的工具箱（ugot 库）拿过来用"。就像你要修东西，先把手边工具箱打开、把螺丝刀掏出来。不拿工具箱，后面一句命令都发不出去。
- **`ug = ugot.UGOT()`**
  意思是"造一个遥控器对象，起名叫 `ug`"。`ug` 就是你要用的那个"遥控器"，后面所有"前进、亮灯、说话"都通过它发。起名 `ug` 只是方便打字，你也可以叫它 `robot`、`kong`，但咱们统一用 `ug`。
- **`ug.initialize("192.168.x.x")`**
  意思是"按这个门牌号（IP），拨通机器人"。把刚才抄的机器人 IP 填进引号里，这一行一跑，电脑就通过 uCode Link（传声筒）找到机器人、跟它握上手。**这一步成功，后面才能发命令**；IP 填错或不在同 WiFi，这里就会连不上（报错也是在提醒你：门牌号写错或送错小区了）。

### 4）一句话串起来
"打开工具箱 → 造个遥控器叫 ug → 按机器人的门牌号拨通它" = 这三行。
只要这三行跑通，后面你想让它前进、亮灯、说话，都只要接着写 `ug.xxx(...)` 就行。门牌号（IP）就是连接的关键，记牢它！

## 六、这周学完，你应该能回答这几个问题
- 机器人是怎么"收到"电脑命令的？（提示：WiFi）
- 为什么这台老电脑不直接装 Python？（提示：Win7 容易装不上）
- uCode Link 是干什么的？（提示：传声筒/桥接）
- 连机器人之前，必须先知道什么数字？（提示：IP）

> 答得上来，就说明理论过关啦！翻《实践手册》去动手吧 💪
